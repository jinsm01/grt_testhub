## 2026-06-24 Bug分析-增加云效实时同步入口

### 问题
Bug分析页面数据来源只有用户手动上传 Excel 一种方式，字段写死且无法实时获取云效平台的最新 Bug 数据，导致：
1. 每次分析前需要手动导出 Excel，操作繁琐
2. 数据时效性差，无法反映当前迭代的实时质量状态
3. 不支持按项目/迭代维度拉取，粒度粗糙

### 修改方案
1. **后端新增云效 API 客户端** (`apps/data_factory/yunxiao_client.py`)
   - 封装云效 OpenAPI：项目搜索、迭代列表、Bug 搜索、字段查询
   - 支持分页拉取（每页 100 条，最大 2000 条）
   - 支持动态字段映射：先获取项目字段列表，再根据字段 `identifier` 提取值，兼容不同项目的自定义字段
   - 支持中心版（需 `organization_id`）和公共版两种模式

2. **数据源适配器扩展** (`apps/data_factory/bug_source_adapter.py`)
   - 新增 `from_yunxiao(params)` 静态方法
   - 复用现有 `NormalizedBug` 标准化逻辑，确保与 Excel 上传的数据结构一致
   - 关键字段动态提取：`title`/`description`/`status`/`severity`/`priority`/`module`/`reporter`/`assignee`/`type`/`created_at`/`closed_at`
   - 不影响现有 Excel 分析流程

3. **后端新增 API 端点** (`apps/data_factory/bug_analysis_view.py` + `urls.py`)
   - `POST /yunxiao/projects/`：获取项目列表（代理接口，前端直接调用）
   - `POST /yunxiao/sprints/`：获取迭代列表（代理接口）
   - `POST /yunxiao/sync/`：同步 Bug 并执行分析，返回结构与 `analyze_bug_excel` 完全一致
   - 分析结果自动保存到 `BugAnalysisRecord`，`source_type='yunxiao_api'`，便于后续追溯

4. **前端新增同步入口** (`frontend/src/views/data-factory/BugAnalysis.vue`)
   - 列表视图筛选栏新增「从云效同步」绿色按钮，与「选择文件」并列
   - 新增 `el-dialog` 表单：访问令牌、组织 ID、项目选择器（远程搜索）、迭代选择器（级联加载）、版本标签、最大拉取数量
   - 项目选择器使用 `remote` 模式，输入关键词实时搜索云效项目
   - 项目切换后自动加载该项目下的迭代列表
   - 同步成功后自动跳转到详情视图，展示分析图表，并刷新历史记录列表

5. **前端 API 扩展** (`frontend/src/api/data-factory.js`)
   - 新增 `getYunxiaoProjects`、`getYunxiaoSprints`、`syncFromYunxiao` 三个接口函数

### 涉及文件
- `apps/data_factory/yunxiao_client.py` (新增)
- `apps/data_factory/bug_source_adapter.py` (修改)
- `apps/data_factory/bug_analysis_view.py` (修改)
- `apps/data_factory/urls.py` (修改)
- `frontend/src/api/data-factory.js` (修改)
- `frontend/src/views/data-factory/BugAnalysis.vue` (修改)

### 踩坑点
1. **云效 API 字段标识差异**：不同项目的字段 `identifier` 可能不同（如 `custom_field_xxx`），必须先调用 `get_fields(space_id)` 获取字段映射，再根据映射提取值。
2. **分页拉取需控制上限**：云效 Workitem 搜索接口 `pageSize` 最大 200，超过会报错，故设置 per_page=100，通过 while 循环分页。
3. **中心版 vs 公共版**：中心版需在 Header 中传 `x-yunxiao-organization`，公共版不需要；API 域名也可能不同，需通过 `domain` 参数兼容。
4. **Token 安全**：前端使用 `type="password"` + `show-password` 隐藏令牌，后端接口要求 `IsAuthenticated`，避免 Token 泄露。
5. **兼容现有分析流程**：云效同步返回的数据结构必须与 Excel 上传完全一致（`modules`、`feature_detail`、`severity_cross`、`severity`、`status`、`priority`、`keywords`、`creator_module`、`timeline`、`risk`、`cluster`、`test_focus`、`meta`），否则详情页图表渲染会报错。

---

## 2026-06-24 云效 API 联调踩坑记录

### 问题1：项目列表返回 404
**现象**：调用 `POST /oapi/v1/projex/organizations/{orgId}/projects:search` 返回 404。
**原因**：
- 错误1：API 路径使用了单数 `/organization/`（旧版），新版中心版必须是复数 `/organizations/`
- 错误2：Header 中使用了 `Authorization: Bearer {token}`，云效中心版要求 `x-yunxiao-token: {token}`
- 错误3：域名错误，使用了前端页面域名 `devops.aliyun.com`，实际 API 域名应为 `openapi-rdc.aliyuncs.com`
**修复**：
```python
# yunxiao_client.py
self.session.headers.update({
    "Content-Type": "application/json",
    "x-yunxiao-token": self.token,
})
# URL 路径修正为复数
return f"{self.domain}/oapi/v1/projex/organizations/{self.organization_id}{api_path}"
# 域名修正
YUNXIAO_DOMAIN = "https://openapi-rdc.aliyuncs.com"
```

### 问题2：响应格式不一致（list vs dict）
**现象**：`_request` 方法里统一用 `resp.json()` 解析，但云效不同接口返回格式不同：
- `projects:search` → 返回数组 `[{...}, {...}]`
- `workitems:search` → 返回数组 `[{...}, {...}]`
- 其他接口 → 返回字典 `{"success": true, "data": {...}}`
**错误**：`data.get("success", True)` 在 data 是 list 时报 `'list' object has no attribute 'get'`。
**修复**：
```python
data = resp.json()
if isinstance(data, list):
    return data
# 只有 dict 才检查 errorCode / success
if isinstance(data, dict) and "errorCode" in data:
    raise YunxiaoAPIError(...)
```

### 问题3：迭代列表接口路径错误
**现象**：调用 `POST /sprints:search` 返回 404。
**原因**：云效没有 `sprints:search` 这个接口。正确的迭代列表接口是 `GET /projects/{id}/sprints`。
**修复**：
```python
def list_sprints(self, space_id: str, ...):
    url_path = f"/projects/{space_id}/sprints"
    return self._request("GET", url_path, params=params)
```

### 问题4：前端 axios 响应处理错误
**现象**：后端返回 200 且 body 正确，但前端显示"获取项目列表失败"。
**原因**：前端代码写了 `if (res.success)`，但 axios 的 `res` 是 response 对象，`res.success` 是 undefined（假值），走了 else 分支。
**修复**：
```javascript
// 应该是 res.data.success
if (res.data && res.data.success) {
  yunxiaoProjects.value = res.data.items || []
} else {
  ElMessage.error((res.data && res.data.message) || '获取项目列表失败')
}
```

### 问题5：自定义字段未解析
**现象**：同步后 severity、priority、module 等字段为空。
**原因**：云效新版把自定义字段放在 `customFieldValues` 数组中，格式为：
```json
[
  {"fieldName": "严重程度", "fieldFormat": "list", "values": [{"displayValue": "3-一般"}]},
  {"fieldName": "优先级", "fieldFormat": "list", "values": [{"displayValue": "高"}]}
]
```
旧代码只处理了 `customFields` / `fieldValueMap`（字典格式）。
**修复**：新增 `_extract_custom_field_values` 函数，遍历 `customFieldValues` 数组，根据 `fieldFormat` 提取 `displayValue`。

### 问题6：工作项类型（type）未取到
**现象**：图表顶部"缺陷"、"线上故障"统计为 0。
**原因**：代码只从 `item.get("category")` 或 `item.get("type")` 获取，但云效实际字段是 `workitemType`（对象格式 `{"name":"缺陷","id":"..."}`）。
**修复**：
```python
"type": _extract_name(item.get("workitemType")) or item.get("category") or item.get("type", CATEGORY_BUG),
```

### 问题7：创建者统计为空
**现象**："创建者 × 模块分布"图表不显示数据。
**原因**：`convert_yunxiao_bugs` 设置了 `reporter` 字段，但后端 `_compute_creator_data` 使用的是 `creator` 字段。
**修复**：同时设置 `creator` 和 `reporter`：
```python
"creator": _extract_name(item.get("creator")),
"reporter": _extract_name(item.get("creator")),
```

### 问题8：状态/创建者/指派给等对象字段解析
**现象**：`status` 显示为 `[object Object]`。
**原因**：云效返回的是对象 `{"name":"待确认","displayName":"待确认","id":"28"}`，旧代码直接赋值。
**修复**：新增 `_extract_name` 辅助函数，优先取 `displayName`，其次 `name` / `nickName` / `realName`。

---

## 2026-06-24 图表扩展：支持云效自定义字段（参与者分布）

### 问题
用户反馈：
1. 图表无法看出是否同步全了
2. 希望支持基于同步数据拓展更多图表
3. 云效 Bug 有自定义的"参与者"字段，需要在图表中体现
4. Bug 工作项类型没有取到，导致顶部统计项（缺陷/线上故障）为空

### 修改方案
1. **修复 type 字段提取**（`yunxiao_client.py`）
   - 从 `workitemType` 对象中提取 `name`，映射到 `type` 字段
   - 这样 `metaData.work_types` 能正确统计"缺陷"数量

2. **修复 creator 字段缺失**（`yunxiao_client.py`）
   - 云效 `creator` 是对象，提取名称后同时赋值给 `creator` 和 `reporter`
   - 后端 `_compute_creator_data` 依赖 `creator` 字段

3. **解析 customFieldValues 数组**（`yunxiao_client.py`）
   - 新增 `_extract_custom_field_values` 函数，处理 `fieldFormat` 为 `list` / `multiList` / `user` / `date` 等情况
   - 所有自定义字段存入 `bug["custom_fields"]` 字典
   - 同时保留 `_raw` 原始数据，供前端展示任意字段

4. **后端新增参与者统计**（`bug_analysis.py`）
   - 新增 `_compute_participant_data(bugs)` 函数
   - 自动识别 `custom_fields` 中的常用参与者字段名：`参与者`、`参与人`、`相关人员`、`协同人`、`参与人员`
   - 支持字符串（逗号/分号分隔）和列表两种格式
   - 返回 `participantData: { "张三": 5, "李四": 3, ... }`

5. **前端新增参与者分布图表**（`BugAnalysis.vue`）
   - 新增 `participantChartRef` 和 `participantData` ref
   - 新增 `renderParticipantChart` 函数：横向柱状图，取 Top 20
   - 图表卡片放在"创建者 × 模块分布"和"Bug 创建时间趋势"之间
   - 仅当 `participantData` 非空时显示

### 涉及文件
- `apps/data_factory/yunxiao_client.py`
- `apps/data_factory/bug_analysis.py`
- `frontend/src/views/data-factory/BugAnalysis.vue`

### 踩坑点
1. **云效自定义字段名不固定**：不同项目可能使用不同的字段名（如"参与者" vs "参与人"），需要用一组候选名匹配。
2. **参与者字段值格式多样**：可能是字符串（逗号分隔多人）、列表（对象列表）、或单个人名对象，需要统一处理。
3. **图表数据为空时的隐藏**：使用 `v-if="Object.keys(participantData).length > 0"` 控制卡片显示，避免空图表占位。
4. **Vue ref 未定义导致 linter 报错**：新增 `participantData` 和 `participantChartRef` 必须在 `<script setup>` 的 ref 声明区同步添加，否则编译报错。
