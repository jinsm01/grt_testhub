## 2026-06-25 Bug分析页面颜色调整（红黄绿过重改为淡雅风格）

### 问题
用户反馈Bug分析页面中的红黄绿颜色过重，视觉效果不够柔和。希望保持颜色类型不变，但调整为更淡雅的风格，类似平台中使用的浅紫色调（如 `#f8f7ff`、`#f5f3ff`、`#7b42f6`）。

### 根因
原有的颜色配置使用了较深的红黄绿色：
- 红色系：`#e74c3c`、`#e94560`、`#ff4d4f`（深红、玫红）
- 黄色系：`#f39c12`、`#e67e22`（橙色、深橙）
- 绿色系：`#27ae60`、`#2ecc71`、`#52c41a`（深绿、浅绿）
- 蓝色系：`#3498db`、`#1890ff`（蓝色）

这些颜色在图表标题、渐变色、图标背景等多个地方使用，整体视觉偏"重"，缺乏现代UI的淡雅感。

### 修改方案
将所有红黄绿颜色调整为淡雅版本，参考平台浅紫色调的风格：

#### 1. 图表颜色调整
**P0/P1/P2 严重度图表**（第2353行、2363行）：
- P0（致命）：`#e74c3c` → `#f8b4b4`（淡粉）
- P1（严重）：`#e94560` → `#ffcdd2`（浅粉）
- P2（一般）：`#3498db` → `#a8d8ea`（淡蓝）

**状态分布图表**（第2370行）：
- `#27ae60` → `#a8e6cf`（淡绿）
- `#f39c12` → `#ffeaa7`（淡黄）
- `#e94560` → `#ffcdd2`（淡粉）
- `#e74c3c` → `#f8b4b4`（淡粉）
- 其他颜色也相应调整为淡雅版本

**优先级分布图表**（第2376行）：
- 颜色数组整体调整为淡雅版本

**模块分布、关键词、创建者、参与者等图表**（多处）：
- 图表标题颜色：`#e94560` → `#b88bd6`（淡紫）
- 渐变色调整为淡粉渐变：`#ffcdd2` 到 `#f5f5f5`

**时间趋势图表**（第2453行）：
- 线条颜色：`#e94560` → `#ffcdd2`（淡粉）
- 区域渐变：`rgba(233,69,96,0.15)` → `rgba(255,205,210,0.3)`（淡粉透明）

#### 2. 图标背景颜色调整
**摘要卡片图标**（第255、268、281、294行）：
- 缺陷图标：`background: #fef0f0; color: #e94560` → `background: #fff5f7; color: #f8b4b4`
- 线上故障图标：`background: #fff2f0; color: #ff4d4f` → `background: #fff5f7; color: #f8b4b4`
- 功能模块图标：`background: #e6f7ff; color: #1890ff` → `background: #f0f7ff; color: #a8d8ea`
- 缺陷聚集点图标：`background: #f6ffed; color: #52c41a` → `background: #f0fff7; color: #a8e6cf`

#### 3. CSS 渐变色调整
**AI风险加载动画**（第3541行）：
- `linear-gradient(135deg, #e74c3c 0%, #c0392b 100%)` → `linear-gradient(135deg, #f8b4b4 0%, #ffcdd2 100%)`

**风险级别头部渐变**（第3640-3650行）：
- P0：`linear-gradient(135deg, #e74c3c 0%, #c0392b 100%)` → `linear-gradient(135deg, #f8b4b4 0%, #ffcdd2 100%)`
- P1：`linear-gradient(135deg, #f39c12 0%, #e67e22 100%)` → `linear-gradient(135deg, #ffeaa7 0%, #ffd9a0 100%)`
- P2：`linear-gradient(135deg, #27ae60 0%, #2ecc71 100%)` → `linear-gradient(135deg, #a8e6cf 0%, #b8e6cf 100%)`

**风险数字徽章**（第3358行）：
- `linear-gradient(135deg, #ef4444 0%, #dc2626 100%)` → `linear-gradient(135deg, #f8b4b4 0%, #ffcdd2 100%)`

#### 4. 新的颜色体系
保持红黄绿蓝紫的颜色类型，但使用淡雅版本：
- 淡红/粉红系：`#f8b4b4`、`#ffcdd2`、`#ffb3c1`
- 淡黄系：`#ffeaa7`、`#ffd9a0`
- 淡绿系：`#a8e6cf`、`#b8e6cf`
- 淡蓝系：`#a8d8ea`
- 淡紫系：`#b88bd6`、`#c9b8d6`

### 涉及文件
- `frontend/src/views/data-factory/BugAnalysis.vue`

### 效果对比
| | 修改前 | 修改后 |
|---|---|---|
| 红色系 | `#e74c3c`（深红） | `#f8b4b4`（淡粉） |
| 黄色系 | `#f39c12`（橙色） | `#ffeaa7`（淡黄） |
| 绿色系 | `#27ae60`（深绿） | `#a8e6cf`（淡绿） |
| 标题颜色 | `#e94560`（玫红） | `#b88bd6`（淡紫） |
| 视觉效果 | 颜色过重，视觉冲击强 | 柔和淡雅，类似平台浅紫风格 |

### 踩坑点
1. **颜色调整要保持语义一致性**：P0（高风险）仍然用红色系，P1用黄色系，P2用绿色系，只是调淡而不是改变颜色类型。
2. **渐变色两端都要调整**：渐变的起止颜色都需要调整为淡雅版本，否则渐变效果会不一致。
3. **透明度渐变颜色值要转换**：`rgba(233,69,96,0.15)` 需要转换为对应的淡色版本 `rgba(255,205,210,0.3)`，注意RGB值要对应新的颜色。
4. **多处颜色定义需要批量修改**：图表标题、图表数据颜色、CSS渐变、图标背景等多个地方都需要同步修改，遗漏一处会导致整体风格不一致。
5. **淡色背景上的白色字看不清**：背景调淡后，如果文字还是白色，会导致可读性问题。需要在淡色背景上使用对应的深色文字：
   - P0区块（淡粉背景）：文字改为深红 `#c0392b`
   - P1区块（淡黄背景）：文字改为深黄 `#e67e22`
   - P2区块（淡绿背景）：文字改为深绿 `#27ae60`
   - 淡蓝背景：文字改为深蓝 `#2563eb`
6. **风险样式要回归平台风格**：用户反馈调整后的颜色风格过于淡，需要回归到与平台风格一致的淡雅设计：
   - 使用极淡的背景渐变：P0用 `#fff5f7→#ffeef2`（极淡红），P1用 `#fffaf0→#fff5e6`（极淡黄），P2用 `#f0fdf4→#ecfdf5`（极淡绿）
   - 使用rgba透明边框：如 `rgba(239, 68, 68, 0.2)`、`rgba(245, 158, 11, 0.2)`、`rgba(34, 197, 94, 0.2)`
   - 使用rgba透明图标背景：如 `rgba(239, 68, 68, 0.1)`
   - 使用中等深度的文字颜色：如 `#e53935`、`#f59e0b`、`#22c55e`（类似平台的 `#7b42f6`）
   - AI风险相关样式统一调整：risk-item、risk-num、ai-loading等全部使用相同的淡雅风格
   - 保持风险语义（红黄绿），但整体风格与平台浅紫色调保持一致
10. **图表分析tab整体颜色偏淡优化**：用户反馈图表分析tab下整体颜色偏淡，需要调整得更醒目：
    - **图表标题颜色**：从淡紫 `#b88bd6` 改为深紫 `#7b42f6`，参与者图表标题用蓝色 `#3b82f6`
    - **图表数据颜色调整**：
      - **模块分布**：渐变色从 `#ffcdd2→#f5f5f5` 改为 `#e53935→#ffcdd2`（深红到淡粉）
      - **严重度交叉图**：P0/P1/P2从淡粉/淡黄/淡蓝改为深红 `#e53935`、深黄 `#f59e0b`、深绿 `#22c55e`
      - **严重度饼图**：颜色数组改为 `['#e53935','#f59e0b','#95a5a6','#22c55e']`
      - **状态饼图**：改为中等深度颜色 `['#22c55e','#95a5a6','#f59e0b','#3b82f6','#e53935','#7b42f6','#ef4444','#06b6d4','#64748b']`
      - **优先级饼图**：改为 `['#ef4444','#e53935','#f59e0b','#3b82f6','#7b42f6','#64748b']`
      - **关键词图表**：渐变色从 `#f5f5f5→#ffcdd2` 改为 `#ffcdd2→#e53935`（淡粉到深红）
      - **创建者/参与者图表**：颜色数组改为中等深度颜色，如 `['#e53935','#3b82f6','#f59e0b','#7b42f6','#22c55e','#ec4899','#06b6d4','#ef4444','#64748b','#10b981','#f97316','#a855f7']`
      - **时间趋势图表**：线条从淡粉 `#ffcdd2` 改为深红 `#e53935`，区域透明度调整为 `rgba(229,57,53,0.25)` 到 `rgba(229,57,53,0.05)`
    - **视觉效果对比**：图表颜色从"淡雅柔和"改为"醒目清晰"，数据更易辨识

11. **隐藏创建者/参与者图表标题**：用户反馈"创建者×模块分布(堆叠)"和"参与者×模块分布(堆叠)"标题重复显示（红框标注处），需要隐藏：
    - **问题原因**：echarts图表内部 title 与外部标题区域重复显示，造成视觉冗余
    - **解决方案**：删除 `renderCreatorChart` 和 `renderParticipantChart` 两个方法中 chart.setOption 的 title 配置
    - **布局调整**：移除 title 后，将 legend 的 top 从 30 调整为 20，grid 的 top 从 80 调整为 60，避免留白
    - **效果**：图表顶部标题消失，图表整体更简洁紧凑

---

## 2026-06-25 AI风险分析结果改为纯中文描述

### 问题
AI风险分析结果显示为JSON格式，包含type、description等字段，用户反馈"看着不适应"，希望以纯中文段落形式呈现。

### 根因
原有实现要求AI输出JSON格式：
```json
{
  "P0": [{"type": "风险类型", "description": "风险说明"}],
  "P1": [...],
  "P2": [...]
}
```
前端解析JSON后分开显示type和description，导致视觉上过于结构化、不自然。

### 修改方案
1. **修改AI Prompt**（`bug_analysis_ai.py` `analyze_risks`）
   - 要求AI输出纯文本段落，按`【P0 高风险】`、`【P1 中风险】`、`【P2 低风险】`标记分隔
   - 每条风险用`- `开头的列表项形式，包含完整描述

2. **修改解析逻辑**
   - 使用正则表达式提取各段落内容
   - 按行拆分，每行作为一条风险项存入`risks['P0/P1/P2']`

3. **修改前端显示**（`BugAnalysis.vue`）
   - 移除el-tag标签，直接显示完整description文本
   - 调整卡片样式，增加padding和line-height，更适合阅读纯文本

### 涉及文件
- `apps/data_factory/bug_analysis_ai.py`
- `frontend/src/views/data-factory/BugAnalysis.vue`

### 效果对比
| | 修改前 | 修改后 |
|---|---|---|
| 输出格式 | JSON结构化数据 | 纯中文段落 |
| 显示方式 | type标签 + description分开 | 完整描述文本 |
| 阅读体验 | 像看数据表 | 像看分析报告 |

---

## 2026-06-25 云效同步保存记录失败（异步上下文 Django ORM 问题）

### 问题
云效同步完成后：
1. AI 分析内容丢失（再次进入详情页时 aiModuleFocus: None）
2. 历史记录中 `_raw_yunxiao` 和 `custom_fields` 数据为空

### 根因
**异步函数中直接调用 Django ORM**：`_run_enhanced_analysis` 是异步函数，内部直接调用了：
- `BugAnalysisRecord.objects.create()` - 保存分析记录
- `_get_bug_analyzer_config_id()` 中的 `AIModelConfig.objects.filter()`

Django ORM 操作必须在同步上下文中执行，在异步函数中直接调用会抛出 `SynchronousOnlyOperation` 异常。

日志显示：
```
ERROR [BugAnalysis] 获取 Bug 分析配置失败: You cannot call this from an async context - use a thread or sync_to_async.
ERROR [BugAnalysis] 保存分析记录失败: You cannot call this from an async context - use a thread or sync_to_async.
```

### 修改方案
1. **使用 sync_to_async 包装 Django ORM 调用**（`bug_analysis_view.py` `_run_enhanced_analysis`）
   - 将 `_get_bug_analyzer_config_id()` 调用改为 `await sync_to_async(_get_bug_analyzer_config_id)()`
   - 将 `BugAnalysisRecord.objects.create()` 调用改为 `await sync_to_async(_save_record_sync)()`

2. **增加超时配置**
   - 云效 API 请求超时从 30秒增加到 60秒
   - 前端同步请求超时从 2分钟增加到 5分钟

### 涉及文件
- `apps/data_factory/bug_analysis_view.py`
- `apps/data_factory/yunxiao_client.py`
- `frontend/src/api/data-factory.js`

### 踩坑点
1. **Django ORM 不能在异步上下文直接调用**：Django 的 ORM 操作是同步的，在 `async` 函数中直接调用会抛异常。必须使用 `sync_to_async` 包装，或者用 `async_to_sync` 将整个异步函数转为同步函数后调用。
2. **错误日志容易被忽略**：`[BugAnalysis] 保存分析记录失败` 错误被 `try-except` 捕获后只打印日志，前端仍然返回成功响应。用户看到同步成功，但数据实际未保存。关键错误需要更醒目的提示。

---

## 2026-06-25 云效同步字段名兼容性修复

### 问题
根据同步日志诊断，云效同步后：
1. 自定义字段数量为 0（custom_fields 未同步）
2. 创建时间、更新时间全部为 None
3. 同步日志中无法查看云效原始字段名，难以定位字段名不匹配问题

### 根因
**字段名不匹配**：云效 API 返回的字段名可能与代码中硬编码的字段名不一致。代码中只处理了有限的几种字段名变体：
- 时间字段：只处理了 `gmtCreate`/`createdAt`/`createTime`/`createdTime`，可能实际返回的是 `gmt_create`/`created_at`/`gmtCreateTime` 等
- 自定义字段：只处理了 `customFieldValues`/`customFields`/`fieldValueMap`/`extraFields`，可能实际返回的是 `customFieldValueList`/`custom_field_values` 等
- 日期解析：`_parse_date_value` 不支持 ISO 8601 格式（如 `2024-01-15T10:30:00+08:00`）和 Unix 时间戳（毫秒/秒）
- `_raw_yunxiao` 数据丢失：`from_yunxiao` 中错误地从 `_raw_yunxiao` 读取原始数据，而 `convert_yunxiao_bugs` 实际保存的是 `_raw`

### 修改方案
1. **增强时间字段提取兼容性**（`yunxiao_client.py` `convert_yunxiao_bugs`）
   - 增加 snake_case 变体：`gmt_create`/`created_at`/`create_time`/`created_time`/`gmt_create_time`
   - 增加修改时间变体：`gmt_modified`/`modified_at`/`update_time`/`updated_time`/`gmt_modify_time`/`gmt_modify_time`
   - 增加关闭时间变体：`gmt_closed`/`closed_at`/`close_time`/`close_time`

2. **增强自定义字段提取兼容性**（`yunxiao_client.py` `convert_yunxiao_bugs`）
   - 增加 `customFieldValueList` 字段名
   - 增加 `custom_field_values` 字段名
   - 支持 `custom_fields_legacy` 为列表类型时，调用 `_extract_custom_field_values` 解析

3. **增强日期解析**（`bug_source_adapter.py` `_parse_date_value`）
   - 支持 ISO 8601 格式（带 `T` 的字符串），如 `2024-01-15T10:30:00+08:00`
   - 支持 Unix 毫秒时间戳（> 1000000000000）
   - 支持 Unix 秒时间戳（> 1000000000）
   - Excel 序列号日期判断：值小于 50000 才当作 Excel 日期

4. **修复 _raw_yunxiao 数据丢失**（`bug_source_adapter.py` `from_yunxiao`）
   - 将 `d.get("_raw_yunxiao", {})` 改为 `d.get("_raw", {})`

5. **同步日志增加原始字段名展示**（`bug_analysis_view.py` `yunxiao_sync_log`）
   - 新增返回字段 `first_bug_raw_keys`：第一条Bug的原始数据中所有字段名列表
   - 截断 `_raw_yunxiao` 内容避免响应过大

6. **前端同步日志增加原始字段名卡片**（`BugAnalysis.vue`）
   - 在"所有自定义字段名"卡片之前新增"云效原始字段名"卡片
   - 展示 `_raw_yunxiao` 中的所有字段名，方便与代码中支持的字段名对比

7. **增强日志诊断**（`yunxiao_client.py`）
   - 打印第一条原始数据的所有字段名
   - 打印所有可能的时间相关字段及其值
   - 打印所有可能的自定义字段相关字段及其值（截断到200字符）

### 涉及文件
- `apps/data_factory/yunxiao_client.py`
- `apps/data_factory/bug_source_adapter.py`
- `apps/data_factory/bug_analysis_view.py`
- `frontend/src/views/data-factory/BugAnalysis.vue`

### 踩坑点
1. **云效 API 字段名不固定**：不同版本、不同部署方式的云效 API，返回的字段名可能有 camelCase、snake_case 甚至完全不同名称。不能依赖固定字段名，必须尝试多种变体。
2. **时间格式多样**：云效可能返回 ISO 8601 字符串、Unix 时间戳（毫秒/秒）、Excel 序列号等。日期解析函数需要支持所有这些格式。
3. **原始数据透传易出错**：`_raw` 和 `_raw_yunxiao` 命名不一致会导致原始数据丢失。原始数据透传时字段名要保持一致。

---

## 2026-06-25 AI 分析异步化改造

### 问题
1. 云效同步时间太长（1-2分钟），用户等待体验差
2. 同步过程中如果 AI 分析失败，整个同步结果中 AI 相关数据为空
3. 用户无法先看到基础分析结果，必须等 AI 分析全部完成

### 根因
**同步和 AI 分析串行执行**：`sync_from_yunxiao` 中调用 `_run_enhanced_analysis`，该函数依次执行：
1. 规则引擎分析（1-3秒）
2. AI 增强分析（30-90秒，5-6次 AI API 调用）
3. 保存记录

整个流程在同一个 HTTP 请求中完成，用户必须等待全部完成才能看到结果。

### 修改方案

#### 后端改造

1. **模型增加 AI 状态字段**（`models.py` `BugAnalysisRecord`）
   - 新增 `ai_status` 字段：枚举值 `none`/`pending`/`running`/`completed`/`failed`
   - 新增 `ai_progress` 字段：0-100 的整数进度

2. **拆分基础分析和 AI 分析**（`bug_analysis_view.py` `_run_enhanced_analysis`）
   - 新增 `skip_ai` 参数：为 `True` 时只执行规则引擎分析，跳过所有 AI 调用
   - 新增 `ai_status` 参数：保存记录时写入的初始 AI 状态
   - 当 `skip_ai=False` 且 AI 分析成功完成后，自动将 `ai_status` 设为 `completed`

3. **新增后台 AI 分析函数**（`bug_analysis_view.py` `_run_ai_analysis_background`）
   - 在线程中运行，使用 `asyncio.run()` 在新事件循环中执行异步 AI 方法
   - 实时更新 `ai_progress`（10%→35%→55%→70%→85%→95%→100%）
   - 完成后更新 `analysis_result` 和 `ai_status='completed'`
   - 异常时更新 `ai_status='failed'`

4. **修改同步接口为异步触发**（`bug_analysis_view.py` `sync_from_yunxiao`）
   - Step 1：调用 `_run_enhanced_analysis(skip_ai=True, ai_status='pending')`，快速完成基础分析并保存记录
   - Step 2：启动 `threading.Thread` 后台执行 AI 分析
   - Step 3：立即返回响应，包含 `record_id` 和 `ai_status='pending'`

5. **新增 AI 状态查询 API**（`bug_analysis_view.py` `bug_analysis_ai_status`）
   - `GET /data-factory/bug-analysis/records/<id>/ai-status/`
   - 返回 `ai_status`、`ai_progress`、`has_ai_data`

6. **记录详情/列表 API 返回 AI 状态**
   - `bug_analysis_record_detail` 返回 `ai_status` 和 `ai_progress`
   - `bug_analysis_records` 返回 `ai_status` 和 `ai_progress`

#### 前端改造

1. **新增 API 函数**（`data-factory.js`）
   - `getBugAnalysisAiStatus(recordId)`：查询 AI 分析状态
   - 缩短 `syncFromYunxiao` 超时：300秒→120秒（只等待基础分析）

2. **同步成功后启动轮询**（`BugAnalysis.vue` `handleYunxiaoSync`）
   - 同步成功后读取 `res.data.ai_status`
   - 如果为 `pending`/`running`，调用 `startAiPolling(recordId)`

3. **实现轮询逻辑**（`BugAnalysis.vue`）
   - `startAiPolling(recordId)`：每 3 秒查询一次 AI 状态
   - `stopAiPolling()`：清除定时器
   - 状态变为 `completed` 时：重新加载记录详情，更新分析结果，渲染图表
   - 状态变为 `failed` 时：显示错误提示
   - `onUnmounted` 时停止轮询，避免内存泄漏

4. **AI 分析 Loading UI**（`BugAnalysis.vue` 模板）
   - 详情页顶部显示 `el-alert`："AI 智能分析进行中 (xx%)"
   - 带 `is-loading` 动画的 `Loading` 图标
   - AI 失败时显示红色错误提示

5. **历史记录加载也支持轮询**（`BugAnalysis.vue` `loadHistoryRecord`）
   - 加载历史记录时读取 `result.ai_status`
   - 如果为 `pending`/`running`，同样启动轮询

### 涉及文件
- `apps/data_factory/models.py`
- `apps/data_factory/bug_analysis_view.py`
- `apps/data_factory/urls.py`
- `frontend/src/api/data-factory.js`
- `frontend/src/views/data-factory/BugAnalysis.vue`

### 踩坑点
1. **Django ORM 在线程中的使用**：后台线程中可以直接使用 Django ORM（同步环境），但 AI 方法是 async 的，需要 `asyncio.run()` 在新事件循环中执行。注意线程中不能使用已有的事件循环。
2. **数据库迁移字段已存在**：如果之前迁移失败但部分执行了，字段可能已经存在。此时需要 `--fake` 参数伪造迁移。
3. **前端响应式更新**：`_rawAnalysisResult` 是全局变量，`applyAnalysisResult` 需要正确触发 Vue 响应式更新。轮询完成后重新调用 `applyAnalysisResult` 和 `renderCharts`。
4. **超时设置**：异步模式下，同步请求只需要等待基础分析（10-30秒），AI 分析在后台进行。前端超时可以从 5 分钟缩短到 2 分钟。
5. **线程命名和 daemon**：后台线程必须设为 `daemon=True`，否则主进程退出时线程可能阻塞。给线程命名方便调试。