# 踩坑优化记录

## 2026-08-11 回归风险区域内容样式结构化优化

### 背景
BugAnalysis 详情页「回归风险」Tab 中，AI 生成的风险描述是一整段超长纯文本（含风险场景、预估数量、回归建议三部分），原先通过 `getRiskType` 取冒号前部分作为类型、`getRiskDesc` 取冒号后部分作为一段 `<span>` 文本展示，导致内容挤成一团、可读性差，用户难以快速定位预估数量和回归建议。

### 原因
- 模板对 `risk.description` 只做了一次冒号拆分，后续内容（预估数量、回归建议）未做结构化解析。
- `risk-desc` 为 inline span，长文本无分段，缺少视觉层次。

### 修改方案
文件：`frontend/src/views/data-factory/BugAnalysis.vue`

1. 新增 `parseRiskDesc(desc)` 解析函数：从冒号后的主体中按关键词正则提取「回归建议」「预估数量」两段，剩余主体按「第一，第二，第三，」分点切分为多段，返回 `{ segments, estimated, suggestion }`。
2. 新增 `parsedRisks` computed：对 P0/P1/P2 三档风险预解析（避免模板内重复调用），每项挂载 `type` 与 `parsed`。
3. 模板 P0/P1/P2 三处 `v-for` 数据源由 `aiRisks.Px` 改为 `parsedRisks.Px`，将原来的单行 `risk-desc` span 替换为结构化块：
   - 风险类型（`risk.type`）改为块级标题，带虚线下边框；
   - 主体分段用 `<p class="risk-desc-segment">` 逐段展示；
   - 预估数量用带蓝色标签的 `risk-meta.estimated` 卡片；
   - 回归建议用带紫色标签的 `risk-meta.suggestion` 卡片，并针对 P0/P1/P2 分别配色（红/橙/绿）。
4. 删除已无引用的 `getRiskDesc` 函数。
5. 新增配套 CSS：`.risk-desc-rich`、`.risk-desc-segment`、`.risk-meta`、`.risk-meta-label`、`.risk-meta-value` 及各档配色样式。

### 效果
风险卡片由一整段文字变为「标题 + 分段描述 + 预估数量标签 + 回归建议标签」的结构化布局，视觉层次清晰，关键信息一目了然。

### 健壮性保障（针对 AI 数据源不确定性）
担心 AI 下次生成的文本不符合解析规则会导致区域空白，已做三层兜底：
1. `parseRiskDesc` 对「预估数量」「回归建议」的提取均为独立 `if`，匹配不到则留空，主体不受影响；主体无「第X」分点时整体作为单段返回，最坏情况退化为原始一整段文本展示。
2. `parseRiskDesc` 入口增加 `typeof desc !== 'string'` 类型保护，非字符串直接返回空结构，避免 `indexOf` 抛异常。
3. `parsedRisks` computed 对每个 item 包裹 `try-catch`，单条解析异常时降级为「原始描述单段」展示，保证区域内容始终可见、不影响其他条目。

### UI 扁平化（消除卡片套卡片）
结构化后视觉上出现四层嵌套卡片（外层渐变大卡 → 头部渐变卡 → 单条风险卡 → 预估/建议小卡），层级太重。仅改 CSS、不动模板与解析：
1. 外层 `.ai-risk-level-block`：白底 + 细色边框，去浓渐变、去阴影。
2. 头部 `.risk-level-header`：透明背景，仅底部细线分隔，文字改深色。
3. 单条 `.risk-item-card`：去背景/边框/圆角，改列表项风格，底部细线分隔，左侧保留 2px 色条作为等级标识，hover 仅加淡背景。
4. 风险类型 `.risk-type`：从块级带虚线下边框的标题改为内联小 chip 标签（圆角淡色背景）。
5. 预估数量/回归建议 `.risk-meta`：去背景框与左边框，改内联轻量行——`::before` 小色点 + 彩色标签 + 值，各档（P0红/P1橙/P2绿）配色保留。

## 2026-08-11 迭代报告列表增加「迭代评估」字段

### 背景
BugAnalysis 列表页历史记录表格只展示 序号/文件名/Bug数量/同步人/同步时间/操作，看不到每条记录的迭代评估结论（能否上线），需点进详情才能看到，列表缺少结论概览。

### 原因
- 迭代评估结论 `overallConclusion` 存储在 `BugAnalysisRecord.analysis_result["personnel_assessment"]["overallConclusion"]`（JSON 字段），由 `personnel_assessment` 接口读写。
- 列表接口 `bug_analysis_records` 构建 items dict 时未从 analysis_result 中提取该字段，前端列表自然拿不到。

### 修改方案
1. 后端 `apps/data_factory/bug_analysis_view.py` 列表接口 items.append 中增加 `overall_conclusion`，链式 `.get()` 从 `analysis_result.personnel_assessment.overallConclusion` 提取，空值兜底为 `''`，无需迁移。
2. 前端 `BugAnalysis.vue` 表格在「Bug 数量」列后新增「迭代评估」列，用 `el-tag` 着色：通过=success / 有条件通过=warning / 不通过=danger，无结论显示「未评估」淡灰文字。
3. 新增 `.unevaluated-text` 样式。

### 效果
列表页直接展示每条记录的迭代评估结论，无需逐条点进详情即可快速判断哪些迭代已评估、结论如何。

### 样式调整
迭代评估列内容由 `el-tag` 改为参考 `.count-badge` 的徽章样式（24px 高 / 4px 圆角 / 13px 字号 / 500 字重），用 padding 自适应文字宽度，按结论着色：通过=浅绿、有条件通过=浅橙、不通过=浅红，与 Bug 数量徽章视觉统一。

## 2026-08-11 详情页迭代评估 Tab 状态不显示

### 现象
已保存过评估结论的记录，进入详情页后「迭代评估」Tab 上看不到结论 badge（状态没显示），必须手动点击切到该 Tab 才会出现。

### 原因
`loadPersonnelAssessment` 仅在 `watch(activeTab)` 中 `newTab === 'personnel'` 时调用，进入详情页默认 Tab 为概览，此时 `overallConclusion` 仍为空，Tab badge 的 `v-if="overallConclusion"` 不渲染。

### 修改方案
在 `applyAnalysisResult`（所有详情加载路径都会调用）末尾，从 `analysis_result.personnel_assessment` 预填 `overallConclusion` / `overallSummary`，使 Tab badge 进入详情页即显示；切到 personnel Tab 时 `loadPersonnelAssessment` 再用接口数据覆盖（值一致，无副作用）。无评估数据时置空为 `''`，避免跨记录残留。

## 2026-08-11 迭代评估能否上线区域容器样式与卡片区域不一致

### 现象
迭代报告详情页「迭代评估」Tab 中，顶部「能否上线评估」区域（`.assessment-conclusion`）与下方卡片区域（`.section-card`）的容器样式不统一，视觉上割裂。

### 原因
两处容器样式参数不一致：
- `border-radius`：能否上线区域为 `8px`，卡片为 `12px`；
- `border` 颜色：能否上线区域为 `rgba(147, 112, 219, 0.15)`，卡片为 `rgba(147, 112, 219, 0.12)`；
- `background`：能否上线区域为渐变 `linear-gradient(135deg, #f8f7ff 0%, #f5f3ff 100%)`，卡片为纯白 `#fff`；
- `box-shadow`：能否上线区域无阴影，卡片有 `0 4px 16px rgba(147, 112, 219, 0.08)`。

### 修改方案
文件：`frontend/src/views/data-factory/BugAnalysis.vue`

将 `.assessment-conclusion` 的 `background`、`border-radius`、`border`、`box-shadow` 四个属性对齐 `.section-card`，统一为白底、`12px` 圆角、`0.12` 透明度紫色边框、`0.08` 透明度紫色阴影，保持同一页面内卡片风格一致。
