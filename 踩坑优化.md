---

## 2026-07-27 调整提示词类型列宽度

### 问题
提示词配置列表中"提示词类型"列宽度不够（140px），导致Bug相关类型名称（如"Bug模块深度分析提示词"）显示被截断。

### 原因分析
新增的Bug分析提示词类型名称较长（如"Bug模块深度分析提示词"），而原有宽度140px是针对较短的4种类型设计的。

### 修改方案
将表格中"提示词类型"列宽度从 `140px` 调整为 `200px`。

**修改文件**: `frontend/src/views/requirement-analysis/PromptConfig.vue`

### 经验教训
1. 字段命名要考虑扩展：新增类型名称较长时，需同步调整列表列宽
2. 预留空间设计：表格列宽应预留足够空间，避免内容截断

---

## 2026-07-27 优化默认提示词弹窗多Tab显示问题

### 问题
"加载默认提示词"弹窗中有13个Tab选项，但弹窗宽度只有700px，导致Tab显示不全，需要优化显示和交互体验。

### 原因分析
1. 弹窗宽度设置为700px，无法容纳13个Tab标签
2. Tab导航栏没有横向滚动功能，超出部分被隐藏
3. Tab标签样式偏大，占用过多空间

### 修改方案

**1. 增加弹窗宽度** (`frontend/src/views/requirement-analysis/PromptConfig.vue`)

将弹窗宽度从 `700px` 调整为 `900px`。

**2. 添加Tab横向滚动样式**

```css
.defaults-content :deep(.el-tabs__nav-wrap) {
  overflow-x: auto;
  overflow-y: hidden;
  -webkit-overflow-scrolling: touch;
  scrollbar-width: thin;
  scrollbar-color: #c4b5e0 transparent;
}

.defaults-content :deep(.el-tabs__nav-scroll) {
  display: flex;
  min-width: min-content;
}
```

**3. 优化Tab标签样式**

减小标签内边距和字体大小，使每个Tab占用更少空间：
```css
.defaults-content :deep(.el-tabs__item) {
  padding: 0 12px;
  height: 36px;
  line-height: 36px;
  font-size: 13px;
  white-space: nowrap;
}
```

**4. 添加美化滚动条**

为横向滚动条添加自定义样式，提升视觉体验。

### 影响范围
- `frontend/src/views/requirement-analysis/PromptConfig.vue`: 弹窗宽度和Tab样式优化

### 经验教训
1. **多Tab场景要考虑滚动**：当Tab数量较多时，应提供横向滚动功能
2. **自适应宽度设计**：弹窗宽度应根据内容动态调整
3. **细节样式优化**：通过调整padding、font-size等让元素更紧凑

---

## 2026-07-27 修复"加载默认提示词"功能缺失 Bug 分析相关选项

### 问题
点击"加载默认提示词"按钮后弹出的预览窗口中，只有4个 Tab（测试用例编写、测试用例评审、知识库问答、断言生成），缺失了新增的9个 Bug 分析相关选项。

### 原因分析
1. 后端 `load_defaults` 接口只返回了4种默认提示词，没有包含新增的 Bug 分析类型
2. 前端弹窗中的 Tab 是硬编码的，没有与后端的 `promptTypeChoices` 保持同步
3. `defaultPrompts` 初始值和 `closeDefaultsModal` 方法中的重置逻辑都是固定的4个字段

### 修改方案

**1. 后端添加默认提示词** (`apps/requirement_analysis/views.py`)

在 `load_defaults` 接口中添加9个 Bug 分析类型的默认提示词：
- `bug_classify`: 缺陷分类提示词
- `bug_severity`: 严重度推断提示词
- `bug_module_fallback`: 模块归类提示词
- `bug_root_cause`: 根因分析提示词
- `bug_test_focus`: 测试建议提示词
- `bug_summary`: 智能摘要提示词
- `bug_risks`: 风险分析提示词
- `bug_keywords`: 关键词提取提示词
- `bug_module_deep`: 模块深度分析提示词

每个提示词都由 `System` 和 `User` 两部分组成，并支持 `{variable}` 模板变量替换。

**2. 前端弹窗动态化** (`frontend/src/views/requirement-analysis/PromptConfig.vue`)

- 将弹窗中的硬编码 Tab 改为使用 `promptTypeChoices` 动态渲染：
```vue
<el-tab-pane
  v-for="choice in promptTypeChoices"
  :key="choice.value"
  :label="choice.label"
  :name="choice.value"
>
  <div class="content-display">
    <div class="content-text">{{ defaultPrompts[choice.value] || $t('promptConfig.noContent') }}</div>
  </div>
</el-tab-pane>
```

- 修改 `defaultPrompts` 初始值为空对象 `{}`
- 修改 `activeTab` 初始值为空字符串 `''`
- 修改 `loadDefaultPrompts` 方法，加载后自动设置默认 Tab 为第一个选项
- 修改 `confirmLoadDefaults` 方法，使用 `getPromptTypeLabel()` 动态获取标签名
- 修改 `closeDefaultsModal` 方法，动态重置 `activeTab`

### 影响范围
- `apps/requirement_analysis/views.py`: `load_defaults` 接口添加9种默认提示词
- `frontend/src/views/requirement-analysis/PromptConfig.vue`: 弹窗 Tab 动态化

### 经验教训
1. **功能扩展要完整**：新增提示词类型时，不仅要修改"创建配置"的下拉框，还要同步更新"加载默认提示词"功能
2. **动态渲染优于硬编码**：Tab 选项应使用动态数据渲染，便于后续扩展
3. **保持数据一致性**：`defaultPrompts`、`activeTab` 等状态的初始化和重置逻辑要与动态选项保持一致

---

## 2026-07-27 修复提示词类型前端下拉选项缺失问题

### 问题
在提示词配置页面的"创建/编辑配置"弹窗中，"提示词类型"下拉框只显示4个选项（用例编写、用例评审、知识库问答、断言生成），缺失了新增的9个 Bug 分析相关选项。

### 原因分析
前端 `PromptConfig.vue` 中的提示词类型选项是**硬编码**在模板中的，没有与后端的 `PROMPT_CHOICES` 保持同步：
1. 后端在 `PromptConfig` 模型的 `PROMPT_CHOICES` 中扩展了9个 Bug 分析类型
2. 但前端 `el-select` 的 `el-option` 是写死的，没有动态加载机制
3. 后端也没有提供获取选项列表的 API 接口

### 修改方案

**1. 后端添加 API 接口** (`apps/requirement_analysis/views.py`)

在 `PromptConfigViewSet` 中新增 `prompt_types` action：
```python
@action(detail=False, methods=['get'])
def prompt_types(self, request):
    """获取所有可用的提示词类型选项"""
    try:
        choices = [{'value': value, 'label': label} for value, label in PromptConfig.PROMPT_CHOICES]
        return Response({
            'message': '获取提示词类型成功',
            'choices': choices
        }, status=status.HTTP_200_OK)
    except Exception as e:
        logger.error(f"获取提示词类型失败: {e}")
        return Response(
            {'error': f'获取失败: {str(e)}'},
            status=status.HTTP_500_INTERNAL_SERVER_ERROR
        )
```

**2. 前端动态加载选项** (`frontend/src/views/requirement-analysis/PromptConfig.vue`)

- 在 `data()` 中添加 `promptTypeChoices: []` 数组存储选项
- 在 `mounted()` 中调用 `loadPromptTypes()` 方法加载选项
- 新增 `loadPromptTypes()` 方法从后端 API 获取选项
- 修改 `el-select` 使用 `v-for` 动态渲染选项：
```vue
<el-select v-model="configForm.prompt_type">
  <el-option
    v-for="choice in promptTypeChoices"
    :key="choice.value"
    :value="choice.value"
    :label="choice.label"
  />
</el-select>
```
- 修改 `getPromptTypeLabel()` 方法使用动态数据：
```javascript
getPromptTypeLabel(type) {
  const choice = this.promptTypeChoices.find(c => c.value === type)
  return choice ? choice.label : type
}
```

**3. 为新类型添加样式**

为9个 Bug 分析类型添加不同颜色的 `type-badge` 样式：
- `.type-badge.bug_classify` - 红色系
- `.type-badge.bug_severity` - 橙色系
- `.type-badge.bug_module_fallback` - 蓝色系
- `.type-badge.bug_root_cause` - 紫色系
- `.type-badge.bug_test_focus` - 青色系
- `.type-badge.bug_summary` - 黄色系
- `.type-badge.bug_risks` - 深红色系
- `.type-badge.bug_keywords` - 天蓝色系
- `.type-badge.bug_module_deep` - 深紫色系

### 影响范围
- `apps/requirement_analysis/views.py`: 新增 `prompt_types` API 接口
- `frontend/src/views/requirement-analysis/PromptConfig.vue`: 动态加载选项 + 新样式

### 经验教训
1. **前后端选项应保持同步**：当后端扩展了 choices 选项时，前端需要同步更新
2. **动态加载优于硬编码**：通过 API 获取选项列表，避免后端每次修改都需要改前端
3. **单一数据源原则**：选项的唯一数据源应该是后端模型，前端通过 API 获取
4. **预留扩展空间**：在设计下拉选项时，应考虑未来可能的扩展需求

---

## 2026-07-25 修复 AI 分析失败问题 - AI 配置不可用时自动回退到 Mock 实现

### 问题
TestHub 应用的 Bug 分析功能中，当用户使用 AI 增强分析时，前端显示"AI 分析失败"错误信息。

### 原因分析
经过代码分析，发现以下几个问题导致 AI 分析失败：

1. **`get_ai_provider()` 强制覆盖 provider**
   - 原代码强制将 `mock` provider 转换为 `qwen`，即使用户请求使用 Mock 实现
   - 当数据库中没有有效的 AI 配置时，qwen 实现会失败，没有 fallback 机制

2. **`_get_config()` 在找不到配置时直接抛出异常**
   - 原代码当 `AIModelConfig` 表中没有匹配的配置时，直接抛出 `ValueError`
   - 没有优雅降级到 Mock 实现的逻辑

3. **`_run_enhanced_analysis()` 和 `enhance_with_ai()` 中的 AI 调用没有统一的 fallback 机制**
   - 当 AI API 调用失败时，只记录错误日志，整个分析流程就终止了
   - 前端收到错误响应，显示"AI 分析失败"

### 修改方案

**1. `apps/data_factory/bug_analysis_ai.py`**

- 新增 `MockFallbackException` 异常类，用于在 AI 配置不可用时触发 Mock 回退
- 恢复 `get_ai_provider()` 支持 `mock` provider，不再强制转换为 `qwen`
- 修改 `_get_config()` 在找不到配置时返回 `None` 而不是抛出异常
- 修改 `_call_llm()` 在 `config` 为 `None` 时抛出 `MockFallbackException`
- 新增 `_run_with_fallback()` 辅助方法，封装 LLM 调用 + Mock 回退逻辑

**2. `apps/data_factory/bug_analysis_view.py`**

- 修改 `_run_enhanced_analysis()` 和 `enhance_with_ai()` 函数
- 在 AI 分析流程中添加 `MockFallbackException` 捕获逻辑
- 检测到 Mock 回退信号时，自动切换到 `MockBugAnalysisAI` 完成分析
- 保证即使没有 AI 配置，分析功能也能正常使用（基于规则引擎）

### 影响范围
- `apps/data_factory/bug_analysis_ai.py`: AI 提供者核心逻辑
- `apps/data_factory/bug_analysis_view.py`: AI 增强分析 API 端点

### 经验教训
1. **配置缺失时应有降级方案**：AI 功能依赖外部配置时，必须设计 fallback 机制
2. **异常处理粒度要合适**：区分配置缺失（可恢复）和其他错误（不可恢复）
3. **Provider 工厂模式要灵活**：`get_ai_provider()` 应该尊重用户的 provider 选择
4. **异步编程中的异常传播**：使用 `asyncio.gather()` 时要注意异常的收集和处理

---

## 2026-07-23 优化 ExecutionList 页面列表字段样式与 SuiteList 保持一致

### 问题
ExecutionList 页面的列表字段样式与同平台的 SuiteList 页面风格不一致：
- 测试用例名称是普通文本，没有链接样式
- 状态标签使用 `el-tag`，而不是自定义的 `status-badge` 样式
- 操作按钮使用 `text` 类型的按钮，没有渐变背景和图标
- 表格容器使用 `el-card`，而不是统一的 `div.card-container`

### 原因分析
不同页面开发时间不同，样式规范没有及时同步：
1. ExecutionList 页面早期开发时使用了 Element Plus 默认组件样式
2. SuiteList 后续统一了更现代的渐变背景按钮和自定义徽章样式
3. helpers 函数只返回 `type` 字段，缺少 `class` 字段支持自定义样式类

### 修改方案

**1. 测试用例列优化**
```vue
<el-table-column label="测试用例" min-width="180">
  <template #default="{ row }">
    <el-link v-if="row.report_path" type="primary" @click="viewReport(row)">
      {{ row.case_name }}
    </el-link>
    <span v-else>{{ row.case_name }}</span>
  </template>
</el-table-column>
```
- 只有在生成报告后才显示为可点击的链接
- 点击链接在新标签页打开报告

**2. 状态标签优化**
```vue
<el-table-column label="状态" width="100" align="center">
  <template #default="{ row }">
    <span class="status-badge" :class="getDisplayStatus(row.status, row.result).class">
      {{ getDisplayStatus(row.status, row.result).text }}
    </span>
  </template>
</el-table-column>
```
- 使用自定义的 `status-badge` 样式类
- 添加 `align="center"` 居中显示

**3. 操作按钮优化**
```vue
<div class="action-buttons">
  <el-button size="small" class="action-btn stop-btn" @click="stopExecution(row)">
    <el-icon><VideoPause /></el-icon>
    <span>停止</span>
  </el-button>
  <el-button size="small" class="action-btn report-btn" @click="viewReport(row)">
    <el-icon><Document /></el-icon>
    <span>报告</span>
  </el-button>
  <el-button size="small" class="action-btn error-btn" @click="viewError(row)">
    <el-icon><Warning /></el-icon>
    <span>错误</span>
  </el-button>
</div>
```
- 使用渐变背景按钮（橙色停止、蓝色报告、红色错误）
- 添加对应的图标（VideoPause、Document、Warning）
- 悬停时添加阴影和上移动效

**4. helpers 函数增强**
```javascript
export function getDisplayStatus(status, result) {
  if (status === 'pending') {
    return { type: 'info', text: '等待中', class: 'pending' }
  }
  if (status === 'running') {
    return { type: 'warning', text: '执行中', class: 'processing' }
  }
  if (status === 'error') {
    return { type: 'danger', text: '执行异常', class: 'failed' }
  }
  if (status === 'stopped') {
    return { type: 'info', text: '已停止', class: 'pending' }
  }
  if (result === 'passed') {
    return { type: 'success', text: '通过', class: 'success' }
  }
  if (result === 'failed') {
    return { type: 'danger', text: '失败', class: 'failed' }
  }
  if (result === 'skipped') {
    return { type: 'warning', text: '跳过', class: 'processing' }
  }
  return { type: 'info', text: status || '-', class: 'pending' }
}
```
- 新增 `class` 字段，返回对应的样式类名
- 支持的状态：success（通过）、failed（失败）、processing（执行中）、pending（等待/已停止）

**5. 表格容器统一**
```vue
<!-- 原：<el-card class="table-card" shadow="never"> -->
<div class="card-container">
  <el-table>...</el-table>
</div>
```

---

## 2026-07-23 优化 APP 自动化「测试报告」页面 UI

### 问题
`/app-automation/reports` 页面（`frontend/src/views/app-automation/reports/ReportList.vue`）顶部统计卡片和列表样式陈旧：
- 顶部 4 个统计卡是简单 `el-card`，只有数字和文字，居中显示，没有图标、渐变、悬停反馈，看上去单调。
- Tab 切换只有「套件报告 / 用例报告」文字，无图标。
- 列表中"套件名称/测试用例"列是普通文本，无视觉重点。
- "设备"、"耗时"、"执行人"列直接铺文字，缺乏图标和视觉层次。
- "通过数/失败数/总计"是 3 个独立的纯色块，缺少 icon 和明显分隔。
- 筛选栏只有下拉和输入框，缺少"刷新"等快捷操作按钮。
- 状态徽章是直角方块，纯色背景偏生硬。

### 原因分析
- 早期开发仅满足基本功能，没及时跟进项目后续的现代化设计规范。
- 同一平台 `SuiteList`、`Dashboard.vue` 已经采用「渐变图标 + 圆角卡片 + 悬停上移」的现代风格，但该页面没有同步。
- 表格列大量使用 `align="center"`，没有按场景区分（如用例名称应左对齐 + 图标 + 链接）。
- 没复用 `el-avatar` 等组件展示执行人。

### 修改方案

**1. 现代化统计卡片（同时用于套件报告、用例报告）**
```vue
<div v-for="stat in suiteStatsCards" :key="stat.label" class="stat-card" :class="stat.theme">
  <div class="stat-icon"><el-icon><component :is="stat.icon" /></el-icon></div>
  <div class="stat-info">
    <div class="stat-number">{{ stat.value }}</div>
    <div class="stat-label">{{ stat.label }}</div>
    <div class="stat-sub" v-if="stat.sub">{{ stat.sub }}</div>
  </div>
</div>
```
- 用 `div` 替代 `el-card`，更易控制阴影/渐变/装饰光晕。
- 左侧 60×60 圆角图标，主题色渐变背景（蓝/绿/红/橙/紫）。
- 右侧 28px 加粗数字、13px 主标签、12px 副说明（如「成功 1 / 占比 100% / 状态良好」）。
- 卡片右上角加 `::before` 装饰光晕圆；hover 上移 4px + 加深阴影。
- 通过 `theme` 字段切换颜色，5 套配色统一管理。

**2. Tab 加图标 + 自定义指示器**
```vue
<el-tab-pane name="suite">
  <template #label>
    <div class="tab-label">
      <el-icon><FolderOpened /></el-icon>
      <span>套件报告</span>
    </div>
  </template>
</el-tab-pane>
```
- 自定义 `tab-label` 在文字前加 icon。
- 通过 `report-tabs` 父类覆盖 Element Plus 默认指示器为 3px 渐变 bar。
- 选中态颜色 `#7b42f6`、字号 15px 加粗、悬停变色。

**3. 用例名称列升级为「图标 + 链接」**
```vue
<el-table-column label="测试用例" min-width="180" align="left" show-overflow-tooltip>
  <template #default="{ row }">
    <div class="case-name-cell">
      <el-icon class="case-icon"><Tickets /></el-icon>
      <el-link v-if="row.report_path" type="primary" :underline="false" class="case-link" @click="viewAllureReport(row)">
        {{ row.case_name || '-' }}
      </el-link>
      <span v-else class="case-text">{{ row.case_name || '-' }}</span>
    </div>
  </template>
</el-table-column>
```
- 左对齐 + 紫色 Tickets 图标作为视觉锚点。
- 仅有 Allure 报告的记录显示为可点击的 `el-link`，点击直接打开报告。
- 套件报告中的「套件名称」采用同样的模式（FolderOpened 图标 + 链接，点击查看详情）。

**4. 设备/耗时/执行人列加入图标 + 头像**
- `device-cell`：Cellphone 图标 + 设备名。
- `duration-cell`：Timer 图标 + 耗时。
- `user-cell`：`el-avatar` 22px 渐变圆形头像（取用户名首字符） + 姓名。

**5. 步骤统计徽章重构**
```vue
<div class="count-cell">
  <span class="count-badge success"><el-icon><CircleCheck /></el-icon>{{ row.passed_steps || 0 }}</span>
  <span class="count-badge failed"><el-icon><CircleClose /></el-icon>{{ row.failed_steps || 0 }}</span>
  <span class="count-badge total"><el-icon><DataLine /></el-icon>总计 {{ row.total_steps || 0 }}</span>
</div>
```
- 用 `count-cell` 容器统一 gap 6px，取代原来的 `margin-right` 写法。
- 每个徽章 4×10 padding，渐变背景 + 圆角 6px + 加粗字号。
- 配套 icon：✓（成功绿）、✗（失败红）、📊（总计紫）。

**6. 状态徽章升级为「圆角胶囊 + 圆点指示」**
- 从直角方块升级为 `border-radius: 20px` 胶囊。
- 左侧加 `::before` 6px 圆点指示器。
- 使用浅色渐变背景（`linear-gradient(135deg, ...)`）+ 阴影。
- `processing` 状态增加 `pulse-processing` 1.6s 呼吸动画。
- 4 套配色：success（绿）、failed（红）、processing（橙）、pending（灰）。

**7. 筛选栏增加图标前缀 + 刷新按钮**
- 项目选择：FolderOpened 前缀图标。
- 状态选择：DataLine 前缀图标。
- 搜索：保留 Search 图标。
- 新增「刷新」按钮：渐变紫底 + Refresh 图标，hover 上移。

**8. 表格左对齐列处理**
- 用例名称列设为 `align="left"`，但全局 `.cell` 强制 `justify-content: center`。
- 增加特例 CSS：
  ```scss
  :deep(td[style*="text-align: left"] .cell),
  :deep(td[style*="text-align:left"] .cell) {
    justify-content: flex-start !important;
  }
  ```
  保证「图标 + 链接」靠左显示，符合主流阅读习惯。

**9. 其他细节**
- 通过率进度条加入 `rate-progress` 类，限制最大宽度 140px、文字加粗。
- 新增图标引入：`FolderOpened`、`Tickets`、`DataLine`、`Cellphone`、`Timer`、`User`、`Refresh`、`TrendCharts`、`CircleCheck`、`CircleClose`、`WarningFilled`。
- statsCards 数据结构扩展为 `{ label, value, sub, icon, theme }`，sub 字段提供「成功数 / 占比 / 状态评估」等辅助信息。

### 影响
- 顶部统计卡视觉层次提升 1 个档次（图标+渐变+悬停+光晕）。
- 列表可读性提升：用例名称、设备、耗时、统计均带图标。
- Tab 切换更直观，操作按钮更醒目。
- 状态徽章更现代，呼吸动画增强「执行中」可感知。
- 所有原有功能、API、字段、交互均保持不变。


**6. 新增样式定义**
```css
/* 停止按钮 */
.action-btn.stop-btn {
  background: linear-gradient(135deg, #fa8c16 0%, #d46b08 100%);
}
.action-btn.stop-btn:hover {
  background: linear-gradient(135deg, #ffa940 0%, #fa8c16 100%);
  transform: translateY(-1px);
  box-shadow: 0 4px 12px rgba(250, 140, 22, 0.4);
}

/* 报告按钮 */
.action-btn.report-btn {
  background: linear-gradient(135deg, #1890ff 0%, #096dd9 100%);
}
.action-btn.report-btn:hover {
  background: linear-gradient(135deg, #40a9ff 0%, #1890ff 100%);
  transform: translateY(-1px);
  box-shadow: 0 4px 12px rgba(24, 144, 255, 0.4);
}

/* 错误按钮 */
.action-btn.error-btn {
  background: linear-gradient(135deg, #ff4d4f 0%, #f5222d 100%);
}
.action-btn.error-btn:hover {
  background: linear-gradient(135deg, #ff7875 0%, #ff4d4f 100%);
  transform: translateY(-1px);
  box-shadow: 0 4px 12px rgba(245, 34, 45, 0.4);
}
```

### 涉及文件
- `frontend/src/views/app-automation/executions/ExecutionList.vue`
- `frontend/src/utils/app-automation-helpers.js`

### 经验教训
1. **平台样式一致性优先**：同一模块内的列表页面应使用相同的样式规范（徽章、按钮、容器）
2. **helpers 函数设计考虑扩展性**：返回对象应包含多个字段（type、text、class），支持不同的使用场景
3. **操作按钮视觉分层**：不同功能的按钮使用不同颜色（橙色停止、蓝色查看、红色错误），提升辨识度
4. **条件渲染提升体验**：测试用例名称只有生成报告后才可点击，避免无效交互
5. **图标增强语义**：按钮添加图标（VideoPause、Document、Warning）提升可理解性

---

## 2026-07-24 优化 ExecutionList 页面"步骤统计"字段为徽章样式

### 问题
ExecutionList 页面"步骤统计"列只使用纯文本配色的方式展示（`通过: 0 失败: 0 总数: 0`），视觉层次不够明显，无法在大量数据中快速识别通过/失败状态。

### 原因分析
1. 早期开发时仅用 `color: #67c23a` / `#f56c6c` 给文字上色，没有背景填充和形状
2. ReportList 后续优化时已升级为 `count-badge` 渐变徽章样式（绿/红/紫三种主题色 + 图标 + 圆角）
3. 两个页面是同一平台（app-automation）下的执行/报告模块，UI 规范本应统一

### 修改方案
**1. 模板：使用 `count-badge` 徽章 + `count-cell` 容器**
```vue
<el-table-column label="步骤统计" width="220" align="center">
  <template #default="{ row }">
    <div class="count-cell">
      <span class="count-badge success">
        <el-icon><CircleCheck /></el-icon>
        {{ row.passed_steps || 0 }}
      </span>
      <span class="count-badge failed">
        <el-icon><CircleClose /></el-icon>
        {{ row.failed_steps || 0 }}
      </span>
      <span class="count-badge total">
        <el-icon><DataLine /></el-icon>
        总计 {{ row.total_steps || 0 }}
      </span>
    </div>
  </template>
</el-table-column>
```

**2. 图标导入：补充 `CircleCheck / CircleClose / DataLine`**
```js
import { Search, Refresh, VideoPause, Document, Warning, CircleCheck, CircleClose, DataLine } from '@element-plus/icons-vue'
```

**3. 样式：新增 `count-cell` 和 `count-badge`（与 ReportList 一致）**
- 通过（success）：绿色渐变 `#d1fae5 → #a7f3d0`，文字 `#047857`
- 失败（failed）：红色渐变 `#fee2e2 → #fecaca`，文字 `#b91c1c`
- 总数（total）：紫色渐变 `#ede9fe → #ddd6fe`，文字 `#5b21b6`
- 容器：inline-flex 居中、间距 6px、圆角 6px

### 优化效果
- 通过/失败/总数三种状态用三种颜色徽章区分，一眼可识别
- 渐变背景 + 图标 + 圆角让数据呈现更现代化
- 与 ReportList 报告页样式保持一致，提升整体 UI 一致性

---

## 2026-07-26 修复 Bug 分析图表 Y 轴刻度显示不全问题

### 问题
在 Bug 分析详情页的图表区域中，Y 轴的刻度数字（如 "6"、"8"、"10" 等）左侧被截断，显示不全。

### 原因分析
ECharts 图表的 `grid` 配置中 `left` 值设置过小（仅 5% ~ 10%），当 Y 轴刻度为两位数时，左侧空间不足以完整显示数字。此外，没有设置 `containLabel: true` 属性，导致 ECharts 无法自动计算包含标签所需的空间。

### 修改方案

**1. `frontend/src/views/data-factory/BugAnalysis.vue`**

修改了以下图表的 `grid` 配置：

- `renderModuleChart`: `{left:'10%'}` → `{left:'12%', right:'5%', containLabel:true}`
- `renderTimelineChart`: `{left:'10%'}` → `{left:'12%', right:'5%', containLabel:true}`
- `renderSeverityCrossChart`: 添加 `containLabel: true`
- `renderCustomFieldCharts`: `{left:'5%'}` → `{left:'12%', right:'5%', containLabel:true}`

### 经验教训
1. **ECharts grid 配置要留足空间**：`left` 值不能太小，对于有两位数刻度的 Y 轴，建议至少 12% 或更多
2. **使用 containLabel**：设置 `containLabel: true` 可以让 ECharts 自动计算包含轴标签的空间，避免手动调整的麻烦
3. **图表容器要有明确的尺寸**：确保图表容器有足够的高度和宽度

---

## 2026-07-25 改造 Bug 分析 AI 提示词为配置化方案

### 问题
`bug_analysis_ai.py` 中的所有 AI 提示词都是硬编码在代码中的，存在以下问题：
1. 提示词修改需要修改代码并重新部署
2. 无法通过数据库动态调整提示词策略
3. 不同环境下无法灵活切换提示词版本

### 原因分析
早期开发时为了快速实现功能，将提示词直接写在代码的 prompt 变量中。这种方式虽然简单，但在实际使用中存在明显的局限性：
1. 产品/运营人员无法独立调整提示词
2. 提示词的版本管理困难
3. 无法针对不同业务场景配置不同的提示词

### 修改方案

**1. 扩展 PromptConfig 模型（`apps/requirement_analysis/models.py`）**

在 `PROMPT_CHOICES` 中添加 Bug 分析相关的提示词类型：
```python
('bug_classify', 'Bug缺陷分类提示词'),
('bug_severity', 'Bug严重度推断提示词'),
('bug_module_fallback', 'Bug模块归类提示词'),
('bug_root_cause', 'Bug根因分析提示词'),
('bug_test_focus', 'Bug测试建议提示词'),
('bug_summary', 'Bug智能摘要提示词'),
('bug_risks', 'Bug风险分析提示词'),
('bug_keywords', 'Bug关键词提取提示词'),
('bug_module_deep', 'Bug模块深度分析提示词'),
```

**2. 创建默认提示词模板（`apps/data_factory/bug_analysis_ai.py`）**

新增 `DEFAULT_PROMPT_TEMPLATES` 字典，包含所有 Bug 分析功能的默认提示词：
- `bug_classify`: 缺陷分类提示词
- `bug_severity`: 严重度推断提示词
- `bug_module_fallback`: 模块归类提示词
- `bug_root_cause`: 根因分析提示词
- `bug_test_focus`: 测试建议提示词
- `bug_summary`: 智能摘要提示词
- `bug_risks`: 风险分析提示词
- `bug_keywords`: 关键词提取提示词
- `bug_module_deep`: 模块深度分析提示词

**3. 实现 PromptConfigService 服务类**

```python
class PromptConfigService:
    """提示词配置服务 - 支持从数据库读取配置或使用默认模板"""
    
    async def get_prompt(self, prompt_type, variables):
        # 1. 尝试从数据库获取配置
        # 2. 如果数据库无配置，使用默认模板
        # 3. 支持模板变量替换
```

核心功能：
- 优先从数据库 `PromptConfig` 表读取配置
- 数据库无配置时回退到 `DEFAULT_PROMPT_TEMPLATES`
- 支持 `{variable}` 占位符的动态替换
- 单例模式 + 内存缓存，减少数据库查询

**4. 改造各个 AI 方法使用配置化提示词**

将以下方法中的硬编码 prompt 替换为 `PromptConfigService.get_prompt()` 调用：
- `classify_defect()` → 使用 `bug_classify` 类型
- `infer_severity()` → 使用 `bug_severity` 类型
- `classify_module_fallback()` → 使用 `bug_module_fallback` 类型
- `generate_root_cause()` → 使用 `bug_root_cause` 类型
- `generate_test_focus()` → 使用 `bug_test_focus` 类型
- `generate_summary()` → 使用 `bug_summary` 类型
- `analyze_risks()` → 使用 `bug_risks` 类型
- `extract_keywords()` → 使用 `bug_keywords` 类型
- `_layer2_semantic_analysis()` → 使用 `bug_module_deep` 类型

### 影响范围
- `apps/requirement_analysis/models.py`: 扩展 PromptConfig 模型的提示词类型
- `apps/data_factory/bug_analysis_ai.py`: 重构 AI 方法的提示词获取逻辑

### 注意事项
1. `PROMPT_CHOICES` 扩展不需要数据库迁移（Django 的 choices 字段是验证层面的配置）
2. 新增的提示词类型需要在数据库 `prompt_config` 表中存在对应记录才会生效
3. 如果数据库中没有配置，会自动使用代码中的默认模板

### 经验教训
1. **AI 提示词应配置化**：提示词是 AI 应用的核心逻辑，应支持动态调整
2. **默认值保障可用性**：即使数据库配置缺失，系统也应能正常工作
3. **类型安全的 choices**：使用 Django 的 choices 字段可以在数据库层面验证配置的有效性
4. **缓存优化性能**：提示词配置相对稳定，使用内存缓存可以减少数据库查询

---

## 2026-07-27 修复云效同步 Bug 模块分布全部归入"其他"

### 问题
云效同步过来的 Bug 数据，在 Bug 分析看板中"模块分布"图表里**100% 都归到了"其他"**，而同期 Excel 上传的数据能正常按 `【模块名】` 分类。

### 原因分析
1. **云效 Bug 标题里没有 `【模块名】` 这种格式**——云效的工作项标题一般是用户自由填写的，不会带方括号标签。
2. `classify_module()` 走的是"标题 `【xx】` 标签 + 兜底标题前10字"逻辑，没标签就只能归到 `其他`。
3. 云效 Bug 数据**其实有模块信息**，但来源不是标题，而是：
   - 顶层 `labels` 字段（云效原生标签，每个元素含 `id/name/color`）
   - `customFieldValues` 里的"所属模块"/"功能模块"等自定义字段
4. 旧代码 `_preprocess_bugs()` **无条件用标题分类覆盖 module 字段**，导致即使云效同步时已经写入了 module（来自 `所属模块` 等自定义字段），也会被清空。
5. `convert_yunxiao_bugs()` 之前**没有解析顶层 `labels` 字段**，只读 `customFieldValues`/`customFields` 等。

### 修改方案
**预期**：模块数据不再从 bug 标题中取，直接取 labels 内容；后续云效提交 Bug 时由业务侧维护 labels 字段。

**1. `apps/data_factory/yunxiao_client.py` — `convert_yunxiao_bugs` 解析顶层 labels**
```python
# 解析顶层 labels 字段（云效原生标签，每个元素含 id/name/color）
labels_raw = item.get("labels") or item.get("label") or []
label_names = []
if isinstance(labels_raw, list):
    for lb in labels_raw:
        if isinstance(lb, dict):
            name = lb.get("name") or lb.get("displayName")
            if name:
                label_names.append(str(name).strip())
        elif isinstance(lb, str) and lb.strip():
            label_names.append(lb.strip())
if label_names:
    cf_map["labels"] = label_names
```

**2. `apps/data_factory/yunxiao_client.py` — module 字段优先级调整**
```python
"module": (label_names[0] if label_names else
           cf_map.get("所属模块") or cf_map.get("功能模块") or
           cf_map.get("module") or item.get("module") or item.get("moduleName", "")),
```

**3. `apps/data_factory/bug_analysis.py` — `_preprocess_bugs` 不再覆盖已有 module**
```python
b['tags'] = extract_tags(b.get('title', ''))
# 仅在 module 为空时按标题分类，避免覆盖云效同步过来的 module（来自 labels）
if not b.get('module'):
    b['module'] = classify_module(b['tags'], b.get('title', ''))
```

**4. `apps/data_factory/yunxiao_client.py` — 多 label 智能挑选模块**

云效一个 Bug 经常有多个 label（如 `["缺陷修复记录", "RMS"]`），不能只取第一个，否则会把状态类 label 误当成模块。新增 `_pick_module_name_from_labels` 辅助函数：

```python
_NON_MODULE_KEYWORDS = [
    "测试", "记录", "修复", "已修复", "已完成", "关闭", "打开", "待处理",
    "同步", "标签", "跟踪", "需求", "任务", "缺陷", "排查", "跟进", "复盘", "回归",
]

def _pick_module_name_from_labels(label_names: List[str]) -> str:
    """从多个 label 中挑选最像模块名的那个"""
    if not label_names:
        return ""
    for name in label_names:
        if name and not any(kw in name for kw in _NON_MODULE_KEYWORDS):
            return name
    return label_names[0]
```

`module` 字段取值改为：`_pick_module_name_from_labels(label_names) or cf_map.get("所属模块") or ...`

### 取值优先级
| 数据源 | module 来源 |
|--------|------------|
| 云效同步（有 labels） | `_pick_module_name_from_labels` 从中挑选最像模块的 |
| 云效同步（无 labels 但有"所属模块"字段） | `所属模块` |
| Excel 上传 | 标题 `【xx】` 标签 / 标题前10字 |

### 经验教训
1. **字段映射不能靠猜**：旧代码用 `cf_map.get("所属模块") or cf_map.get("功能模块")` 等一堆"猜"出来的 key 名，应对字段名不确定性时，应该**主动打印实际 cf_map 的 key**（代码里已有日志但没暴露给业务侧）。
2. **不要在预处理阶段盲目覆盖**：`_preprocess_bugs` 用标题推断 module 没问题，但应该只在**没有显式值时**才推断，否则会清掉上游已正确填入的模块信息。
3. **数据源差异需要在适配器层就处理好**：`from_yunxiao` 适配器应该负责"把云效原生字段映射到内部统一字段"，让下游 `_preprocess_bugs` 不用关心数据源差异。
4. **数据契约要靠业务侧维护**：本次需要云效提交 Bug 时由业务侧维护 labels 字段，这是在多个系统间约定数据契约时常见的做法——选一个字段后必须由上游持续维护，否则会逐步失效。
5. **多标签要智能挑选而非简单取第一个**：业务侧一个 Bug 挂多个 label 是常态（既打"模块"也打"状态"），代码必须有能力识别"哪个 label 是模块、哪个是状态"，不能无脑取 `labels[0]`。本次用一个轻量级关键词黑名单实现，后续如果业务侧 label 命名变得不规范，应升级为正则匹配或配置化白名单。

### 后续修复：自定义字段图表 tooltip 显示 undefined
本次改动让"labels"成为第一个有数据进入自定义字段分布图表的字段，**同时暴露了一个预先存在但从未触发的 tooltip bug**：

**问题**：`frontend/src/views/data-factory/BugAnalysis.vue` 中 `renderCustomFieldCharts` 的 tooltip formatter 写成了 `p => \`${p.name}: ${p.value}条\``，但 `trigger: 'axis'` 时 formatter 收到的是**数组**，应该用 `p[0].name` 和 `p[0].value`。

**原因**：这个图表在 `customFieldCharts` 中只对"有分布意义的字段"渲染。之前 `custom_fields` 里大多是 priority/severity/参与者等已排除字段，导致这张图从未被渲染过，bug 一直藏着。labels 加入后第一次渲染，bug 就暴露了。

**修改方案**：把 `p.name` / `p.value` 改为 `p[0].name` / `p[0].value`，与同文件 `renderTimelineChart` 写法保持一致。

### 回归校验
- Excel 上传路径不受影响：Excel 的 bug dict 没有 `module` 字段，仍走 `if not b.get('module')` 分支按标题分类。
- 云效无 labels 的历史数据：仍走 `if not b.get('module')` 分支，回退到标题分类（行为与之前一致）。
- 前端 modulesData 接口契约不变。

---

## 2026-07-19 统一 SuiteList 页面徽章样式与 DeviceList、ProjectList 一致