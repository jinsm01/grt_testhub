---

## 2026-07-19 统一 SuiteList 页面徽章样式与 DeviceList、ProjectList 一致

### 问题
SuiteList 页面的徽章样式与 DeviceList、ProjectList 不一致：
- status-badge 使用较小的 padding（0 12px）和字体（12px），而其他页面使用 6px 16px 和 13px
- count-badge 宽度较窄（min-width: 32px），而其他页面使用固定宽度 44px

### 原因分析
不同页面开发时间不同，徽章样式没有统一规范：
1. SuiteList 早期开发时使用了较小的样式参数
2. DeviceList 和 ProjectList 后续统一了更大的徽章尺寸
3. 导致同一平台内视觉风格不一致，影响用户体验

### 修改方案
将 SuiteList 的徽章样式统一到 DeviceList、ProjectList 的标准：

**1. status-badge 统一样式**
```css
.status-badge {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 6px 16px;          /* 原 0 12px */
  border-radius: 4px;
  font-size: 13px;             /* 原 12px */
  font-weight: 500;
  transition: all 0.3s ease;
  white-space: nowrap;
}
```

**2. count-badge 统一样式**
```css
.count-badge {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 44px;                 /* 原 min-width: 32px */
  height: 24px;
  border-radius: 4px;
  font-size: 13px;             /* 原 12px */
  font-weight: 500;
  background: #e6f7ff;
  color: #1890ff;
  white-space: nowrap;
}
```

### 涉及文件
- `frontend/src/views/app-automation/suites/SuiteList.vue`

### 经验教训
1. **平台视觉一致性**：同一平台内的徽章、按钮等基础组件应保持统一样式
2. **样式规范先行**：开发新页面时参考已有页面的样式规范，避免后期统一调整
3. **使用固定尺寸**：count-badge 等数字徽章使用固定宽度，避免内容长短不一导致视觉不齐

---

## 2026-07-19 优化 count-badge 样式使用 padding 替代固定宽高

### 问题
用例数、套件数等数量徽章（count-badge）使用固定宽高（width: 44px, height: 24px），与状态徽章（status-badge）的样式风格不一致：
- status-badge 使用 padding 控制尺寸，内容自适应宽度
- count-badge 使用固定宽高，数字位数不同时视觉不协调

### 原因分析
早期开发时为保持数字徽章宽度一致，使用了固定宽高。但实际应用中：
1. 数字可能是 1 位数（如 5），也可能是 3 位数（如 128）
2. 固定宽度导致 1 位数看起来空旷，3 位数可能被压缩
3. 与状态徽章的视觉风格不统一

### 修改方案
将 count-badge 改为使用 padding，参考 status-badge 样式：

```css
.count-badge {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 6px 16px;     /* 替代原 width: 44px; height: 24px; */
  border-radius: 4px;
  font-size: 13px;
  font-weight: 500;
  background: #e6f7ff;
  color: #1890ff;
  white-space: nowrap;
}
```

**优势**：
1. 数字位数不同时，宽度自适应，视觉更协调
2. 与 status-badge 的 padding 值一致，保持统一的高度和间距
3. 语义更清晰：padding 表示内边距，更符合徽章的设计意图

### 涉及文件
- `frontend/src/views/app-automation/suites/SuiteList.vue`
- `frontend/src/views/app-automation/projects/ProjectList.vue`
- `frontend/src/views/app-automation/devices/DeviceList.vue`
- `frontend/src/views/app-automation/elements/ElementList.vue`

### 经验教训
1. **灵活布局优先**：使用 padding 而非固定尺寸，让内容决定容器大小
2. **同类组件统一样式**：状态徽章和数量徽章本质都是标签，应使用相同的尺寸控制方式
3. **数字徽章的自适应**：数字位数变化大时，自适应宽度比固定宽度更合适

---

## 2026-07-19 统一 SuiteList 与 ExecutionList 筛选组件布局结构

### 问题
SuiteList 的筛选组件使用 `div.filter-bar` + `flex` 布局，与 ExecutionList 的 `el-card` + `el-row/el-col` 布局不一致，导致平台内页面风格不统一。

### 原因分析
不同页面开发时间不同，布局方式选择不一致：
1. SuiteList 早期使用自定义的 flex 布局，筛选项较多
2. ExecutionList 后续使用 Element Plus 的栅格系统，更规范
3. 两个页面的筛选组件结构、样式差异明显

### 修改方案
将 SuiteList 的筛选组件改为与 ExecutionList 统一的布局结构：

**1. 模板结构调整**
- 将 `<div class="filter-bar">` 改为 `<el-card class="toolbar" shadow="never">`
- 使用 `el-row` + `el-col` 栅格布局替代 flex 布局
- 筛选项顺序调整为：搜索框 → 项目选择 → 状态筛选 → 新建按钮

**2. 筛选项优化**
- 移除设备选择、应用选择（保留在执行时选择）
- 新增状态筛选框（未执行、执行中、通过、失败、执行异常）
- 隐藏刷新按钮（自动刷新机制已足够）

**3. 样式调整**
- 删除 `.filter-bar` 及其子样式
- 改用 `.toolbar` 样式，保持紫色主题

**最终布局**：

| 组件 | 列宽 |
|------|------|
| 搜索框 | :span="6" |
| 项目选择 | :span="4" |
| 状态筛选 | :span="4" |
| 新建按钮 | :span="10" (右对齐) |

### 涉及文件
- `frontend/src/views/app-automation/suites/SuiteList.vue`

### 经验教训
1. **统一布局系统**：平台内所有页面应使用相同的布局系统（如 Element Plus 栅格）
2. **筛选项精简**：不必要的筛选项应移除，保持界面简洁
3. **组件复用**：相同功能模块（如筛选栏）应使用相同的组件结构和样式

---

## 2026-07-19 统一 ScheduledTasks 页面布局与 ProjectList 风格一致

### 问题
ScheduledTasks 页面使用独立的布局结构（`.scheduled-tasks`、`.header`、`.filters`），与平台其他页面（ProjectList）的 `.page-container`、`.filter-bar`、`.card-container` 布局风格不一致。

### 原因分析
不同页面开发时间不同，布局命名和结构选择不一致：
1. ScheduledTasks 使用 `el-row/el-col` 栅格布局筛选区，并有独立的标题头部
2. ProjectList 使用 flex 布局筛选区，更加简洁
3. 表格缺少卡片容器包裹，与其他页面视觉层次不同

### 修改方案
将 ScheduledTasks 的布局改为与 ProjectList 统一：

**1. 模板结构调整**
- 将 `.scheduled-tasks` 改为 `.page-container`
- 删除独立的 `.header` 区域（标题多余，面包屑已显示）
- 将 `.filters` 改为 `.filter-bar`，使用 flex 布局
- 添加 `.card-container` 包裹表格和分页

**2. 筛选组件优化**
- 移除"重置"和"查询"按钮，改为筛选变更时自动触发查询
- 筛选项排列：项目 → 任务类型 → 触发器类型 → 状态 → 新建按钮

**3. 样式统一**
- 使用 `.page-container` 渐变背景
- 使用 `.filter-bar` 白色卡片样式
- 使用 `.card-container` 包裹表格
- 使用 `.create-btn` 渐变按钮样式

### 涉及文件
- `frontend/src/views/app-automation/scheduled-tasks/ScheduledTasks.vue`

### 经验教训
1. **统一命名规范**：页面容器使用 `.page-container`，筛选栏使用 `.filter-bar`，表格容器使用 `.card-container`
2. **去除冗余标题**：面包屑已有页面名称，无需独立标题区域
3. **自动查询优化**：筛选项变更时自动查询，减少用户点击次数

---

## 2026-07-16 统一 SceneBuilder 页面按钮样式与平台其他模块一致

### 问题
SceneBuilder 页面按钮使用 Element Plus 默认样式（紫色 type="primary"、绿色 type="success"），与平台其他模块（ProjectDetail 等）的渐变紫色 + 白色描边风格不一致，视觉上不统一。

### 原因分析
1. 平台其他模块（ProjectDetail、TestCaseForm 等）使用统一的 `action-btn` 样式：
   - `edit-btn`：紫色渐变背景 `#7b42f6 → #5a32a3`
   - `cancel-btn`：白色背景 + 紫色描边
2. SceneBuilder 页面直接使用 Element Plus 的 `type="primary"` 和 `type="success"`，没有覆盖样式
3. 导致整个页面按钮色调混杂，破坏整体风格一致性

### 修改方案
将 SceneBuilder 中所有主要操作按钮改为与平台一致的 `action-btn` 样式：

**1. 添加统一 action-btn 样式到 SceneBuilder**
```css
.ui-test-scene-builder .action-btn {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 8px 16px;
    border-radius: 6px;
    font-weight: 500;
    transition: all 0.3s ease;
}

.ui-test-scene-builder .action-btn.edit-btn {
    background: linear-gradient(135deg, #7b42f6 0%, #5a32a3 100%);
    border: none;
    color: #ffffff;
}

.ui-test-scene-builder .action-btn.edit-btn:hover {
    background: linear-gradient(135deg, #6b35e8 0%, #4a2593 100%);
    transform: translateY(-1px);
    box-shadow: 0 4px 12px rgba(123, 66, 246, 0.3);
}

.ui-test-scene-builder .action-btn.cancel-btn {
    background: #ffffff;
    border: 1px solid rgba(147, 112, 219, 0.3);
    color: #5a32a3;
}

.ui-test-scene-builder .action-btn.cancel-btn:hover {
    background: #f5f3ff;
    border-color: #7b42f6;
    color: #4a2593;
    transform: translateY(-1px);
    box-shadow: 0 2px 8px rgba(123, 66, 246, 0.15);
}
```

**2. 修改场景配置卡片头部按钮**
- 保存用例：添加 `class="action-btn edit-btn"`
- 重置：添加 `class="action-btn cancel-btn"`

**3. 修改组件库卡片头部按钮**
- 导入组件包：添加 `class="action-btn edit-btn"`
- 导出组件包：添加 `class="action-btn cancel-btn"`

**4. 修改场景步骤卡片按钮**
- 另存为自定义组件：添加 `class="action-btn edit-btn"`

**5. 修改组件配置卡片按钮**
- 创建元素工具：添加 `class="action-btn edit-btn"`

**6. 清理重复 el-button 标签**
- 修复了组件配置区域由于编辑操作产生的重复 `<el-button>` 起始标签

### 涉及文件
- `frontend/src/views/app-automation/test-cases/SceneBuilder.vue`

### 经验教训
1. **跨页面样式一致性**：平台应建立统一的按钮组件库或样式约定，避免不同页面风格割裂
2. **使用语义化 className**：`action-btn edit-btn` 比 `type="primary"` 更易维护和定制
3. **保持使用本地作用域**：使用 `.ui-test-scene-builder` 前缀避免污染其他页面
4. **复制代码时注意细节**：编辑时容易产生重复标签，需要仔细检查 DOM 结构

---

## 2026-07-16 优化页面标题布局，合并操作按钮到场景配置区

### 问题
页面顶部有"App自动化用例编排"标题 + "保存用例"、"重置"按钮，与下方"场景配置"卡片头部存在视觉重复。标题区域占用了额外空间，布局不紧凑。

### 原因分析
1. **冗余的标题区域**：面包屑已经显示"用例编排"，页面顶部再放一个标题显得多余
2. **按钮位置不合理**：操作按钮（保存/重置）与配置区分离，用户需要跨越较长距离才能操作
3. **空间利用低**：标题区域占用了 12-20px 高度的 padding/margin，没有实际信息价值

### 修改方案
将标题"App自动化用例编排"删除，操作按钮"保存用例"、"重置"合并到场景配置卡片的 header 区域右侧。

**1. 删除 page-header 区域**
- 移除整个 `<div class="page-header">` 块
- 移除相关 CSS：`.page-header`、`.page-header h3`、`.header-actions`

**2. 按钮整合到场景配置卡片 header**
- 在 `header-meta` 区域依次排列：变量数 tag → 保存用例按钮 → 重置按钮
- 使用 `gap: 12px` 保持间距

**3. 清理无用样式**
- 删除 `.page-header` 相关 CSS，避免样式污染

### 涉及文件
- `frontend/src/views/app-automation/test-cases/SceneBuilder.vue`

### 经验教训
1. **避免视觉重复**：面包屑已有"用例编排"，页面顶部无需重复标题
2. **操作按钮就近原则**：保存/重置等全局操作应放在最常用的区域附近，减少用户操作距离
3. **删除元素时同步清理样式**：避免遗留无用 CSS 造成维护负担

---

## 2026-07-16 优化场景配置区交互布局（场景变量+运行时配置）

### 问题
场景配置区（场景名称、所属项目、场景描述、场景变量、API_BASE_URL、重试次数、重试间隔）布局松散，不够紧凑，视觉效果不符合现代主流风格：
- 表单 label 横向排列占用大量横向空间
- 场景变量每一行没有视觉分组，整体散乱
- "删除"按钮是文字链接，不够明显
- 重试次数/间隔使用普通输入框，没有数字调节器
- 缺少区块标题和分组，模块边界不清

### 原因分析
原表单使用了 `label-width="120px" size="small"` 的传统布局，这种布局方式：
1. label 占据固定宽度，导致输入框被压缩
2. size="small" 让所有控件变小，但整体没有紧凑感
3. 场景变量项是简单的 grid 布局，缺少视觉层次
4. 没有使用 el-divider 进行区块分组

### 修改方案
采用 Linear、Notion 等现代工具的布局风格重新组织：

**1. 表单布局重构**
- 改用 `label-position="top"`：label 在输入框上方，更符合现代风格
- 使用 `size="default"`：控件大小适中，视觉舒适
- 调整栅格比例：场景名称(8) + 所属项目(6) + 场景描述(10)

**2. 添加卡片头部**
- 头部使用紫色渐变背景，与其他卡片统一
- 左侧显示图标 + "场景配置" 标题
- 右侧显示变量数标签

**3. 区块分组（el-divider）**
- 用分割线分隔"基础信息"、"场景变量"、"运行时配置"三块
- 分割线中间显示图标 + 文字

**4. 场景变量项优化**
- 添加左侧渐变背景和圆角边框
- 悬停时背景变化 + 阴影提升
- 调整列宽比例：160/130/110/1.2fr/1.5fr/36px
- 变量名添加 document 图标前缀
- "删除"按钮改为圆形危险按钮，更明显
- 无变量时显示虚线大按钮"添加第一个变量"

**5. 运行时配置优化**
- API_BASE_URL 添加 link 图标前缀
- 重试次数/间隔改用 el-input-number 数字调节器
- 三个字段并排：URL(12) + 重试次数(6) + 重试间隔(6)

### 涉及文件
- `frontend/src/views/app-automation/test-cases/SceneBuilder.vue` - 模板和样式重构
- 图标导入：`Setting, Document, Coin, Operation, Delete, Plus`

### 经验教训
1. **现代表单趋向 label-top**：相比传统 label-width 固定宽度，label 在上方更灵活，控件可以更宽
2. **视觉分组很重要**：用 el-divider 或 section 划分不同模块，提升信息层次
3. **删除按钮要明显**：文字"删除"链接容易忽略，圆形危险按钮更直观
4. **数字字段用 input-number**：相比普通输入框，自带调节按钮且可限制范围
5. **空状态友好提示**：变量为空时显示"添加第一个变量"大按钮，引导用户操作

---

## 2026-07-16 优化 App自动化用例编排页面视觉风格

### 问题
App自动化用例编排页面（SceneBuilder.vue）布局样式过于传统，视觉效果不够现代化：
- 卡片样式单薄，缺乏层次感
- 组件拖拽区域视觉效果不佳
- 步骤列表显示不够清晰
- 配置面板布局过于紧凑
- 缺少交互过渡效果

### 原因分析
页面样式是早期开发时定义的，未与后续统一的紫色主题风格同步更新：
1. 卡片使用简单的边框和浅色背景，没有现代感的阴影和渐变
2. 组件项使用虚线边框，视觉上不够精致
3. 步骤索引样式单调，没有视觉焦点
4. 滚动条使用浏览器默认样式，与主题不符
5. 缺少悬停、激活等状态的视觉反馈

### 修改方案
在不改变页面功能和浅紫色背景的前提下，对视觉样式进行全面优化：

**1. 页面头部优化**
- 添加毛玻璃效果（backdrop-filter: blur）
- 使用渐变背景和圆角设计
- 标题使用更深的紫色，增强视觉层次

**2. 组件库卡片优化**
- 组件项使用渐变背景和圆角边框
- 添加悬停动画（transform + box-shadow）
- 类型标签使用紫色背景突出显示
- 滚动条自定义样式

**3. 场景步骤区域优化**
- 步骤索引使用紫色渐变和阴影效果
- 选中状态使用渐变背景区分
- 悬停显示微动效
- 子步骤区域使用暖色调区分

**4. 配置面板优化**
- 空状态提示使用渐变背景和虚线边框
- 字段分组添加悬停效果
- 表单区域添加滚动条美化

**5. 通用优化**
- 对话框添加圆角和阴影
- Tab 标签使用紫色主题
- 按钮悬停添加动画效果
- 输入框聚焦显示紫色边框
- 全局滚动条统一紫色主题

### 关键样式代码示例

```css
/* 卡片毛玻璃效果 */
.palette-card {
    border-radius: 12px;
    background: rgba(255, 255, 255, 0.92);
    backdrop-filter: blur(10px);
    box-shadow: 0 2px 12px rgba(139, 92, 246, 0.08);
}

/* 组件项悬停动效 */
.palette-item:hover {
    border-color: #a78bfa;
    background: linear-gradient(135deg, #fefefe 0%, #f5f3ff 100%);
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(139, 92, 246, 0.15);
}

/* 步骤索引渐变 */
.scene-index {
    background: linear-gradient(135deg, #a78bfa 0%, #8b5cf6 100%);
    box-shadow: 0 2px 4px rgba(139, 92, 246, 0.3);
}

/* 按钮悬停效果 */
.el-button--primary:hover {
    background: linear-gradient(135deg, #8b5cf6 0%, #7c3aed 100%);
    transform: translateY(-1px);
    box-shadow: 0 4px 12px rgba(139, 92, 246, 0.3);
}
```

### 涉及文件
- `frontend/src/views/app-automation/test-cases/SceneBuilder.vue` - 全面优化样式部分

### 视觉改进要点
1. **渐变背景**：卡片头部、按钮、索引等使用渐变，增加现代感
2. **毛玻璃效果**：卡片使用 backdrop-filter 增强层次感
3. **微动效**：悬停时添加 transform 和 box-shadow 变化
4. **统一紫色主题**：所有交互元素使用统一的紫色系（#a78bfa - #8b5cf6）
5. **圆角统一**：卡片 12px，按钮/标签 8px，步骤项 10px

### 经验教训
1. **UI 优化应保持主题色一致性**——所有交互元素使用同一色系
2. **适当使用微动效提升体验**——但不要过度，影响性能
3. **保留原有功能是优化的前提**——不改变任何交互逻辑
4. **滚动条样式自定义**——提升整体视觉一致性

---

## 2026-07-15 修复设备截图失败时的错误提示问题

### 问题
设备离线时截图返回500错误，前端只显示"截图失败"，没有显示具体原因。同时后端返回格式不一致（有时用 `msg`，有时用 `message`）。

### 原因分析
1. **前端错误处理不完善**：catch 块中只显示硬编码的"截图失败"，没有提取后端返回的具体错误信息
2. **后端缺少设备连接状态检查**：只检查数据库中的 `status` 字段，没有通过 ADB 实际检查设备连接状态
3. **返回字段名不一致**：后端有时用 `msg`，有时用 `message`

### 修改方案

**1. 前端改进错误提示**
```javascript
} catch (error) {
  console.error('截图失败:', error)
  // 显示更详细的错误信息
  const errorMsg = error.response?.data?.message || error.response?.data?.msg || error.message || '截图失败'
  ElMessage.error(errorMsg)
}
```

**2. 后端添加设备连接状态检查**
```python
# 先检查设备是否真的在线（通过ADB）
check_result = subprocess.run(
    [adb_path, '-s', device.device_id, 'get-state'],
    capture_output=True,
    text=True,
    timeout=5
)

if check_result.returncode != 0 or 'device' not in check_result.stdout:
    # 设备实际已离线，更新数据库状态
    device.status = 'offline'
    device.save(update_fields=['status'])
    return Response({
        'code': 400,
        'message': '设备连接已断开，请先连接设备',
        'success': False
    }, status=status.HTTP_400_BAD_REQUEST)
```

**3. 统一返回字段名**
将所有 `msg` 字段改为 `message`，保持一致性。

### 涉及文件
- `frontend/src/views/app-automation/elements/components/CaptureElementDialog.vue`
- `apps/app_automation/views/device_views.py`

### 经验教训
1. **设备操作前应检查实际连接状态**：数据库状态可能已过期，需要通过ADB实时检查
2. **API返回字段应保持一致**：统一使用 `message` 而非混用 `msg`
3. **前端应显示具体错误信息**：帮助用户理解问题原因并采取正确行动

---

## 2026-07-15 修复设备列表刷新后数据消失的问题

### 问题
设备列表页面，点击"刷新设备"按钮后，设备数据消失；但切换菜单后再回来，数据又显示了。

### 原因分析
前端有两个不同的API调用，行为不一致：
1. **`getDevices()`** - 调用 `getDeviceList` API，从**数据库**读取设备列表
2. **`refreshDevices()`** - 调用 `discoverDevices` API，通过 **ADB扫描**当前连接的设备

问题在 `refreshDevices()` 函数的实现：
```javascript
const refreshDevices = async () => {
  const res = await discoverDevices()
  if (res.data.success) {
    devices.value = res.data.devices || []  // 直接使用ADB扫描结果
  }
}
```

当ADB检测不到设备时（设备离线、ADB服务未启动等），`res.data.devices` 返回空数组，前端就显示空列表。但数据库中的设备记录仍然存在，所以切换菜单后调用 `getDevices()` 又能读到数据。

**本质问题**：`discoverDevices` API 的设计是返回"当前ADB连接的设备"，而不是"数据库中的所有设备"。

### 修改方案
修改前端的 `refreshDevices()` 函数，在ADB扫描成功后，重新从数据库获取设备列表：

```javascript
const refreshDevices = async () => {
  refreshing.value = true
  try {
    const res = await discoverDevices()
    if (res.data.success) {
      ElMessage.success(res.data.message || '设备列表已刷新')
      // 刷新后重新从数据库获取设备列表，确保显示所有设备（包括离线设备）
      await getDevices()
    } else {
      ElMessage.error(res.data.message || '刷新设备列表失败')
    }
  } catch (error) {
    console.error('刷新设备列表失败:', error)
    ElMessage.error('刷新设备列表失败: ' + (error.message || '未知错误'))
  } finally {
    refreshing.value = false
  }
}
```

### 涉及文件
- `frontend/src/views/app-automation/devices/DeviceList.vue`

### 经验教训
1. **区分"扫描"和"获取"API的行为**：扫描类API返回的是实时检测结果，获取类API返回的是数据库持久化数据
2. **刷新操作后应重新获取持久化数据**：确保前端显示与数据库一致，而不是依赖扫描结果
3. **离线设备的管理策略**：设备离线时不应该从列表中消失，而是显示"离线"状态

---

## 2026-07-14 修复 app-automation 模块菜单切换页面不变的问题

### 问题
app-automation 模块下切换菜单时，页面内容不更新，始终显示同一个页面。

### 原因分析
**根本原因**：Vue Router 的组件复用机制。当不同路由使用相同文件名的组件时（如 app-automation/projects/ProjectList.vue 和 ui-automation/projects/ProjectList.vue），Vue 会推断组件名为相同的 'ProjectList'，导致路由切换时复用组件实例而不重新渲染。

**次要原因**：
1. Layout 的 `<router-view />` 没有设置 `:key` 属性，路由切换时不会强制重新创建组件实例。
2. ProjectList.vue 组件中缺少 `formatDate`、`handleView`、`handleEdit` 函数定义，导致组件渲染失败，进而影响整个应用的状态。

### 修改方案
**1. 在 Layout 的 router-view 上添加 :key 属性**
```vue
<router-view :key="$route.fullPath" />
```
这会强制路由切换时重新创建组件实例。

**2. 为 app-automation 模块各组件添加唯一组件名**
使用 `defineOptions` 为每个组件定义唯一的 name（Vue 3.3+ 中 defineOptions 是编译器宏，不需要导入）：
```javascript
defineOptions({ name: 'AppProjectList' })
```

**3. 补充缺失的函数定义**
在 ProjectList.vue 中添加缺失的函数：
```javascript
function formatDate(dt) {
  if (!dt) return '-'
  return new Date(dt).toLocaleDateString('zh-CN', { year: 'numeric', month: '2-digit', day: '2-digit' })
}

function handleView(row) {
  viewDetail(row)
}

function handleEdit(row) {
  openEditDialog(row)
}
```

### 涉及文件
- `frontend/src/layout/index.vue` - 添加 `:key="$route.fullPath"`
- `frontend/src/views/app-automation/projects/ProjectList.vue` - 添加 `defineOptions`，补充 `formatDate`、`handleView`、`handleEdit` 函数
- `frontend/src/views/app-automation/devices/DeviceList.vue` - 添加 `defineOptions({ name: 'AppDeviceList' })`
- `frontend/src/views/app-automation/packages/PackageList.vue` - 添加 `defineOptions({ name: 'AppPackageList' })`
- `frontend/src/views/app-automation/elements/ElementList.vue` - 添加 `defineOptions({ name: 'AppElementList' })`
- `frontend/src/views/app-automation/test-cases/TestCaseList.vue` - 添加 `defineOptions({ name: 'AppTestCaseList' })`
- `frontend/src/views/app-automation/suites/SuiteList.vue` - 添加 `defineOptions({ name: 'AppSuiteList' })`
- `frontend/src/views/app-automation/notification/NotificationLogs.vue` - 添加 `defineOptions({ name: 'AppNotificationLogs' })`
- `frontend/src/views/app-automation/reports/ReportList.vue` - 添加 `defineOptions({ name: 'AppReportList' })`
- `frontend/src/views/app-automation/scheduled-tasks/ScheduledTasks.vue` - 添加 `defineOptions({ name: 'AppScheduledTasks' })`
- `frontend/src/views/app-automation/dashboard/Dashboard.vue` - 添加 `defineOptions({ name: 'AppAutomationDashboard' })`

### 经验教训
1. **Vue Router 组件复用机制**——当两个路由使用相同组件时，Vue 会复用实例，导致页面不更新。添加 `:key="$route.fullPath"` 可强制重新渲染。
2. **组件名冲突**——相同文件名的组件可能被推断为相同的组件名，导致复用。使用 `defineOptions` 定义唯一组件名是最佳实践。
3. **defineOptions 不需要导入**——Vue 3.3+ 中 `defineOptions` 是编译器宏，直接使用即可，不需要 `import { defineOptions } from 'vue'`。
4. **函数定义与模板使用必须一致**——模板中使用的函数必须在 script 中定义，否则会导致组件渲染失败，进而影响整个应用的路由切换。

---

## 2026-07-14 统一 app-automation 模块表格表头颜色和布局结构

### 问题
app-automation 模块下各页面的表格表头颜色仍使用旧紫色系（#5a32a3），与平台其他模块（#8b5cf6）不一致。部分页面仍使用 el-card 包裹，缺少统一的 filter-bar 和 card-container 布局结构。

### 原因分析
之前的样式统一只更新了页面背景和卡片样式，但表格表头颜色和页面布局结构未完全统一。各页面使用的颜色值不一致，部分页面仍使用 el-card 组件而非标准的 div 容器。

### 修改方案
**1. 统一表头颜色**
将所有表格表头颜色从 `#5a32a3` 更新为 `#8b5cf6`：
```css
.el-table__header th {
  color: #8b5cf6;  /* 原 #5a32a3 */
}
```

**2. 统一页面布局结构**
替换 el-card 为标准布局结构：
- `.page-container` - 页面主容器，包含渐变背景
- `.filter-bar` - 筛选栏，包含搜索、筛选和操作按钮
- `.card-container` - 卡片容器，包含表格和分页

**3. 统一按钮样式**
创建按钮使用紫色渐变背景：
```css
.create-btn {
  background: linear-gradient(135deg, #a78bfa 0%, #8b5cf6 100%);
  box-shadow: 0 4px 12px rgba(167, 139, 250, 0.3);
}
```

### 涉及文件
- `frontend/src/views/app-automation/projects/ProjectList.vue`
- `frontend/src/views/app-automation/test-cases/TestCaseList.vue`
- `frontend/src/views/app-automation/packages/PackageList.vue`
- `frontend/src/views/app-automation/suites/SuiteList.vue`
- `frontend/src/views/app-automation/reports/ReportList.vue`
- `frontend/src/views/app-automation/notification/NotificationLogs.vue`
- `frontend/src/views/app-automation/devices/DeviceList.vue`
- `frontend/src/views/app-automation/elements/ElementList.vue`

### 经验教训
1. **样式修改应逐层深入**——先统一大布局，再统一细节样式
2. **对比参考页面**——以已统一的页面（executions）为基准进行对比修改
3. **使用 :deep() 穿透 scoped 样式**——确保能覆盖 Element Plus 组件的默认样式

---

## 2026-07-14 统一 app-automation 模块列表样式和分页组件风格

### 问题
app-automation 模块下各页面的表格和分页组件样式与 ui-automation 模块不一致：
- 表格表头颜色、行悬停效果、条纹背景不统一
- 分页组件样式差异较大，有的居中有的居右
- 缺少统一的紫色主题色配置
- 表格边框和圆角风格不一致

### 原因分析
虽然之前已统一了页面背景和卡片样式，但表格和分页组件的样式仍使用 Element Plus 默认风格，没有按紫色主题进行定制。

### 修改方案
**1. 统一表格样式**
为所有包含 `el-table` 的页面添加统一的紫色主题表格样式：

```css
/* 表格主题变量 */
--el-color-primary: #a78bfa;
--el-table-header-bg-color: #ffffff;
--el-table-row-hover-bg-color: #f8f7ff;
--el-table-stripe-bg-color: #fafaff;

/* 表头样式 */
.el-table__header th {
  background-color: #ffffff;
  color: #5a32a3;
  font-weight: 600;
  font-size: 14px;
  padding: 16px;
}

/* 表格行样式 */
.el-table__body tr:hover {
  background-color: #f8f7ff;
}
.el-table__row--striped {
  background-color: #fafaff;
}
```

**2. 统一分页容器类名**
将所有分页容器从 `.pagination` 统一为 `.pagination-container`：

```html
<div class="pagination-container">
  <el-pagination ... />
</div>
```

**3. 统一分页样式**
添加紫色主题的分页组件样式：
- 居中显示
- 按钮圆角 8px
- 悬停显示紫色阴影效果
- 页码选中状态显示紫色边框和背景

### 涉及文件
- `frontend/src/views/app-automation/projects/ProjectList.vue`
- `frontend/src/views/app-automation/test-cases/TestCaseList.vue`
- `frontend/src/views/app-automation/test-cases/SceneBuilder.vue`
- `frontend/src/views/app-automation/devices/DeviceList.vue`
- `frontend/src/views/app-automation/packages/PackageList.vue`
- `frontend/src/views/app-automation/executions/ExecutionList.vue`
- `frontend/src/views/app-automation/suites/SuiteList.vue`
- `frontend/src/views/app-automation/notification/NotificationLogs.vue`
- `frontend/src/views/app-automation/reports/ReportList.vue`
- `frontend/src/views/app-automation/scheduled-tasks/ScheduledTasks.vue`
- `frontend/src/views/app-automation/elements/ElementList.vue`

### 经验教训
1. **样式修改应覆盖所有相关组件**——表格、分页、表单等都需要统一主题风格
2. **使用 CSS 变量定义主题色**——便于后续统一调整和维护
3. **组件样式应考虑嵌套深度**——Element Plus 组件需要使用 `:deep()` 穿透 scoped 样式

---

## 2026-07-14 统一 app-automation 模块前端页面样式风格

### 问题
app-automation 模块下各页面样式风格与其他模块（如 ui-automation）不一致，存在以下问题：
- 页面背景为纯白色，缺乏主题感
- 卡片阴影和边框样式不统一，有的过于单薄
- 配色方案混乱，有蓝色主题（#409eff）、灰色主题等多种风格并存
- 圆角大小不一致（4px、8px、12px 混用）
- 页面标题字号和颜色不统一

### 原因分析
app-automation 模块是后续新增的功能模块，在开发过程中没有统一的设计规范，各页面自行定义样式，导致视觉风格碎片化。

### 修改方案
以 ui-automation 模块为基准，统一 app-automation 模块的视觉语言：

**1. 统一页面背景**
```css
background: linear-gradient(135deg, #f5f3ff 0%, #ede9fe 100%);
min-height: calc(100vh - 60px);
padding: 24px;
```

**2. 统一卡片样式**
```css
background: linear-gradient(135deg, #ffffff 0%, #f8f7ff 100%);
border-radius: 12px;
box-shadow: 0 4px 16px rgba(147, 112, 219, 0.1);
border: 1px solid rgba(147, 112, 219, 0.1);
```

**3. 统一主题色**
- 主色：`#5a32a3`（紫色系，与 ui-automation 保持一致）
- 标题色：`#1a1a1a`
- 边框色：`rgba(147, 112, 219, 0.1)`

**4. 统一表格样式**
```css
.el-table__header th {
  background-color: rgba(147, 112, 219, 0.05);
  color: #5a32a3;
  font-weight: bold;
}
.el-table__body tr:hover {
  background-color: rgba(147, 112, 219, 0.08);
}
```

**5. 统一页面标题**
```css
font-size: 24px;
font-weight: bold;
color: #1a1a1a;
```

### 涉及文件
- `frontend/src/views/app-automation/Index.vue`
- `frontend/src/views/app-automation/dashboard/Dashboard.vue`
- `frontend/src/views/app-automation/devices/DeviceList.vue`
- `frontend/src/views/app-automation/projects/ProjectList.vue`
- `frontend/src/views/app-automation/test-cases/TestCaseList.vue`
- `frontend/src/views/app-automation/test-cases/SceneBuilder.vue`
- `frontend/src/views/app-automation/elements/ElementList.vue`
- `frontend/src/views/app-automation/executions/ExecutionList.vue`
- `frontend/src/views/app-automation/suites/SuiteList.vue`
- `frontend/src/views/app-automation/scheduled-tasks/ScheduledTasks.vue`
- `frontend/src/views/app-automation/reports/ReportList.vue`
- `frontend/src/views/app-automation/packages/PackageList.vue`
- `frontend/src/views/app-automation/notification/NotificationLogs.vue`
- `frontend/src/views/app-automation/settings/AppSettings.vue`（已符合规范，无需修改）

### 经验教训
1. **新模块开发前应制定统一的设计规范**——包括配色、圆角、阴影、间距等设计token
2. **优先使用 CSS 变量定义主题色**——便于后续统一调整和维护
3. **大型项目应抽离公共样式组件**——避免重复定义样式，提高一致性
4. **样式修改后应进行构建验证**——确保没有语法错误导致构建失败

---

## 2026-07-14 修复首页 APP 自动化入口需连点 10 次才能进入的限制

### 问题
首页 APP 自动化卡片需要**连续点击 10 次**才能跳转到对应页面，每次点击会弹出 `功能完善中，请耐心等待（n/10）` 的提示，体验非常差。

### 原因分析
在 `frontend/src/views/Home.vue` 的 `handleNavigate` 中，对 `type === 'app'` 做了"后门"特殊处理：

```js
// 点击计数器（用于APP自动化后门功能）
const appClickCount = ref(0)
const lastAppClickTime = ref(0)
const SECRET_CLICK_THRESHOLD = 10 // 连续点击10次后允许访问
const CLICK_RESET_TIMEOUT = 3000  // 3秒内未点击则重置计数

// 对 APP 自动化进行特殊处理
if (type === 'app') {
  const currentTime = Date.now()
  if (currentTime - lastAppClickTime.value > CLICK_RESET_TIMEOUT) {
    appClickCount.value = 0
  }
  lastAppClickTime.value = currentTime
  appClickCount.value++

  if (appClickCount.value < SECRET_CLICK_THRESHOLD) {
    ElMessage.info(`功能完善中，请耐心等待（${appClickCount.value}/${SECRET_CLICK_THRESHOLD}）`)
    return
  }
  ElMessage.success(`APP自动化功能已解锁！`)
  appClickCount.value = 0
}
```

APP 自动化作为正常功能模块（路由 `/app-automation/dashboard` 已在侧边栏菜单、侧边栏跳转链接等多处可访问），再在前端做"连点 10 次解锁"的限制已无实际意义，反而影响用户操作。

### 修改方案
删除 `Home.vue` 中 `app` 类型的特殊处理逻辑和点击计数器相关变量，让 `handleNavigate` 对所有入口一视同仁：

```js
const handleNavigate = (type) => {
  const routes = {
    'ai': '/ai-generation/requirement-analysis',
    'api': '/api-testing/dashboard',
    'ui': '/ui-automation/dashboard',
    'app': '/app-automation/dashboard',
    'ai-intelligent': '/ai-intelligent-mode/testing',
    'assistant': '/ai-assistant/chat',
    'config': '/configuration/ai-model',
    'data': '/data-factory/by-scenario'
  }

  if (routes[type]) {
    router.push(routes[type])
  }
}
```

### 涉及文件
- `frontend/src/views/Home.vue` - 移除 `appClickCount` / `lastAppClickTime` / `SECRET_CLICK_THRESHOLD` / `CLICK_RESET_TIMEOUT` 及 `handleNavigate` 中 `type === 'app'` 的特殊分支

### 经验教训
1. **不要在前端做"未完成功能"的连点解锁**——既无安全价值，也极影响体验，应通过产品上下架流程控制
2. 路由/页面是否对外开放，应由后端权限和前端路由表统一控制，而不是依赖奇怪的点击计数

---

## 2026-07-09 修复创建定时任务失败问题（模型字段缺失导致序列化错误）

### 问题
创建定时任务时报 500 错误，前端报错：`Failed to load resource: the server responded with a status of 500 (Internal Server Error)`

### 原因分析
在提交 `1358f13` 中，为 `TestSuiteSerializer` 添加了 `mainline_test_case` 和 `mainline_case_updated` 字段的获取方法，但 **TestSuite 模型中并未添加这些字段**：

1. **TestSuiteSerializer 添加的字段获取方法**：
   ```python
   def get_mainline_test_case(self, obj):
       """获取关联的主线用例"""
       if obj.mainline_test_case:  # ← 访问不存在的字段
           return {...}
   
   def get_mainline_case_updated(self, obj):
       """判断关联主线用例是否有更新"""
       if not obj.mainline_test_case or not obj.mainline_case_checked_at:  # ← 访问不存在的字段
           return False
   ```

2. **TestSuite 模型实际字段**：
   ```python
   # 模型中只有这些字段，没有 mainline_test_case 和 mainline_case_checked_at
   ['id', 'project', 'name', 'description', 'environment', 'pre_process_script', 
    'post_process_script', 'created_by', 'created_at', 'updated_at', 'requests']
   ```

3. **问题触发场景**：
   - 创建 ScheduledTask 时会序列化关联的 TestSuite
   - 序列化器尝试访问 `obj.mainline_test_case` 字段
   - 因字段不存在抛出 `AttributeError`，导致 500 错误

### 修改方案
在 `TestSuiteSerializer` 的字段获取方法中添加 `hasattr()` 检查，避免访问不存在字段：

```python
def get_mainline_test_case(self, obj):
    """获取关联的主线用例"""
    # 检查字段是否存在，避免 AttributeError
    if hasattr(obj, 'mainline_test_case') and obj.mainline_test_case:
        return {
            'id': obj.mainline_test_case.id,
            'title': obj.mainline_test_case.title,
            'module': obj.mainline_test_case.module,
            'priority': obj.mainline_test_case.priority,
        }
    return None

def get_mainline_case_updated(self, obj):
    """判断关联主线用例是否有更新（自上次确认后）"""
    # 检查字段是否存在，避免 AttributeError
    if not hasattr(obj, 'mainline_test_case') or not hasattr(obj, 'mainline_case_checked_at'):
        return False
    if not obj.mainline_test_case or not obj.mainline_case_checked_at:
        return False
    from django.utils import timezone
    return obj.mainline_test_case.updated_at > obj.mainline_case_checked_at
```

### 涉及文件
- `apps/api_testing/serializers.py` - TestSuiteSerializer

### 经验教训
1. **修改序列化器添加字段时，必须确保模型已定义对应字段**
2. **使用 SerializerMethodField 时，应在获取方法中添加 hasattr() 检查**
3. **提交代码前应完整测试所有相关功能，包括定时任务创建**
4. **定时任务功能依赖 croniter 包，需确保该依赖已安装**

### 依赖问题
修复后发现另一个问题：**缺少 croniter 依赖包**

**错误信息**：
```python
ModuleNotFoundError: No module named 'croniter'
```

**原因**：ScheduledTask.calculate_next_run() 方法需要 croniter 来处理 Cron 表达式计算下次运行时间，但环境中未安装该依赖。

**解决方法**：
```bash
pip install croniter
```

---

## 2026-07-09 修复 API Testing Allure 报告无法访问问题

### 问题
访问 `http://127.0.0.1:3000/api-testing/allure-reports/execution_477/summary.html` 时无法正常显示 Allure 报告。

### 原因分析
1. **前端代理配置正常**：`vite.config.js` 中已正确配置 `/api-testing/allure-reports/` 代理到后端
2. **后端缺少静态文件路由**：`backend/urls.py` 中只配置了 APP 自动化的报告路径，但**缺少 API Testing 的 Allure 报告静态文件路由配置**
3. **报告文件已生成**：报告文件存储在 `MEDIA_ROOT/api-testing/allure-reports/execution_{id}/` 目录，但没有对应的 URL 路由映射

### 修改方案
在 `backend/urls.py` 中添加 API Testing 的 Allure 报告静态文件访问路由：

```python
# API测试 Allure 报告访问
urlpatterns += [
    path('api-testing/allure-reports/<path:path>', 
         serve, 
         {'document_root': os.path.join(settings.MEDIA_ROOT, 'api-testing', 'allure-reports')}),
]
```

这类似于已存在的 APP 自动化报告配置：
```python
# APP自动化 Allure 报告访问
urlpatterns += [
    path('app-automation-reports/<path:path>', 
         serve, 
         {'document_root': os.path.join(settings.MEDIA_ROOT, 'app-automation', 'allure-reports')}),
]
```

### 涉及文件
- `backend/urls.py`

---

## 2026-06-26 文件名显示改为后端返回指定格式

### 问题
迭代报告列表"文件名"列显示使用前端正则处理去掉 `_数字条` 后缀，应该由后端返回指定格式。

### 修改方案

#### 1. 后端返回 `display_name` 字段
（`apps/data_factory/bug_analysis_view.py`）
- 在 `bug_analysis_records` 接口返回数据中新增 `display_name` 字段
- 使用正则去掉 `_数字条` 后缀：
  ```python
  display_name = r.file_name
  if display_name:
      import re
      display_name = re.sub(r'_\d+条$', '', display_name)
  ```
- 返回结构：
  ```python
  {
      'file_name': r.file_name,        # 原始文件名
      'display_name': display_name or r.file_name,  # 显示文件名
  }
  ```

#### 2. 前端直接使用 `display_name`
（`frontend/src/views/data-factory/BugAnalysis.vue`）
- 表格列改为直接使用后端返回的 `display_name`：
  ```vue
  <span class="file-name">{{ row.display_name || row.file_name || '未命名' }}</span>
  ```
- 删除前端 `formatFileName` 函数

### 涉及文件
- `apps/data_factory/bug_analysis_view.py`
- `frontend/src/views/data-factory/BugAnalysis.vue`

---

## 2026-07-14 修复 ProjectList.vue 表格项目名称、描述字段未垂直居中对齐的问题

### 问题
`/app-automation/projects` 页面表格中，"项目名称" 和 "描述" 列的表头和内容都未垂直居中，文字显示在单元格顶部，表头内容偏左。

### 原因分析
Element Plus 的 `el-table` 单元格内容是包裹在 `<div class="cell">` 容器中。原先的样式只设置了：
- `th` 的 `text-align: center`（水平居中）
- `td` 的 `vertical-align: middle`（表格单元格垂直居中）

但 `.cell` 内部仍然是默认的 block 布局，没有显式设置垂直居中样式。当 `text-align: center` 设置到 `th` 上时，会触发 Element Plus header 内部的特殊布局，但单纯的 `vertical-align: middle` 在某些情况下会被内嵌的 `.cell` 容器覆盖或无法作用于子内容，导致文字仍显示在顶部。

此外，使用 `:deep(th)` 选择器在某些情况下优先级不足，无法覆盖 Element Plus 的默认样式，导致表头偏左。

### 修改方案
参考 `/ai-generation/projects` 页面的表头样式，使用 `:deep(.el-table__header th)` 选择器，并去掉 `.cell` 的 flex 布局：

```css
:deep(.el-table__header th) {
  background-color: #ffffff !important;
  color: #5a32a3 !important;
  font-weight: 600 !important;
  font-size: 14px;
  border-bottom: 1px solid #e9ecef;
  padding: 0 !important;
  text-align: center;
  transition: all 0.3s ease;

  &:hover {
    background-color: #ffffff !important;
  }
}

:deep(.el-table__header th .cell) {
  background-color: #ffffff !important;
  color: #5a32a3 !important;
  font-weight: 600 !important;
  white-space: nowrap !important;
  line-height: 24px !important;
  padding: 16px !important;
}

:deep(td) {
  padding: 14px 16px;
  border-bottom: 1px solid #e9ecef;
  color: #333;
  font-size: 14px;
  font-weight: 400;
  line-height: 24px;
  transition: all 0.3s ease;
  vertical-align: middle;
}
```

### 涉及文件
- `frontend/src/views/app-automation/projects/ProjectList.vue`

### 经验教训
1. **参考已验证的页面样式**——当样式问题难以解决时，参考平台中已经正常显示的页面样式是最有效的方法。
2. **使用 `.el-table__header th` 选择器**——这是 Element Plus 表格组件的标准表头选择器，优先级足够高。
3. **保持样式简洁**——不需要对 `.cell` 使用 flex 布局，Element Plus 默认的表格布局已经能正确处理居中对齐。
4. **`header-align="center"` 和 `align="center"` 是关键**——在 `el-table-column` 组件上设置这两个属性是居中对齐的基础。

---

## 2026-07-23 通知日志页面隐藏查询/重置按钮并实现自动查询

### 问题
`/app-automation/notification-logs` 页面的筛选区域包含「查询」「重置」两个按钮，用户体验不够流畅：
- 需要先选条件再点查询按钮，操作繁琐
- 「重置」按钮与 Element Plus 组件自带的 `clearable` 功能重复

### 原因分析
1. 早期为了兼容多种交互习惯（输入框按回车、清空内容自动搜索等），保留了两个按钮
2. 实际上 `el-select` 和 `el-date-picker` 已经通过 `@change` 实现了自动查询
3. 唯一缺自动触发的是 `el-input`（仅在 `clear` 和 `keyup.enter` 时触发）

### 修改方案
**文件：`frontend/src/views/app-automation/notification/NotificationLogs.vue`**

1. 删除按钮列 `<el-col :span="6">`，并将剩余三列的 `span` 调整为 `8`（保持单行布局）
2. 为 `el-input` 添加 `@input="handleSearch"`，实现边输入边查询
3. 移除不再使用的 `handleReset` 函数（避免无用代码）

```vue
<el-row :gutter="20">
  <el-col :span="8">
    <el-input v-model="searchForm.taskName" placeholder="搜索任务名称" clearable
              @clear="handleSearch" @input="handleSearch">
      <template #prefix><el-icon><Search /></el-icon></template>
    </el-input>
  </el-col>
  <el-col :span="8">
    <el-date-picker v-model="searchForm.dateRange" type="daterange" range-separator="至"
                    start-placeholder="开始日期" end-placeholder="结束日期"
                    value-format="YYYY-MM-DD" @change="handleSearch" />
  </el-col>
  <el-col :span="8">
    <el-select v-model="searchForm.status" placeholder="发送状态" clearable @change="handleSearch">
      <el-option label="全部" value="" />
      <el-option label="发送成功" value="success" />
      <el-option label="发送失败" value="failed" />
      <el-option label="待发送" value="pending" />
    </el-select>
  </el-col>
</el-row>
```

### 涉及文件
- `frontend/src/views/app-automation/notification/NotificationLogs.vue`

### 经验教训
1. **Element Plus 的 `clearable` 已自带清空图标**——「重置」按钮与 `clearable` 重复，可直接删除。
2. **`@input` 事件配合 `v-model` 可实现实时搜索**——无需额外的 debounce 也能提供流畅体验（与 `ElementList.vue` 保持一致）。
3. **按钮触发与组件 change 触发的区别**——`el-select`/`el-date-picker` 通过 `@change` 自动触发查询时务必确认事件名正确，不要写成 `@change` 之后又重复点按钮。
4. **删除功能后同步清理无用的 script 函数**——移除 `handleReset` 避免遗留死代码。

---

## 2026-07-23 通知日志筛选组件样式对齐 api-testing/automation

### 问题
`/app-automation/notification-logs` 页面的筛选组件沿用了 Element Plus 默认样式（`el-row` + `el-col` 栅格布局），与平台其他页面（`/api-testing/automation`）的紫色主题卡片式筛选条不一致：
- 默认边框颜色（浅灰）与紫色主题割裂
- 没有统一的圆角、阴影、聚焦高亮
- 不同模块之间风格不统一，影响整体观感

### 原因分析
1. Element Plus 表格的 `:deep(.el-input__wrapper)` 默认 box-shadow 不易直接覆盖
2. 早期不同模块由不同开发者实现，缺少统一的筛选条样式规范
3. `el-row`/`el-col` 栅格布局在小屏下经常出现列错位

### 修改方案
**文件：`frontend/src/views/app-automation/notification/NotificationLogs.vue`**

1. **模板层**——把 `el-row` / `el-col` 栅格改为 flex 布局：
   ```vue
   <div class="filters">
     <el-input class="filter-input" v-model="searchForm.taskName" ... />
     <el-date-picker class="filter-date" ... popper-class="purple-date-picker" />
     <el-select class="filter-select" ... popper-class="purple-select-dropdown" />
   </div>
   ```

2. **样式层**——复用 `api-testing/automation` 页面（[AutomationSuiteList.vue](file:///Users/jinshaomin/Documents/jinsm/test_hub/grt_testhub/frontend/src/views/api-testing/AutomationSuiteList.vue#L985-L1084)）的紫色主题：
   ```scss
   .filters {
     display: flex;
     align-items: center;
     gap: 16px;
     flex-wrap: wrap;

     .filter-input { width: 240px; }
     .filter-date { width: 320px; }
     .filter-select { width: 160px; }

     .filter-input, .filter-date, .filter-select {
       :deep(.el-input__wrapper) {
         box-shadow: 0 0 0 1px rgba(147, 112, 219, 0.2) inset;
         border-radius: 8px;
         background-color: transparent;

         &:hover, &.is-focus {
           box-shadow: 0 0 0 1px #7b42f6 inset;
         }
       }
     }
   }
   ```

3. **下拉菜单**——添加 `purple-select-dropdown` 和 `purple-date-picker` 全局 popper 样式：
   ```scss
   .purple-select-dropdown {
     .el-select-dropdown__item {
       &.selected { color: #7b42f6; font-weight: 600; }
       &:hover { background: rgba(123, 66, 246, 0.1); }
     }
   }
   .purple-date-picker {
     .el-date-table td.current:not(.disabled) span {
       background-color: #7b42f6; color: #ffffff;
     }
     .el-date-table td.today span { color: #7b42f6; }
   }
   ```

### 涉及文件
- `frontend/src/views/app-automation/notification/NotificationLogs.vue`

### 经验教训
1. **统一视觉风格**——平台中同类组件（筛选条、表格、按钮）应使用相同的色值、圆角和阴影规范。
2. **使用 `popper-class` 穿透到下拉面板**——`scoped` 样式无法直接作用于 `body` 末尾的 teleport 内容，必须通过 `popper-class` 配合全局样式。
3. **flex + gap 比 el-row/el-col 更灵活**——避免 24 栅格被强行均分带来的宽度限制（`span=8` 强制三个 8 等分）。
4. **`:deep(.el-input__wrapper)` 是覆盖 Element Plus 输入框样式的标准入口**——用 `box-shadow inset` 模拟边框比 `border` 更稳定，不会破坏内部 padding。
5. **`el-input` 不需要 `popper-class`**——`popper-class` 只对有下拉面板的组件（select、date-picker、cascader）有效。

---

## 2026-07-23 通知日志日期筛选器宽度调整为 280px

### 问题
通知日志筛选区中 `el-date-picker` 宽度为 320px，整体偏宽，与 `api-testing/automation` 页面（`month-range-picker` 280px）不一致。

### 原因分析
- 第一次对齐样式时只参考了颜色、圆角、阴影等主题规范，宽度直接照搬 `el-col :span="8"` 在 24 栅格下的等分宽度（≈320px）
- 没有逐项对照 `AutomationSuiteList.vue` 中每个控件的 width 值

### 修改方案
**文件：`frontend/src/views/app-automation/notification/NotificationLogs.vue`**

```scss
.filter-input { width: 240px; }   // 与 project-select 一致
.filter-date  { width: 280px; }   // 与 month-range-picker 一致（原 320px）
.filter-select { width: 160px; }  // 与 filter-select 一致
```

### 经验教训
1. **逐项对照参考文件的 width 值**——不要只复制主题色，要把每个控件的尺寸也一起对齐。
2. **`.el-col :span="8"` 强行 24 等分的宽度不可靠**——父容器宽度变化时（侧边栏折叠、屏幕缩放）所有筛选控件宽度都会随之改变，而固定像素值更稳定。

---

## 2026-07-23 通知日志表格表头排序图标隐藏

### 问题
`/app-automation/notification-logs` 页面表格部分列设置了 `sortable="custom"`，导致表头显示排序三角图标，影响页面整洁性；同时鼠标悬停表头显示手型光标，暗示可排序，但实际业务场景中不需要用户手动排序。

### 原因分析
1. Element Plus 的 `sortable="custom"` 会自动在表头右侧显示排序图标（`el-table__sort-icon` 或 `caret-wrapper`）
2. 设置了 `sortable` 属性的表头默认鼠标光标为 `pointer`，暗示可交互
3. 虽然代码中保留了 `@sort-change` 事件处理函数，但业务上不需要这个功能

### 修改方案
**文件：`frontend/src/views/app-automation/notification/NotificationLogs.vue`**

在表格样式中添加排序图标隐藏规则：
```scss
:deep(.el-table__sort-icon) {
  display: none;
}

:deep(.caret-wrapper) {
  display: none;
}
```

同时将表头鼠标光标改为默认（非手型）：
```scss
:deep(.el-table__header th) {
  cursor: default;
}
```

### 涉及文件
- `frontend/src/views/app-automation/notification/NotificationLogs.vue`

### 经验教训
1. **`:deep()` 深度选择器是必须的**——在 `<style scoped>` 中直接写 `.el-table__sort-icon` 无法作用到 Element Plus 子组件内部 DOM。
2. **覆盖多个类名确保兼容性**——不同版本的 Element Plus 排序图标可能使用 `.el-table__sort-icon` 或 `.caret-wrapper`，最好同时覆盖。
3. **`cursor: default` 配合隐藏图标**——隐藏图标后应同步修改光标样式，避免用户误解表头可交互。

---

## 2026-07-23 通知日志页面移除日期筛选组件

### 问题
`/app-automation/notification-logs` 页面筛选区包含日期范围选择器，但实际业务中用户很少通过日期筛选通知日志，该组件占用了宝贵的页面空间且增加了视觉复杂度。

### 原因分析
1. 日期筛选组件（`el-date-picker` type="daterange"）宽度较大，即使缩小到 120px 仍显得拥挤
2. 删除该组件后，筛选区只剩下任务名称输入框和发送状态下拉框，布局更简洁
3. 用户之前多次反馈日期筛选组件宽度问题，说明该功能使用率低

### 修改方案
**文件：`frontend/src/views/app-automation/notification/NotificationLogs.vue`**

1. **模板层**——删除 `<el-date-picker>` 组件（L15-L26）
2. **脚本层**——从 `searchForm` 中移除 `dateRange` 字段，从 `fetchLogs` 中移除日期参数处理逻辑
3. **样式层**——删除 `.filter-date` 样式和 `.purple-date-picker` 全局样式

### 涉及文件
- `frontend/src/views/app-automation/notification/NotificationLogs.vue`

### 经验教训
1. **删除功能要彻底**——移除 UI 组件的同时必须同步清理：数据模型字段、接口参数处理逻辑、相关样式，避免遗留死代码。
2. **全局样式（非 scoped）也要清理**——`.purple-date-picker` 不在 `<style scoped>` 内，容易被忽略。
3. **优先保留核心筛选条件**——任务名称和发送状态是通知日志最常用的筛选维度，日期筛选可通过接口默认时间范围替代。
