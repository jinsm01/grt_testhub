---

## 2026-08-10 快捷改状态交互优化：移到操作列+支持上传验证截图

### 问题
Bug列表中「已修复」状态的快捷改状态操作放在状态列，交互不够直观；改状态时无法上传验证截图到云效评论，缺乏验证凭证。

### 原因
1. 状态列的下拉菜单交互不够明显，用户难以发现可点击
2. 原有 `quickChangeStatus` 函数仅做状态更新，不支持上传附件
3. 云效客户端缺少评论API（`add_comment`）

### 修改方案
1. **状态列**：移除下拉菜单，恢复为纯静态标签展示
2. **操作列**：新增「改状态」下拉按钮（仅「已修复」状态显示），宽度从180px增至240px
3. **快捷改状态弹窗**：选择目标状态后弹出对话框，支持上传验证截图+备注文本
4. **后端新增API** `quick_change_bug_status`：
   - 更新Bug状态
   - 若有截图：上传附件到工作项 → 获取 `embedMarkdown` → 添加评论（状态变更说明+备注+嵌入图片）
   - 若无截图但有备注：直接添加文字评论
5. **YunxiaoClient** 新增 `add_comment` 方法（POST `/workitems/{id}/comments`）

### 涉及文件
- `frontend/src/views/data-factory/BugSyncManagement.vue` — 状态列改静态、操作列加下拉、快捷改状态弹窗
- `frontend/src/api/data-factory.js` — 新增 `quickChangeBugStatus` API封装
- `apps/data_factory/bug_analysis_view.py` — 新增 `quick_change_bug_status` 视图
- `apps/data_factory/yunxiao_client.py` — 新增 `add_comment` 方法
- `apps/data_factory/urls.py` — 注册新路由

---

## 2026-08-10 修复Bug管理页面顶部三个按钮颜色重复问题

### 问题
Bug管理页面（`/ai-generation/bug-analysis/sync`）顶部工具栏有三个按钮："新建Bug并同步"、"Token管理"、"一键同步当前页"，其中"新建Bug并同步"和"Token管理"两个按钮颜色完全相同（都是紫色渐变），无法区分。

### 原因
"Token管理"按钮虽然设置了 `type="warning"`，但同时使用了 `custom-btn` 类，该类用 `!important` 覆盖了背景色为紫色渐变（`#7b42f6 → #5a32a3`），与"新建Bug并同步"按钮完全相同，导致 warning 类型样式被覆盖失效。

### 修改方案
为"Token管理"按钮使用独立的 `token-btn` 类，设置橙色/琥珀色渐变（`#e6a23c → #cf8a1e`），与 Element Plus warning 主题色一致：

1. 模板中将 `class="custom-btn"` 改为 `class="token-btn"`
2. CSS 中新增 `.token-btn` 样式，包含正常态、hover/focus 态、active 态

### 涉及文件
- `frontend/src/views/data-factory/BugSyncManagement.vue`

### 最终效果
- 新建Bug并同步：紫色渐变（`#7b42f6`）
- Token管理：橙色渐变（`#e6a23c`）
- 一键同步：绿色渐变（`#00b96b`）

### 经验教训
1. **避免多个按钮共用同一自定义类**：当需要不同按钮有不同颜色时，应给每个按钮独立的样式类，而不是共用一个类再用 type 属性区分，因为自定义类的 `!important` 会覆盖 type 样式
2. **按钮颜色要有语义区分**：创建操作用品牌色（紫/蓝）、管理操作用警告色（橙）、同步操作用成功色（绿），符合用户对颜色语义的认知

---

## 2026-08-10 Token管理功能迁移至Bug管理页面

### 问题
Bug分析页面（`/ai-generation/bug-analysis/history`）的工具栏中有"反向同步"、"新建Bug"、"Token管理"三个按钮，其中"反向同步"和"新建Bug"按钮在该页面不需要，Token管理功能更适合放在Bug管理页面（`/ai-generation/bug-analysis/sync`）。

### 原因
- 迭代报告页面（history）不应包含Bug创建和反向同步功能，这些属于Bug管理页面的职责
- Token管理功能与Bug管理（同步、新建）关系更密切，应放在sync页面

### 修改方案
1. 从 `BugAnalysis.vue` 移除"反向同步"按钮和"新建Bug"按钮
2. 将 Token 管理抽屉及相关逻辑从 `BugAnalysis.vue` 抽取为独立组件 `TokenManagerDrawer.vue`
   - 使用 `v-model` 控制显示/隐藏
   - 通过 `tokens-updated` 事件通知父组件刷新 Token 下拉选项
3. 在 `BugSyncManagement.vue` 中添加"Token管理"按钮和 `TokenManagerDrawer` 组件
4. 清理 `BugAnalysis.vue` 中已无用的 Token 管理代码（模板、状态变量、函数、样式、import）

### 涉及文件
- `frontend/src/views/data-factory/TokenManagerDrawer.vue`（新建）
- `frontend/src/views/data-factory/BugSyncManagement.vue`
- `frontend/src/views/data-factory/BugAnalysis.vue`

---

## 2026-08-08 修复编辑Bug表单数据返显问题 - 最终方案

### 问题
在Bug同步管理页面点击"编辑"按钮后，Bug编辑表单中的项目字段显示的是ID而不是名称，且编辑时切换令牌后处理人和模块字段被禁用。

### 根本原因
**数据缺失**：`BugSyncItem` 模型中没有 `token_id` 字段，导致编辑时无法获取令牌信息。

**问题链**：
1. 后端创建 BugSyncItem 时，未将 `token_id` 保存到 `local_data`
2. 前端从 `row.token_id` 获取令牌（永远为 null）
3. 没有 token_id，无法加载成员和标签列表
4. 处理人和模块字段被禁用

### 最终解决方案

**1. 后端修复**：创建时保存 token_id

```python
# bug_analysis_view.py - create_bug_to_yunxiao()
bug_data = {
    "token_id": token.id if hasattr(token, 'id') else None,  # 添加 token_id
    "title": title,
    ...
}
```

**2. 前端修复**：从 local_data 获取 token_id

```javascript
// BugSyncManagement.vue - editBugSyncItem()
token_id: row.local_data?.token_id || null,  // 从 local_data 获取
```

**3. 编辑时界面**：
- ✅ **令牌字段**：禁用（显示令牌名称）
- ✅ **项目字段**：隐藏（避免显示ID）
- ✅ **迭代字段**：隐藏（简化界面）
- ✅ **其他字段**：标题、描述、状态、优先级、处理人、所属模块等可正常编辑

### 涉及文件
- `frontend/src/views/data-factory/BugSyncManagement.vue`
- `apps/data_factory/bug_analysis_view.py`

### 经验教训
1. **数据完整性**：创建记录时要保存所有必要的关联信息
2. **调试技巧**：使用 console.log 检查数据流，快速定位问题
3. **前后端配合**：前端显示问题可能是后端数据缺失导致
4. **数据结构理解**：了解模型字段和数据存储位置（如 JSONField）
5. **禁用优于切换**：编辑时禁用关键字段（令牌），避免数据不一致

---

## 2026-08-08 修复编辑Bug表单数据无法正确返显问题

### 问题
在Bug同步管理页面点击"编辑"按钮后，Bug编辑表单中的前三个字段（访问令牌、项目、迭代）以及其他字段（处理人、所属模块等）数据没有正确返显，导致用户需要重新选择这些字段。

### 原因分析

问题根源有两个：

**问题1：watch 监听 token_id 会清空表单数据**

在 `editBugSyncItem` 函数中设置了完整的表单数据（包括 token_id、space_id、sprint_id、assignee、module 等），但同时有一个 watch 监听 `token_id` 的变化，当 token_id 变化时会清空以下字段：
- `bugFormProjects.value = []`
- `bugFormSprints.value = []`
- `bugFormMembers.value = []`
- `bugFormLabels.value = []`
- `bugForm.value.space_id = ''`
- `bugForm.value.sprint_id = ''`
- `bugForm.value.assignee = ''`
- `bugForm.value.module = ''`

这个 watch 在创建模式下是合理的（切换 Token 时需要清空项目等相关字段），但在编辑模式下会导致刚设置的数据被立即清空。

**问题2：onBugFormProjectChange 会清空表单值**

编辑时需要调用 `onBugFormProjectChange` 加载迭代、成员、标签列表，但这个函数会清空：
- `bugForm.value.sprint_id = ''`
- `bugForm.value.assignee = ''`
- `bugForm.value.module = ''`

导致刚设置的值被清空。

**问题3：项目下拉框没有选项列表**

编辑时需要加载项目列表（bugFormProjects），否则项目下拉框即使有 space_id 值也无法正确显示项目名称。

### 修改方案

**1. 修改 watch 逻辑：编辑模式下不自动清空数据**

在 watch 回调函数中判断：当处于编辑模式时，直接跳过清空逻辑。

**修改文件**：`frontend/src/views/data-factory/BugSyncManagement.vue`

```javascript
// 修改前
watch(() => bugForm.value.token_id, async (newTokenId) => {
  bugFormProjects.value = []
  // ... 清空逻辑
})

// 修改后
watch(() => bugForm.value.token_id, async (newTokenId) => {
  // 编辑模式下不自动清空数据（editBugSyncItem 会主动加载选项）
  if (bugDialogMode.value === 'edit') {
    if (newTokenId && bugForm.value.space_id) {
      // 项目字段已隐藏，保留原项目ID，重新加载处理人和模块选项
      onBugFormProjectChange(bugForm.value.space_id, true)
    }
    return
  }
  bugFormProjects.value = []
  // ... 清空逻辑
})
```

**2. 修改 onBugFormProjectChange 函数：添加 preserveFormValues 参数**

添加 `preserveFormValues` 参数，当为 true 时保留表单值不清空。

```javascript
// 修改前
async function onBugFormProjectChange(spaceId) {
  // ...
  bugForm.value.sprint_id = ''
  bugForm.value.assignee = ''
  bugForm.value.module = ''
  // ...
}

// 修改后
async function onBugFormProjectChange(spaceId, preserveFormValues = false) {
  // ...
  // 仅在非编辑模式下清空表单值
  if (!preserveFormValues) {
    bugForm.value.sprint_id = ''
    bugForm.value.assignee = ''
    bugForm.value.module = ''
  }
  // ...
}
```

**3. 修改 editBugSyncItem 函数：正确加载选项列表**

1. 先加载项目列表（确保项目下拉框有选项）
2. 再加载迭代/成员/标签列表（传递 `preserveFormValues=true`）

```javascript
// 修改前
function editBugSyncItem(row) {
  bugDialogMode.value = 'edit'
  bugForm.value = { ... }
  showBugDialog.value = true
  if (bugForm.value.token_id && bugForm.value.space_id) {
    onBugFormProjectChange(bugForm.value.space_id)  // 会清空表单值！
  } else if (bugForm.value.token_id) {
    loadBugFormProjects()
  }
}

// 修改后
function editBugSyncItem(row) {
  bugDialogMode.value = 'edit'
  bugForm.value = { ... }
  showBugDialog.value = true
  if (bugForm.value.token_id) {
    // 先加载项目列表
    loadBugFormProjects().then(() => {
      // 项目列表加载完成后，加载迭代/成员/标签列表（保留表单值）
      if (bugForm.value.space_id) {
        onBugFormProjectChange(bugForm.value.space_id, true)  // 传递 true 保留表单值
      }
    })
  }
}
```

### 涉及文件
- `frontend/src/views/data-factory/BugSyncManagement.vue`：
  - 修改 watch 逻辑，编辑模式下不清空数据
  - 修改 onBugFormProjectChange 函数，添加 preserveFormValues 参数
  - 修改 editBugSyncItem 函数，正确加载选项列表

### 经验教训
1. **watch 副作用要考虑所有场景**：当 watch 用于清空关联字段时，要考虑编辑场景下数据是主动设置的，不应该被清空
2. **函数参数设计要有扩展性**：`onBugFormProjectChange` 应该支持"仅加载选项，不清空表单值"的场景
3. **下拉框返显需要选项列表**：编辑时不仅要设置字段值，还要加载对应的选项列表，否则下拉框无法正确显示
4. **数据加载顺序很重要**：先加载项目列表，再加载迭代列表，确保下拉框有选项
5. **函数职责要清晰**：加载选项列表的函数不应该有清空表单值的副作用，或至少应该可控

---

## 2026-08-08 工具栏布局调整 - 筛选组件与创建按钮互换位置

### 问题
Bug同步管理页面的工具栏布局不符合常规使用习惯：创建按钮在左侧，筛选组件在右侧。通常筛选功能使用频率更高，应该放在更显眼的左侧位置。

### 修改方案

将工具栏左右两侧内容互换：
- **修改前**：左侧 = 创建按钮 + 刷新按钮；右侧 = 筛选下拉框
- **修改后**：左侧 = 筛选下拉框；右侧 = 创建按钮 + 刷新按钮

**修改文件**：`frontend/src/views/data-factory/BugSyncManagement.vue`

```vue
<!-- 修改前 -->
<div class="toolbar-left">
  <el-button type="primary" @click="openCreateBugDialog">...</el-button>
  <el-button @click="loadBugSyncItems">...</el-button>
</div>
<div class="toolbar-right">
  <el-select v-model="filterSyncStatus">...</el-select>
  <el-select v-model="filterSeverity">...</el-select>
</div>

<!-- 修改后 -->
<div class="toolbar-left">
  <el-select v-model="filterSyncStatus">...</el-select>
  <el-select v-model="filterSeverity">...</el-select>
</div>
<div class="toolbar-right">
  <el-button type="primary" @click="openCreateBugDialog">...</el-button>
  <el-button @click="loadBugSyncItems">...</el-button>
</div>
```

### 涉及文件
- `frontend/src/views/data-factory/BugSyncManagement.vue`：互换工具栏左右两侧内容

### 经验教训
1. **高频操作优先原则**：筛选是高频操作，应放在更显眼的位置（左侧）
2. **F型浏览习惯**：用户浏览页面时习惯从左到右、从上到下，左侧内容更容易被注意到
3. **功能分组要合理**：同类功能应放在一起，创建和刷新都是操作类按钮，放在右侧更合适

---

## 2026-08-08 修复状态字段传中文名称导致"currentStatus can not transit to status not existed"400错误

### 问题
编辑Bug并保存修改时，云效返回`HTTP 400: {"errorMessage":"currentStatus can not transit to status not existed, ..., toStatus: 待确认"}`。

### 根本原因
和标签字段问题完全相同：云效API的`status`字段要求传递**状态ID（identifier）**，不接受中文状态名称。
- 之前代码中虽然有`YUNXIAO_STATUS_MAP`硬编码映射，但只是将中文名称映射为同名中文名称（如`'待确认': '待确认'`），没有转为ID
- 创建Bug时云效使用默认初始状态，不涉及状态流转所以没暴露问题
- 更新Bug时涉及状态流转，云效严格校验状态ID，传中文名称直接报400
- 不同项目的状态列表/状态ID不同，硬编码状态名称→ID映射不可行，必须动态从API获取

### 修改方案

**1. 后端新增状态ID解析方法（yunxiao_client.py）**

```python
def _resolve_status_id(self, space_id, status):
    """
    将状态值（ID或名称）解析为状态ID：
    - 24位hex → 直接作为ID
    - 名称字符串 → 调用list_workitem_statuses查状态列表找对应ID
    - 支持中文名称和英文名称(nameEn)匹配
    """
```

**2. update_workitem中自动解析status**
```python
if status is not None:
    if space_id and status:
        payload["status"] = self._resolve_status_id(space_id, status)
    else:
        payload["status"] = status
```

**3. update_bug_status增加space_id参数**
```python
def update_bug_status(self, workitem_id, status, space_id=None):
    return self.update_workitem(workitem_id=workitem_id, status=status, space_id=space_id)
```

**4. 移除bug_analysis_view.py中硬编码的YUNXIAO_STATUS_MAP**

不再需要维护状态名称映射表，状态值直接传给yunxiao_client，由`_resolve_status_id`自动从项目状态列表中查找对应ID。

### 涉及文件
- `apps/data_factory/yunxiao_client.py`：
  - 新增 `_resolve_status_id()` 方法解析状态名称到ID
  - `update_workitem()` 中status字段在有space_id时自动解析为ID
  - `update_bug_status()` 新增 `space_id` 参数
- `apps/data_factory/bug_analysis_view.py`：
  - `update_bug_to_yunxiao()` 移除硬编码`YUNXIAO_STATUS_MAP`，直接传递原始状态值（已通过space_id解析）

### 经验教训
1. **云效API大量字段使用identifier而非displayValue**：status、labels、严重程度、优先级、工作项类型、处理人、验证者...全部使用24位hex ID，中文名称只用于展示。在客户端层必须统一封装名称→ID的解析。
2. **不同项目/空间的枚举值不同**：状态、严重程度、标签等在每个云效项目中可以自定义，不能硬编码映射表，必须通过API动态查询当前空间的枚举列表。
3. **创建和更新API行为差异**：创建时不校验/使用默认值的字段，更新时可能严格校验。测试时不能只测创建，也要测编辑更新。
4. **通用封装原则**：所有涉及枚举/引用类型的字段（状态、标签、人员、严重程度、优先级、工作项类型），都应该在yunxiao_client层面做名称→ID的自动解析，调用方只传业务名称即可，不用关心ID格式。

---

## 2026-08-08 修复标签字段传名称导致"标签未找到"400错误

### 问题
修复Bug同步标签字段时，将前端下拉框`:value="label.id || label.name"`错误改为`:value="label.name"`，导致编辑Bug保存时传递标签名称字符串给云效API，云效返回`HTTP 400: {"errorMessage":"标签未找到"}`。

### 根本原因
云效API的`labels`字段要求传递**标签ID数组**（24位hex格式），不接受标签名称字符串。
- 创建Bug时云效API对无效标签名称可能静默忽略（不报错但标签不显示）
- 更新Bug时云效API严格校验标签存在性，传名称直接返回400错误
- 前端`el-select`的`allow-create`模式下，选中已有标签应传ID，新输入标签传名称
- 后端没有对labels做ID解析/创建标签的处理，直接透传给云效

### 修改方案

**1. 前端恢复标签ID传递逻辑**
```vue
<!-- 恢复为：已有标签传ID，新标签传名称 -->
<el-option :value="label.id || label.name" />
```

**2. 后端新增标签解析方法（yunxiao_client.py）**

```python
def create_label(self, space_id, name, color="#1890ff"):
    """创建新标签，返回标签信息"""

def _resolve_label_ids(self, space_id, labels):
    """
    将标签值（ID或名称混合）解析为纯标签ID数组：
    - 24位hex → 直接作为ID
    - 名称字符串 → 查标签列表找对应ID
    - 不存在的名称 → 自动创建标签获取ID
    - 无法解析的 → 跳过并记录警告
    """
```

**3. 创建/更新工作项时自动解析labels**
```python
# create_workitem
if labels:
    payload["labels"] = self._resolve_label_ids(space_id, labels)
else:
    payload["labels"] = []

# update_workitem 新增 space_id 参数
def update_workitem(self, workitem_id, ..., labels=None, space_id=None, ...):
    if labels is not None:
        if space_id and labels:
            payload["labels"] = self._resolve_label_ids(space_id, labels)
        else:
            payload["labels"] = labels if labels else []
```

**4. update_bug_to_yunxiao 传入space_id**
```python
local_data = sync_item.local_data or {}
space_id = local_data.get('space_id', '')
# ...
payload = client.update_workitem(..., labels=labels_param, space_id=space_id if space_id else None, ...)
```

### 涉及文件
- `apps/data_factory/yunxiao_client.py`：
  - 新增 `create_label()` 方法创建标签
  - 新增 `_resolve_label_ids()` 方法解析标签为ID
  - `create_workitem()` 中发送labels前调用 `_resolve_label_ids`
  - `update_workitem()` 新增 `space_id` 参数，发送labels前解析
- `apps/data_factory/bug_analysis_view.py`：
  - `update_bug_to_yunxiao()` 提前获取 `space_id`，调用 `update_workitem` 时传入
- `frontend/src/views/data-factory/BugSyncManagement.vue`：
  - 恢复所属模块下拉框 `:value="label.id || label.name"`

### 经验教训
1. **云效API字段格式必须严格匹配**：`labels`字段只接受标签ID，不接受名称。类似地，用户字段接受userId，严重程度/优先级接受自定义字段identifier。传错类型会导致静默失败或400错误。
2. **创建和更新API行为可能不一致**：创建API可能对无效参数静默忽略，更新API则严格校验。不能因为创建时不报错就认为参数格式正确。
3. **el-select的allow-create与value选择**：使用`allow-create`时，选中已有选项应传稳定的ID，新创建的值传字符串名称，后端需要能处理混合情况。
4. **跨系统字段映射要做双向转换**：从云效读取时标签名称→ID需要映射，推送到云效时名称也要转成ID，必须在客户端层封装统一的解析方法，不能让每个调用方自己处理。

---

## 2026-08-08 修复云效编号serialNumber提取问题并新增重新同步功能

---

## 2026-08-08 修复Bug同步中严重程度和标签字段不正确同步问题

### 问题
在Bug同步管理页面对比本地与云效数据时发现两个问题：
1. **严重程度显示不一致**：云效显示"3--一般"（数字编号格式），本地显示"P2-一般"（P等级格式），从云效反向同步后会变成"3--一般"导致前端样式和下拉框回显异常
2. **标签（所属模块）未同步到云效**：本地填写的"所属模块"（对应云效标签字段）没有正确同步到云效，云效页面标签字段显示为空

### 根本原因

**问题1：严重程度格式不统一**
- 云效API返回的严重程度显示值格式多样："3-一般"、"3--一般"（双横杠）、纯数字"3"、identifier（24位hex）等
- 本地系统统一使用P0-P4格式（P0-致命、P1-严重、P2-一般、P3-轻微、P4-建议）
- 反向同步（resync、批量从云效拉取）时直接使用了云效原始显示值，没有转换为本地P等级格式
- `SEVERITY_VALUE_MAP`中缺少"3--一般"等变体映射，也缺少反向映射能力

**问题2：标签同步失败**
- 前端"所属模块"下拉框`:value="label.id || label.name"`，选中已有标签时传的是标签ID（24位hex），但云效API的`labels`字段接受**标签名称字符串**，传ID导致云效无法识别，标签被忽略
- `update_workitem`方法没有暴露`labels`参数，编辑Bug并保存时标签不会同步更新到云效
- 反向同步（resync）时没有提取labels信息更新本地module字段

### 修改方案

**1. 后端：新增严重程度标准化函数（yunxiao_client.py）**

```python
# 新增反向映射表
SEVERITY_TO_P_MAP = {
    "1": "P0", "1-致命": "P0", "1--致命": "P0",
    "2": "P1", "2-严重": "P1", "2--严重": "P1",
    "3": "P2", "3-一般": "P2", "3--一般": "P2",
    "4": "P3", "4-轻微": "P3", "4--轻微": "P3",
    "ea83daaf913f8a287abe48b80d": "P1",
    "1e6094d49e58006000b29bab40": "P2",
    "c3a209a534e6ea640ba71ee004": "P3",
    # ... 更多映射
}

@staticmethod
def normalize_severity_from_yunxiao(severity: str) -> str:
    """将云效返回的各种严重程度格式统一转为P0-P4"""
    # 支持: "3-一般", "3--一般", "3", identifier, "一般", "P2", "P2-一般" 等
```

**2. 后端：convert_yunxiao_bugs 中标准化severity**

```python
"severity": YunxiaoClient.normalize_severity_from_yunxiao(
    cf_map.get("严重程度") or cf_map.get("seriousLevel") or item.get("severity") or ""
),
"labels": label_names,  # 同时保存labels列表
```

**3. 后端：resync_bug_item 中标准化severity并提取labels**

```python
# 标准化严重程度
remote_severity = YC.normalize_severity_from_yunxiao(remote_severity_raw) if remote_severity_raw else ""

# 提取云效标签
remote_labels = _extract_labels(workitem_detail)
remote_module = _pick_module_name_from_labels(remote_labels) if remote_labels else ""

# 更新到local_data
if remote_severity and remote_severity != local_data.get("severity"):
    local_data["severity"] = remote_severity
if remote_module and remote_module != local_data.get("module"):
    local_data["module"] = remote_module
```

**4. 后端：update_workitem 支持labels参数，update_bug_to_yunxiao传递labels**

```python
# yunxiao_client.py - update_workitem 新增 labels 参数
def update_workitem(self, workitem_id, ..., labels=None, ...):
    if labels is not None:
        payload["labels"] = labels if labels else []

# bug_analysis_view.py - update_bug_to_yunxiao 传递labels
module_val = update_data.get('module', '').strip()
labels_param = [module_val] if module_val else None
payload = client.update_workitem(..., labels=labels_param, ...)
```

**5. 前端：统一使用标签名称作为value**

```vue
<!-- 修改前 -->
<el-option :value="label.id || label.name" />

<!-- 修改后 -->
<el-option :value="label.name" />
```

同时在`YUNXIAO_STATUS_MAP`中补充了"待确认"状态映射（与截图中云效状态一致）。

### 涉及文件
- `apps/data_factory/yunxiao_client.py`：
  - 扩展`SEVERITY_VALUE_MAP`，增加双横杠、纯中文等变体
  - 新增`SEVERITY_TO_P_MAP`反向映射表
  - 新增`normalize_severity_from_yunxiao()`静态方法
  - 更新`_resolve_severity_value()`先标准化再映射
  - `update_workitem()`新增`labels`参数
  - `convert_yunxiao_bugs()`中标准化severity并保存labels字段
- `apps/data_factory/bug_analysis_view.py`：
  - `resync_bug_item()`中使用`normalize_severity_from_yunxiao`标准化严重程度
  - `resync_bug_item()`中新增labels提取和module更新逻辑
  - `update_bug_to_yunxiao()`中传递labels参数到云效
  - `YUNXIAO_STATUS_MAP`补充"待确认"状态
- `frontend/src/views/data-factory/BugSyncManagement.vue`：
  - 所属模块下拉框value从`label.id || label.name`改为`label.name`，确保传给后端的是标签名称

### 经验教训
1. **跨系统字段映射需要双向考虑**：不仅要处理本地→云效的格式转换（推送到云效），也要处理云效→本地的反向转换（从云效拉取），必须建立统一的标准化函数
2. **云效API字段类型要明确**：`labels`字段接受名称字符串数组，不是ID数组。前端选择器的value类型必须与API要求一致
3. **更新接口要与创建接口保持同等能力**：create_bug支持labels但update_workitem不支持，会导致编辑后字段不同步
4. **反向同步要覆盖所有字段**：resync功能最初只同步了状态、处理人等关键字段，遗漏了严重程度标准化和标签同步，需要定期审视同步字段的完整性
5. **显示值vs标识符**：云效自定义字段（如严重程度）同时有displayValue（如"3--一般"）和identifier（24位hex），本地应维护一套自己的标准格式（P0-P4），在输入输出两端做转换

---

## 2026-08-09 修复所属模块显示标签ID而非中文名称问题

### 问题
Bug列表中的"所属模块"列显示的是24位hex标签ID（如`b88cd6c0f7236ac48c0409786a`），而不是中文标签名称。编辑Bug时如果选中已有标签，保存后module字段也变成ID，导致列表显示异常。

### 根本原因
前端下拉框`:value="label.id || label.name"`在选中已有标签时传递的是标签ID（24位hex字符串），后端接收到后直接存入`local_data.module`字段，列表展示时直接输出ID字符串。

同时存在历史数据问题：之前错误代码创建了name为奇怪字符串（如`5a1fa459edf636a0027125250e`）的标签，云效返回的labels中name字段本身就是错误值，需要通过标签列表API用ID反查正确名称。

### 解决方案

**1. 前端：统一使用标签名称作为value**

```vue
<!-- BugSyncManagement.vue 所属模块下拉框 -->
<el-option :value="label.name" />  <!-- 传名称，确保后端存中文名 -->
```

**2. 后端：新增标签ID→名称反向查询方法**

```python
# yunxiao_client.py 新增 _resolve_label_name()
def _resolve_label_name(self, space_id: str, label_val: str) -> str:
    """将标签值（ID或名称）解析为标签名称（用于本地存储/显示）
    - 如果是24位hex（ID），查标签列表找对应名称
    - 如果已经是名称字符串，直接返回
    """
```

**3. 后端：创建/更新Bug时确保module存储名称**

```python
# create_bug_to_yunxiao 和 update_bug_to_yunxiao 中
if module and space_id:
    module_name = client._resolve_label_name(space_id, module)
local_data['module'] = module_name  # 存名称而非ID
```

**4. 后端：resync时通过标签列表API用ID反查正确名称**

```python
# resync_bug_item 中提取labels时同时收集ID和name
# 调用 client.list_project_labels() 构建ID→名称映射
# 用workitem标签的ID匹配标签列表中的正确名称
```

**5. 后端：改进_pick_module_name_from_labels优先选中文标签**

```python
# 使用正则 _CJK_RE = re.compile(r'[\u4e00-\u9fff]')
# 优先选择包含中文字符的标签，排除24位hex ID和乱码
chinese_labels = [n for n in label_names if n and _CJK_RE.search(n)]
```

### 涉及文件
- `frontend/src/views/data-factory/BugSyncManagement.vue`：所属模块下拉框`:value="label.name"`
- `apps/data_factory/yunxiao_client.py`：
  - 新增`_resolve_label_name()`方法
  - 改进`_pick_module_name_from_labels()`优先选中文标签
- `apps/data_factory/bug_analysis_view.py`：
  - `create_bug_to_yunxiao()`存储module时反查名称
  - `update_bug_to_yunxiao()`存储module时反查名称
  - `resync_bug_item()`提取labels时同时收集ID，通过标签列表API反查正确名称

### 经验教训
1. **下拉框value类型必须与存储需求一致**：列表展示需要中文名，就传中文名；API调用需要ID时，在后端做转换。不要让前端选择器的value类型影响本地存储的可读性
2. **双向映射必备**：有名称→ID的解析（给云效API传ID），就必须有ID→名称的反查（本地存储显示中文名）
3. **历史数据修正**：修复当前逻辑的同时，必须考虑已存在的脏数据如何通过resync自动修正
4. **中文字符检测**：在标签/名称类字段处理中，加入中文字符正则检测可以有效过滤ID、乱码等非预期值，优先选择用户可读的内容

---

## 2026-08-10 修复编辑Bug时状态传递中文名称报错"currentStatus can not transit to status not existed"问题

### 问题
编辑Bug保存时，云效API返回HTTP 400错误：
```json
{"errorCode":"400","errorMessage":"currentStatus can not transit to status not existed, workitem: d963fadf17ea71347619304f64, toStatus: 待确认"}
```
状态字段"待确认"是中文名称，云效update API要求传递状态ID（短数字如"28"），而不是中文名称。

### 根本原因
原来的`list_workitem_statuses()`方法调用云效API `/spaces/{spaceId}/statuses`，该接口返回**404 Not Found**，云效OpenAPI并未公开获取状态列表的接口。导致`_resolve_status_id()`无法将中文状态名称解析为对应的ID，直接返回了中文名称给云效API。

### 解决方案
采用**工作项数据自动收集+进程内缓存**方案：

**1. 废弃有问题的list_workitem_statuses API调用**

```python
# 原API路径返回404，不可用
# GET /oapi/v1/projex/organizations/{orgId}/spaces/{spaceId}/statuses -> 404
```

**2. 新增状态缓存机制**

```python
# YunxiaoClient.__init__ 中添加
self._status_cache: Dict[str, Dict[str, str]] = {}  # {space_id: {status_name: status_id}}
```

**3. 新增_collect_statuses_from_workitems方法**
从工作项列表的status字段中自动收集 name->id 和 nameEn->id 映射，更新缓存。

**4. 在返回工作项数据的方法中自动收集状态**
- `get_workitem()`: 返回单个工作项后调用收集
- `search_workitems()`: 返回工作项列表后批量收集
- `resync_bug_item()`和批量检查逻辑中：提取space_id后手动调用收集

**5. 重写_resolve_status_id解析逻辑**
1. 如果传入值非中文（已是ID），直接返回
2. 查缓存，命中直接返回ID
3. 缓存未命中时，主动调用search_workitems拉取一页50条Bug，自动收集状态填充缓存
4. 再次查找缓存，找到返回ID
5. 最终找不到返回原值并打警告日志

### 缓存收集效果
调用一次解析后，自动收集到该项目所有状态映射：
```python
{
    '待确认': '28', 'New': '28',
    '已修复': '29', 'Fixed': '29',
    '已关闭': '100085', 'Closed': '100085',
    '已验证': '27357dc74199ab5de13c4fa192',
    ...
}
```

### 涉及文件
- `apps/data_factory/yunxiao_client.py`：
  - 添加`_status_cache`缓存字典
  - 添加`_collect_statuses_from_workitems()`自动收集状态方法
  - 修改`get_workitem()`新增space_id参数，返回时自动收集状态
  - 修改`search_workitems()`返回结果时自动收集状态
  - 重写`_resolve_status_id()`使用缓存+主动搜索策略解析状态ID
  - `list_workitem_statuses()`标记为废弃，返回空列表
- `apps/data_factory/bug_analysis_view.py`：
  - `create_bug_to_yunxiao()`调用get_workitem时传入space_id
  - `resync_bug_item()`提取space_id后手动调用`_collect_statuses_from_workitems()`
  - 批量检查状态变更的循环中同样手动收集状态

### 经验教训
1. **云效OpenAPI并非所有接口都公开**：遇到404时不要假设是路径拼写错误，可能该接口根本未开放。需要寻找替代方案
2. **从现有数据中提取元数据是有效方案**：当获取元数据的API不可用时，从业务数据（工作项列表/详情）中反向提取字段映射是可行的替代方案
3. **缓存+懒加载策略适合低频调用场景**：状态解析只在更新Bug时触发，第一次搜索50条Bug收集状态后，后续所有调用都走内存缓存，性能开销可以接受
4. **自动收集优于硬编码映射**：不同项目/组织的状态配置可能不同，自动从实际数据收集比硬编码状态映射表更通用、更易维护

---

## 2026-08-10 优化严重程度字段显示与云效平台完全一致

### 问题
当前Bug创建和列表中，严重程度字段显示的是本地自定义的P0/P1/P2/P3格式（如"P2-一般"），与云效平台显示的"1-致命"/"2-严重"/"3-一般"/"4-轻微"格式不一致。

### 根本原因
之前的设计将云效返回的严重程度`displayValue`（如"3-一般"）通过`normalize_severity_from_yunxiao`转换成了本地P等级格式（如"P2"）用于存储和显示，导致与云效平台显示不一致。

### 解决方案
将严重程度显示格式改为与云效平台完全一致：

**1. 前端修改**：
- 筛选器选项改为云效格式："1-致命"/"2-严重"/"3-一般"/"4-轻微"
- 创建/编辑表单选项改为云效格式
- 默认值从"P2"改为"3-一般"
- 新增`getSeverityClass()`函数，兼容新旧格式显示颜色样式
- CSS类名从`.severity-p0/p1/p2/p3`改为`.severity-s1/s2/s3/s4`对应云效1-4级

**2. 后端修改**：
- 新增`SEVERITY_TO_DISPLAY_MAP`映射表，将各种格式（identifier/P等级/纯中文/双横杠等）统一转换为云效标准显示格式
- 修改`normalize_severity_from_yunxiao()`返回云效标准显示格式（"1-致命"/"2-严重"/"3-一般"/"4-轻微"），不再返回P等级
- 保留`SEVERITY_VALUE_MAP`用于将显示值转换为云效API所需的identifier

**3. 兼容处理**：
- `getSeverityClass()`同时支持旧P等级格式（P0/P1/P2/P3）和新云效格式，确保历史数据显示正常
- `SEVERITY_TO_DISPLAY_MAP`支持旧P等级格式自动转换为云效格式，重新同步后历史数据会自动更新

### 云效严重程度实际选项
通过API搜索300条Bug收集到该项目的严重程度选项：
- "2-严重" -> identifier: "ea83daaf913f8a287abe48b80d"
- "3-一般" -> identifier: "1e6094d49e58006000b29bab40"
- "4-轻微" -> identifier: "c3a209a534e6ea640ba71ee004"
- "1-致命"（该项目暂无数据，预留支持）

### 涉及文件
- `frontend/src/views/data-factory/BugSyncManagement.vue`：
  - 筛选器和表单选项改为云效格式
  - 默认值改为"3-一般"
  - 新增`getSeverityClass()`样式映射函数
  - CSS样式适配新类名
- `apps/data_factory/yunxiao_client.py`：
  - 新增`SEVERITY_TO_DISPLAY_MAP`映射表
  - 修改`normalize_severity_from_yunxiao()`输出云效标准格式

### 经验教训
1. **字段显示应与第三方平台保持一致**：对接第三方平台（如云效）时，显示格式应与平台原生格式保持一致，减少用户认知负担
2. **显示值与API提交值分离**：本地存储用于显示的值应是用户可读的原生格式（如"3-一般"），推送到API时再转换为所需的identifier格式
3. **新旧格式兼容很重要**：修改字段格式时必须考虑历史数据兼容，通过映射函数同时支持新旧格式显示，重新同步后自动更新
4. **从实际数据验证选项**：不要假设选项列表，应通过实际API数据收集确认目标平台真实使用的选项

---

## 2026-08-10 新建Bug默认状态与云效一致改为"待确认"

### 问题
本地平台新建Bug默认状态是"新建"，而云效平台新建缺陷默认状态是"待确认(New)"，两边不一致。同时状态选项也不完整，缺少云效实际的"已修复""已验证""不予解决""暂不修复""再次打开"等状态。

### 根本原因
前端硬编码了状态选项，使用的是通用issue管理的典型状态（新建/进行中/已解决/已关闭/重新打开），而非云效缺陷类型实际的状态流转。

### 云效缺陷实际状态列表
通过搜索500条缺陷数据收集到完整状态：
| 状态名称 | 英文名 | ID |
|---------|--------|----|
| 待确认 | New | "28" |
| 处理中 | In Progress | "100010" |
| 已修复 | Fixed | "29" |
| 已验证 | - | "27357dc74199ab5de13c4fa192" |
| 再次打开 | Reopen | "30" |
| 已关闭 | Closed | "100085" |
| 不予解决 | - | "25378289e6c5afff19ab9539c2" |
| 暂不修复 | Won't Fix | "31" |

### 解决方案
**前端修改** [BugSyncManagement.vue](file:///Users/jinshaomin/Documents/jinsm/test_hub/grt_testhub/frontend/src/views/data-factory/BugSyncManagement.vue)：
1. 默认状态从"新建"改为"待确认"（与云效新建默认一致）
2. 创建/编辑表单状态选项替换为云效实际的8个状态
3. 编辑时默认回填值改为"待确认"

**后端说明**：
- 创建Bug时不传递status参数，云效自动使用默认初始状态"待确认"，与前端默认选中值一致
- 更新Bug时`_resolve_status_id`已支持自动收集和解析所有状态名称到ID的映射，新状态可以正常流转

### 经验教训
1. **初始状态不要硬编码**：对接第三方平台创建工作项时，初始状态应与平台默认一致，避免创建后状态不对
2. **从实际数据收集状态选项**：不同工作项类型（缺陷/需求/任务）的状态流转可能不同，应通过实际API数据收集确认，而不是使用通用模板

---

## 2026-08-10 修复选择"1-致命"严重程度创建Bug报错"字段【严重程度】所填值无效"

### 问题
创建Bug时选择严重程度为"1-致命"，云效API返回错误："字段【严重程度】所填值无效"（HTTP 400）。

### 根本原因
`SEVERITY_VALUE_MAP`中"1-致命"对应的identifier是空字符串，导致`_resolve_severity_value()`无法正确转换，直接返回了显示值"1-致命"字符串给云效API，而云效API要求传递24位hex标识符。

之前只通过部分Bug数据收集严重程度选项，项目中使用"1-致命"的Bug较少，未能在初始收集时获取到对应的identifier。

### 解决方案
1. 通过搜索700条历史Bug，收集到完整的4个严重程度选项及identifier：
   - "1-致命" -> "bb82d190a4a5a866eaea26c878"
   - "2-严重" -> "ea83daaf913f8a287abe48b80d"
   - "3-一般" -> "1e6094d49e58006000b29bab40"
   - "4-轻微" -> "c3a209a534e6ea640ba71ee004"

2. 修改 [yunxiao_client.py](file:///Users/jinshaomin/Documents/jinsm/test_hub/grt_testhub/apps/data_factory/yunxiao_client.py)：
   - 补全`SEVERITY_VALUE_MAP`中"1-致命"、"1--致命"、"P0"、"致命"对应的identifier
   - 补全`SEVERITY_TO_DISPLAY_MAP`中identifier "bb82d190a4a5a866eaea26c878" 到"1-致命"的映射

### 经验教训
1. **字段选项映射必须完整**：对于下拉选项字段，所有选项都必须配置正确的identifier，不能留空，否则创建时选择该选项会报错
2. **大数据量收集才能覆盖全部选项**：从工作项数据收集字段选项时，需要搜索足够多的数据量（如500+条），才能覆盖使用频率较低的选项
3. **双向映射都要完整**：不仅要配置显示值到identifier的映射（提交API用），也要配置identifier到显示值的映射（拉取数据显示用），两边都要补全

---

## 2026-08-10 补全优先级"紧急"选项identifier映射

### 问题
优先级字段"紧急"选项在`PRIORITY_VALUE_MAP`中identifier为空字符串，选择"紧急"创建Bug会导致云效API报错"字段【优先级】所填值无效"。

### 根本原因
初始配置`PRIORITY_VALUE_MAP`时，"紧急"对应的identifier留空，未从实际云效数据中收集该选项。

### 云效优先级实际选项
通过搜索700条历史Bug收集到完整优先级选项：
| 显示值 | identifier |
|--------|------------|
| 紧急 | bbcfd59822418c4db9a0aee427 |
| 高 | 6cf54369e61c70354317af5331 |
| 中 | 4960b27f8aca5963455f767ff1 |
| 低 | 883c53c3d49b90bea1adccf684 |

### 解决方案
修改 [yunxiao_client.py](file:///Users/jinshaomin/Documents/jinsm/test_hub/grt_testhub/apps/data_factory/yunxiao_client.py) 中`PRIORITY_VALUE_MAP`：
- 补全"紧急"对应的identifier为`bbcfd59822418c4db9a0aee427`
- 调整选项顺序为：紧急、高、中、低（与云效平台排序一致）

**说明**：优先级显示值本身就是中文（紧急/高/中/低），与云效显示一致，拉取数据时直接取`displayValue`即可，不需要像严重程度那样做复杂的格式转换和双向映射。

---

## 2026-08-10 实现Bug附件上传和描述区粘贴图片功能

### 功能需求
1. Bug提交支持附件字段，可以上传视频、文档等文件
2. Bug描述区域支持粘贴图片，粘贴后图片自动作为附件上传

### 云效API调研
云效新版OpenAPI（中心版）直接支持附件上传，接口为：
- `POST /oapi/v1/projex/organizations/{organizationId}/workitems/{id}/attachments`
- Content-Type: multipart/form-data
- 参数：file（文件二进制）
- 返回：包含id、name、size、url（临时下载地址）、embedUrl（永久嵌入地址）、embedHtml（img标签）、embedMarkdown（markdown图片）

注意：上传附件**必须先创建工作项拿到workitem_id**，然后才能上传。

### 实现方案

**后端** [yunxiao_client.py](file:///Users/jinshaomin/Documents/jinsm/test_hub/grt_testhub/apps/data_factory/yunxiao_client.py)：
1. 新增`upload_attachment(workitem_id, file_content, filename, content_type)`方法，使用multipart/form-data POST文件到云效
2. 新增`list_workitem_attachments(workitem_id)`获取附件列表

**后端** [bug_analysis_view.py](file:///Users/jinshaomin/Documents/jinsm/test_hub/grt_testhub/apps/data_factory/bug_analysis_view.py)：
1. 修改`create_bug_to_yunxiao`接口，支持`multipart/form-data`格式（通过request.FILES接收文件）
2. 创建Bug成功获取workitem_id后，遍历所有上传文件逐个上传到云效
3. 如果是图片文件，收集返回的`embedMarkdown`，追加到描述末尾并更新工作项
4. 视频/文档等非图片文件直接作为附件关联到工作项，不修改描述
5. 单个附件上传失败不影响主流程，继续上传其他文件

**前端** [BugSyncManagement.vue](file:///Users/jinshaomin/Documents/jinsm/test_hub/grt_testhub/frontend/src/views/data-factory/BugSyncManagement.vue)：
1. 描述textarea增加`@paste`事件监听，处理剪贴板图片粘贴
2. 新增"选择附件"按钮（el-upload组件），支持选择图片/视频/文档
3. 待上传附件列表展示，显示文件名、大小，支持删除
4. 粘贴图片自动加入待上传列表，并提示用户
5. 创建提交时改用FormData格式，同时传递文本字段和文件
6. 文件大小限制：单文件不超过50MB

**前端API** [data-factory.js](file:///Users/jinshaomin/Documents/jinsm/test_hub/grt_testhub/frontend/src/api/data-factory.js)：
1. 修改`createBugToYunxiao`支持第二个config参数，允许传递headers配置
2. 超时时间从30秒增加到120秒，适配大文件上传

### 功能说明
1. **图片粘贴**：在描述输入框中直接Ctrl+V粘贴截图，图片会自动加入待上传列表，提交后自动插入到描述末尾
2. **附件选择**：点击"选择附件"按钮可以选择多个文件上传
3. **支持格式**：
   - 图片：jpg/jpeg/png/gif/bmp/webp（自动插入描述）
   - 视频：mp4/mov/avi/wmv/flv（作为附件）
   - 文档：pdf/word/excel等任意格式（作为附件）
4. **大小限制**：单个文件不超过50MB
5. **编辑模式**：当前编辑Bug暂不支持追加附件（后续可以扩展）

### 经验教训
1. **云效附件上传需要先创建工作项**：不像有些平台支持预上传，云效必须先拿到workitem_id才能上传附件，所以流程是"先创建Bug -> 再逐个上传附件 -> 如果是图片则更新描述"
2. **multipart/form-data不能预设Content-Type**：使用requests上传文件时，不要在session中预设Content-Type: application/json，否则会导致boundary错误，需要单独构造headers
3. **图片上传后embedMarkdown可直接用在描述中**：云效返回的embedMarkdown格式是`![image](永久URL)`，可以直接追加到描述字段更新工作项，在云效网页端能正常显示图片
4. **Django request.FILES获取上传文件**：Django REST Framework中，multipart/form-data格式的文件通过request.FILES获取，文本字段仍然可以通过request.data.get获取

---

## 2026-08-10 修复编辑Bug时描述输入框显示图片markdown链接问题

### 问题描述
创建带图片附件的Bug后，再打开编辑弹窗时，描述textarea中显示的是markdown图片源码`![xxx](https://...)`而不是图片预览，看起来不友好。图片已经作为附件存在，描述中不需要保留这些链接。

### 修改方案

**后端** [bug_analysis_view.py](file:///Users/jinshaomin/Documents/jinsm/test_hub/grt_testhub/apps/data_factory/bug_analysis_view.py)：
1. 新增`_clean_bug_description(desc)`工具函数，使用正则移除描述中的markdown图片语法`![...](url)`，以及单独的云效附件URL行`(https://...)`，并清理多余空行
2. 创建Bug时，不再将图片的embedMarkdown追加到描述（图片只作为附件上传到云效即可，不需要写进描述）
3. 存储`local_data['desc']`时（创建和更新）都调用`_clean_bug_description`清理

**前端** [BugSyncManagement.vue](file:///Users/jinshaomin/Documents/jinsm/test_hub/grt_testhub/frontend/src/views/data-factory/BugSyncManagement.vue)：
1. 新增`cleanDescription(desc)`函数，与后端逻辑一致，过滤markdown图片链接
2. 编辑Bug回填表单时，`desc`字段先经过`cleanDescription`清理，确保输入框只显示纯文本描述

### 经验教训
1. **富文本附件不要塞进纯文本字段**：纯文本textarea无法渲染markdown/HTML，把图片链接写进去只会显示源码，影响用户体验。图片/视频/文档统一通过附件字段管理即可
2. **输入和输出都要清理**：不仅新创建的Bug不再往描述写图片链接，历史数据在编辑回填和更新存储时也要做清理，保证数据一致性

---

## 2026-08-10 添加一键同步所有功能，移除刷新按钮

### 需求
1. 列表右上角原来有"刷新"按钮，实际用处不大（分页切换等操作会自动刷新），移除
2. 添加"一键同步所有"按钮，可以一键批量重新同步所有已同步到云效的Bug，显示同步进度

### 实现方案

**前端** [BugSyncManagement.vue](file:///Users/jinshaomin/Documents/jinsm/test_hub/grt_testhub/frontend/src/views/data-factory/BugSyncManagement.vue)：
1. 移除原来的刷新按钮，替换为绿色的"一键同步所有"按钮
2. 添加批量同步状态：`batchResyncLoading`、`batchResyncProgress`、`batchResyncTotal`
3. 新增`resyncAllBugs()`函数：
   - 过滤出所有有`yunxiao_workitem_id`的记录（即已同步过的）
   - 弹出确认框显示待同步数量
   - 逐个调用单个重新同步接口，每次间隔200ms避免请求过快
   - 按钮实时显示进度：`同步中 (x/总数)`
   - 单个失败不终止，继续同步下一个，最后统计成功/失败数量
   - 全部完成后自动刷新列表，弹出结果汇总（失败列表最多显示10条）

### 功能说明
- 点击按钮后会先确认，然后开始逐个同步
- 同步过程中按钮显示loading和进度，不可重复点击
- 网络错误或单个Bug同步失败不影响其他，最后会汇总报告
- 同步间隔200ms，避免触发云效API限流
- 已筛选的列表只同步当前页中已同步过的项

---

## 2026-08-10 优化批量同步：添加复选框支持选择部分同步，移除单行重新同步按钮

### 需求
1. 移除每行操作列的"重新同步"按钮
2. 在表格最前面增加复选框选择列
3. 通过筛选后，可以勾选需要的Bug进行批量同步；不勾选则同步当前页全部已同步的Bug
4. 按钮文案根据选中状态动态变化

### 实现方案

**前端** [BugSyncManagement.vue](file:///Users/jinshaomin/Documents/jinsm/test_hub/grt_testhub/frontend/src/views/data-factory/BugSyncManagement.vue)：
1. 表格添加 `type="selection"` 复选框列，宽度50px，放在最前面
2. 添加 `bugTableRef` 引用，`selectedBugItems` 存储选中项，`handleSelectionChange` 处理选择变化
3. 移除操作列的"重新同步"按钮，操作列宽度从260px缩窄到180px
4. 修改一键同步按钮文案：
   - 未选中：显示"一键同步当前页"
   - 已选中：显示"同步选中 (X)"
   - 同步中：显示"同步中 (x/总数)"
5. 修改 `resyncAllBugs()` 函数逻辑：
   - 优先同步选中的项（有yunxiao_workitem_id的）
   - 没有选中则同步当前页（筛选后）所有已同步过的项
   - 同步完成后自动清空选中状态并刷新列表

### 使用说明
- 可以先通过顶部筛选条件（同步状态/严重程度）过滤列表
- 勾选需要重新同步的Bug，点击"同步选中"按钮
- 如果不勾选任何项，点击按钮会同步当前筛选结果下的所有已同步Bug
- 同步完成后自动清空勾选，刷新列表数据

---

## 2026-08-10 筛选优化：移除同步状态筛选，添加创建人搜索

### 修改内容
1. **移除**：顶部"同步状态"下拉筛选
2. **新增**：创建人模糊搜索输入框，支持按创建人姓名关键字筛选
3. **后端**：创建Bug时存储当前登录用户名到`local_data.creator`字段
4. **严重程度筛选**：顺便给严重程度下拉框加上`filterable`支持搜索选择

### 实现细节
- 创建人搜索使用输入框，支持实时模糊匹配，大小写不敏感
- 新创建的Bug会自动记录创建人（取`request.user.username`，未登录默认为`system`）
- 历史数据没有creator字段不影响搜索，搜索时兼容多种字段名（`local_data.creator`/`creator`/`created_by`）

---

## 2026-08-10 筛选改为按令牌筛选（替代创建人搜索）

### 问题原因
创建人搜索不可用，因为：
1. 历史数据没有存储creator字段
2. 每个人使用自己配置的云效令牌提交Bug，令牌本身就是身份标识

### 修改方案
将创建人搜索改为按令牌下拉筛选：
- 使用下拉选择框，选项直接复用页面已加载的`yunxiaoTokenOptions`令牌列表
- 选项显示令牌名称，值为令牌ID
- 筛选条件：`local_data.token_id === 选中的令牌ID`
- 支持filterable搜索选择，支持clearable清空
- 令牌名称本身带有标识性，可以一眼区分是谁提交的Bug

**修复字段名问题**：令牌选项显示字段是`display`不是`name`，一开始写错导致只显示数字ID"1"，改为`token.display`后正确显示令牌名称（如"金绍敏"）。

---

## 2026-08-10 优化Token管理抽屉列表样式与Bug管理页面统一

### 问题
Token管理抽屉（TokenManagerDrawer.vue）中的Token列表样式简陋，与BugSyncManagement.vue的列表风格不统一，视觉体验不一致。

### 修改方案
参考 [BugSyncManagement.vue](file:///Users/jinshaomin/Documents/jinsm/test_hub/grt_testhub/frontend/src/views/data-factory/BugSyncManagement.vue) 的紫色主题表格风格，优化 [TokenManagerDrawer.vue](file:///Users/jinshaomin/Documents/jinsm/test_hub/grt_testhub/frontend/src/views/data-factory/TokenManagerDrawer.vue) 列表样式：

**模板调整**（不影响功能逻辑）：
1. 表格移除`border`属性，添加`stripe`斑马纹
2. 所有列添加`header-align="center"`统一表头居中
3. 标签、Token脱敏、更新时间列添加`show-overflow-tooltip`
4. 操作按钮包裹`.action-buttons`容器，编辑按钮改为`type="primary"`
5. 更新时间用`<span class="time-text">`包裹

**样式新增**：
1. `.token-list-card`：白色卡片背景 + 紫色边框 + 圆角阴影
2. 表格deep样式：表头紫色字体（`#5a32a3`）、行hover浅紫背景（`#f8f7ff`）、斑马纹行（`#fafaff`）
3. `.action-buttons`：编辑按钮紫色渐变、删除按钮红色渐变，hover上浮效果
4. 抽屉头部：渐变背景 + 紫色标题
5. 保存按钮：紫色渐变 + hover上浮阴影
6. 表单区域：增加紫色边框 + 圆角

### 涉及文件
- `frontend/src/views/data-factory/TokenManagerDrawer.vue`

### 经验教训
1. **组件间样式要统一**：同一系统内多个列表/表格应保持一致的视觉风格（配色、圆角、间距、按钮样式），提升整体一致性
2. **scoped样式用:deep穿透**：Element Plus组件内部样式需要用`:deep()`穿透 scoped 限制才能生效

---

## 2026-08-10 修复Token列表更新时间时区偏移问题

### 问题
Token管理抽屉中"更新时间"列显示的时间比实际时间差8小时（少8小时），存在时区偏移。

### 根本原因
Django settings 配置了 `USE_TZ = True` 和 `TIME_ZONE = 'Asia/Shanghai'`，DateTimeField 存储的是 UTC 时间。`to_list_dict()` 中直接用 `self.updated_at.strftime('%Y-%m-%d %H:%M:%S')` 格式化输出的是 UTC 时间字符串。前端再用 `new Date(row.updated_at).toLocaleString('zh-CN')` 解析时，将该 UTC 字符串当作本地时间处理，没有做时区转换，导致时间偏差。

### 修改方案
**后端** [models.py](file:///Users/jinshaomin/Documents/jinsm/test_hub/grt_testhub/apps/data_factory/models.py)：
- `to_list_dict()` 中使用 `timezone.localtime(self.created_at, local_tz).strftime(...)` 将 UTC 时间转换为本地时区后再格式化输出

**前端** [TokenManagerDrawer.vue](file:///Users/jinshaomin/Documents/jinsm/test_hub/grt_testhub/frontend/src/views/data-factory/TokenManagerDrawer.vue)：
- 移除 `new Date(row.updated_at).toLocaleString('zh-CN')` 解析，直接显示后端返回的已格式化字符串 `{{ row.updated_at || '-' }}`，避免前端二次解析产生时区问题

### 涉及文件
- `apps/data_factory/models.py`：`YunxiaoToken.to_list_dict()` 使用 `timezone.localtime` 转换时区
- `frontend/src/views/data-factory/TokenManagerDrawer.vue`：前端直接显示后端格式化字符串

### 经验教训
1. **Django USE_TZ=True时DateTimeField存的是UTC**：`strftime()` 直接输出的是UTC时间，需要先用 `timezone.localtime()` 转为本地时区再格式化
2. **后端格式化好的时间字符串前端不要二次解析**：后端已输出 `2026-08-10 15:24:30` 格式字符串，前端不应再用 `new Date()` 解析，否则浏览器会按本地时区再次偏移
3. **时间格式化应统一在后端处理**：后端统一使用 `timezone.localtime` + `strftime` 输出本地时间字符串，前端只负责展示，避免前后端时区逻辑不一致

---

## 2026-08-10 修复创建Bug时验证者(verifier)始终为同一人的问题

### 问题
创建Bug同步到云效时，验证者字段始终显示为"胖博"（项目成员列表的第一个成员），而不是根据当前使用的Token对应的用户来设置。

### 原因
`bug_analysis_view.py` 的 `create_bug_to_yunxiao` 视图函数中，验证者(verifier)的解析逻辑存在问题：

1. **前端没有传verifier参数**：视图函数从 `request.data` 中提取了 `assignee`（处理人），但没有提取 `verifier`，导致 `resolved_verifier` 初始值始终为 `None`
2. **fallback逻辑不合理**：当 `resolved_verifier` 为 `None` 时，代码直接使用 `members[0]["userId"]`（项目第一个成员的userId）作为验证者，而项目第一个成员恰好是"胖博"
3. **`token.created_by`无法使用**：`_resolve_yunxiao_client` 返回的是 `YunxiaoClient` 实例，`client.token` 是PAT字符串而非 `YunxiaoToken` 模型对象，原代码 `getattr(token, 'created_by', None)` 取到的是字符串的属性（永远为None），且 `created_by` 存的是Django系统用户名而非云效用户名

### 修改方案
通过云效 OpenAPI `GET /oapi/v1/platform/user`（GetUserByToken）直接根据Token获取对应的云效用户信息，其中 `id` 字段即为云效userId：

**1. yunxiao_client.py 新增 `get_current_user()` 方法**

```python
def get_current_user(self) -> Optional[Dict[str, Any]]:
    """根据当前Token获取云效用户信息 (GET /oapi/v1/platform/user)"""
    url = f"{self.domain}/oapi/v1/platform/user"
    resp = self.session.get(url, timeout=30)
    # 返回 {id, name, nickName, username, email, ...}
```

注意：该接口路径是 `/oapi/v1/platform/user`，不在 `_get_base_url` 的 `/oapi/v1/projex/organizations/{orgId}` 前缀下，需要直接拼接URL。

**2. bug_analysis_view.py 中 verifier 解析逻辑**

```python
# 通过云效API获取当前Token对应的用户ID，作为验证者
if not resolved_verifier:
    current_user = client.get_current_user()
    if current_user and current_user.get("id"):
        resolved_verifier = str(current_user["id"])

# fallback: Token无法获取用户时，取项目第一个成员
if not resolved_verifier:
    members = get_members()
    if members:
        resolved_verifier = members[0]["userId"]
```

### 涉及文件
- `apps/data_factory/yunxiao_client.py`：新增 `get_current_user()` 方法，调用 `/oapi/v1/platform/user` 获取Token对应的云效用户
- `apps/data_factory/bug_analysis_view.py`：`create_bug_to_yunxiao()` 中 verifier 改为通过 `client.get_current_user()` 获取

### 经验教训
1. **云效有GetUserByToken接口**：`GET /oapi/v1/platform/user` 可直接根据PAT Token获取当前用户信息（id/name/email等），无需自己维护Token与用户的映射关系
2. **API路径前缀不同**：大多数云效projex接口在 `/oapi/v1/projex/organizations/{orgId}/` 下，但platform接口在 `/oapi/v1/platform/` 下，不能用统一的 `_get_base_url` 拼接
3. **`_resolve_yunxiao_client`返回的是client实例**：`client.token` 是PAT字符串，不是 `YunxiaoToken` 模型对象，不能通过 `client.token.created_by` 获取创建者信息
4. **Django用户名≠云效用户名**：`YunxiaoToken.created_by` 存的是Django系统用户名（`user.username`），与云效的userName/displayName/userId完全不同，不能直接用于云效API

---

## 2026-08-10 修复Bug列表刷新时行高闪烁问题

### 问题
Bug同步管理页面（`/ai-generation/bug-analysis/sync`）刷新列表加载时，表格行从默认较高高度"闪"到最终紧凑高度，产生明显的视觉跳动。

### 根本原因
1. **`transition: all 0.3s ease` 作用在表格结构元素上**：`.el-table`、`th`、`.el-table__row`、`td` 都设置了 `transition: all`，当 Element Plus 内部在数据渲染后重新计算行高/padding/line-height 时，这些布局属性的变化被过渡动画放大，形成了可见的"闪"效果
2. **`bugSyncItemsLoading` 初始值为 `false`**：组件挂载到 `onMounted` 调用 `loadBugSyncItems` 之间有一个短暂间隙，表格以非 loading 状态渲染空数据，造成二次布局切换

### 修改方案
**1. 将 `transition: all 0.3s ease` 替换为 `transition: background-color 0.3s ease`**

涉及4处 CSS 规则：
- `.el-table`：移除 transition（整体表格不需要过渡）
- `th`：改为 `transition: background-color 0.3s ease`（保留 hover 背景色过渡）
- `.el-table__row`：改为 `transition: background-color 0.3s ease`（保留 hover 背景色过渡）
- `td`：改为 `transition: background-color 0.3s ease`（保留 hover 背景色过渡）

**2. `bugSyncItemsLoading` 初始值从 `false` 改为 `true`**

确保组件首次渲染时表格就处于 loading 状态，避免空表短暂闪现后再变为 loading 态。

### 涉及文件
- `frontend/src/views/data-factory/BugSyncManagement.vue`

### 经验教训
1. **`transition: all` 是性能和视觉陷阱**：`all` 会监听所有 CSS 属性变化，包括 padding、height、line-height 等布局属性。布局属性的变化通常不应做过渡动画，否则会产生布局跳动/闪烁。应只对 `background-color`、`color`、`box-shadow` 等视觉属性设置过渡
2. **loading 状态初始值应为 `true`**：组件挂载后需要异步加载数据时，loading 初始值应设为 `true`，避免首次渲染时先渲染空数据再切换到 loading 态的二次闪烁

---

## 2026-08-10 Bug列表「已修复」状态快捷改状态功能

### 需求
Bug列表中状态为「已修复」的Bug，希望能快捷操作改为「已验证」或「已关闭」，不用每次都打开编辑弹窗修改。

### 交互方案
将「已修复」状态标签本身变为可点击的下拉菜单：
1. 当Bug状态为「已修复」时，状态标签显示一个向下小箭头图标，hover时高亮提示可点击
2. 点击后弹出下拉菜单，包含「标记为「已验证」」「标记为「已关闭」」两个选项
3. 选择后弹出确认框，确认后直接调用API更新状态，无需打开编辑弹窗
4. 其他状态保持原有的静态展示，不加交互

### 实现方案

**前端** [BugSyncManagement.vue](file:///Users/jinshaomin/Documents/jinsm/test_hub/grt_testhub/frontend/src/views/data-factory/BugSyncManagement.vue)：
1. 状态列模板改为条件渲染：当 `status === '已修复'` 时用 `el-dropdown` 包裹状态标签，否则用普通 `span`
2. 导入 `CaretBottom` 图标，在可点击标签末尾显示向下箭头
3. 新增 `quickChangeStatus(row, newStatus)` 函数：确认后调用 `updateBugToYunxiao(row.id, { token_id, status })` 部分更新
4. CSS 新增 `.status-clickable` 类：`cursor: pointer` + hover 时 `box-shadow` 高亮 + 箭头样式
5. 状态列宽度从 100px 扩展到 120px（容纳箭头图标）

**后端**：无需修改，`update_bug_to_yunxiao` 接口已支持部分更新（只传 `token_id` + `status` 即可）

### 涉及文件
- `frontend/src/views/data-factory/BugSyncManagement.vue`

### 经验教训
1. **部分更新优于全量更新**：后端 `update_bug_to_yunxiao` 遍历字段列表只取非 None 的值构建 update_data，天然支持只传变更字段的场景，快捷操作无需发送完整表单
2. **上下文操作优于弹窗操作**：对于状态流转这种高频快捷操作，在状态标签上直接提供下拉菜单比打开编辑弹窗修改更高效，减少操作步骤
3. **条件渲染控制交互范围**：只对「已修复」状态启用快捷菜单，其他状态保持静态展示，避免过度交互干扰

---

## 2026-08-10 修复快捷改状态「再次打开」报400错误

### 问题
快捷改状态选择「再次打开」时，云效API返回HTTP 400错误：`currentStatus can not transit to status not existed, toStatus: 再次打开`，状态中文名未被解析为ID。

### 原因
`_resolve_status_id()` 缓存未命中时只搜索50条Bug来收集状态映射。「再次打开」(Reopen) 是低频状态，50条Bug中很可能没有此状态的Bug，导致缓存收集不到该状态名称→ID的映射，最终将中文名直接传给云效API。

### 修改方案
扩大状态搜索范围：从搜索1页50条改为搜索2页、每页200条（最多400条Bug），覆盖低频状态。

修改 [yunxiao_client.py](file:///Users/jinshaomin/Documents/jinsm/test_hub/grt_testhub/apps/data_factory/yunxiao_client.py) 的 `_resolve_status_id` 方法：
1. `per_page` 从50改为200（API最大允许值）
2. 搜索从1页改为最多2页（`for page in (1, 2)`）
3. 每页搜索后检查缓存是否命中，命中即返回
4. 如果搜索结果为空则提前终止翻页

### 涉及文件
- `apps/data_factory/yunxiao_client.py`：`_resolve_status_id()` 方法扩大搜索范围

### 经验教训
1. **低频数据采样不足**：从业务数据中收集元数据（如状态映射）时，低频值（如「再次打开」）在小样本中可能完全不存在，需要足够的采样量
2. **缓存收集策略要考虑数据分布**：状态分布不均匀（待确认/已修复占大多数，再次打开/暂不修复极少），简单的分页采样需要更多数据才能覆盖低频值

---

## 2026-08-10 Bug描述字段添加模板快捷插入功能

### 需求
Bug创建表单的描述字段是纯文本输入框，用户每次都要手打「前置条件」「操作步骤」等结构化内容，效率低。希望能提供模板快捷插入，提升提交描述效率。

### 实现方案
在描述输入框上方添加工具栏，包含两部分：

1. **快捷标签**：5个常用模板标签 `【前置条件】` `【操作步骤】` `【预期结果】` `【实际结果】` `【复现概率】`
   - 点击后在光标位置插入标签+换行，光标自动移到插入内容之后
   - 支持在已有文本中间插入，不覆盖原有内容
2. **填入完整模板**：一键填充完整的Bug描述模板（含编号占位），适合从零开始写

**技术细节**：
- `descTextareaRef` 通过 `ref` 引用 el-input 组件，通过 `$el.querySelector('textarea')` 获取原生 textarea 元素
- `insertDescTag` 使用 `textarea.selectionStart/End` 获取光标位置，用 `setSelectionRange` 恢复光标
- `fillDescTemplate` 直接设置 `bugForm.desc`，光标移到开头
- textarea 行数从4行增加到6行，容纳模板内容

### 涉及文件
- `frontend/src/views/data-factory/BugSyncManagement.vue`：描述工具栏模板、insertDescTag/fillDescTemplate函数、CSS样式