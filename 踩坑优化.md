# UI 自动化踩坑与优化记录

---

## 2025-06-15

### 1. 套件执行报告中步骤显示"待执行"但实际已执行

**原因：**
- `mark_task_complete` 回调内部未调用 `step_callback` 更新前端状态
- 套件执行完成后缺少兜底标记机制，部分任务状态未被同步

**修改方案：**
- 在 `ai_base.py` 的 `on_step_end` 回调中，当检测到 `mark_task_complete` action 时，主动调用 `step_callback` 推送状态变更
- 在 `views.py` 的 `run_suite` 中新增 `_auto_mark_completed_tasks` 方法，套件执行成功后自动将所有 `pending` 任务标记为 `completed`

---

### 2. 套件中第二个用例未走步骤解析快速路径

**原因：**
- `run_single_case` 方法中未实现快速路径逻辑，每次都会调用 LLM 解析步骤
- 单场景的 `analyze_task` 有快速路径，但套件的 `run_single_case` 没有同步该逻辑

**修改方案：**
- 在 `ai_base.py` 的 `run_single_case` 方法中，添加与 `analyze_task` 相同的快速路径和缓存逻辑：
  - 检测带编号步骤（如 `1. xxx`、`2. xxx`）
  - 命中快速路径时直接正则解析，跳过 LLM 调用

---

### 3. 快速路径必须步骤数 >= 2，单步骤用例无法走快速路径

**原因：**
- 快速路径判断条件为 `len(numbered_lines) >= 2`，单步骤用例被排除

**修改方案：**
- 将条件放宽为 `len(numbered_lines) >= 1`，支持单步骤用例走快速路径
- 同时适用于单场景和套件执行

---

### 4. 单场景登录运行很快，放到套件里就变慢

**原因：**
- 单场景的 `final_task` Prompt 末尾有 6 条性能优化规则，套件的 Prompt 没有
- 缺少规则导致 LLM 思考更多、重复操作、失败重试增加，整体变慢

**修改方案：**
- 在 `ai_base.py` 的 `run_single_case` 中，给 `final_task` 追加与 `run_task` 相同的 6 条规则：
  1. `NO JAVASCRIPT IN INPUT`
  2. `DROPDOWN & MODAL ISOLATION`
  3. `ULTRALIGHT THINKING`（50 tokens 内响应）
  4. `RETRY LOGIC`
  5. `DO NOT REPEAT`
  6. `VERIFICATION`

---

### 5. 套件执行完第一个用例衔接第二个用例中间耗时

**原因：**
- 每个用例执行完后，`_process_gif_recording`（移动 GIF 文件）和 `safe_save`（数据库保存）在主线程同步执行
- GIF 文件较大时，`shutil.move` 阻塞主线程，下一个用例必须等待

**修改方案：**
- 在 `views.py` 的 `run_suite` 中，将 `_process_gif_recording` + `safe_save` 放到后台线程执行
- 主线程立即进入下一个用例循环，消除衔接等待

---

### 6. 套件所有用例执行完后浏览器进程未销毁

**原因：**
- `PersistentBrowserSession.close_async()` 和 `close()` 中调用的是 `self.browser_session.close()`
- `BrowserSession` 没有 `close()` 方法，实际调用的是 Pydantic `BaseModel` 的空方法，不会关闭底层 Chrome 进程

**修改方案：**
- 将 `self.browser_session.close()` 改为 `self.browser_session.kill()`
- `kill()` 是 browser-use 提供的强制关闭浏览器进程并清理状态的方法
- 同时保留 `controller.close()` 的关闭逻辑

---

## 2026-06-20：执行报告三页签整合为单页展示

### 问题原因
- 执行报告原本通过 `el-radio-group` 切换三个页签（执行摘要、详细步骤、性能分析），用户需要来回切换才能查看完整信息
- 每次切换页签都会重新调用后端 API，体验不佳

### 修复方案
#### 后端 —— 新增完整报告接口
- `reports.py` 新增 `generate_full_report()`：合并 `generate_summary_report()`、`generate_detailed_report()`、`generate_performance_report()` 的数据，一次返回所有内容
- `views.py` 的 `generate_report` 和 `export_pdf` 方法支持 `report_type='full'`，PDF 导出时 `full` 按 `detailed` 模板生成

#### 前端 —— 单页展示改造
- 去掉 `el-radio-group` 页签切换，改为顶部操作栏（仅保留导出按钮）
- 三个区域按顺序纵向排列：
  1. 执行摘要（`v-if="reportData.overview || reportData.statistics"`）
  2. 详细步骤（`v-if="reportData.detailed_steps && reportData.detailed_steps.length > 0"`）
  3. 性能分析（`v-if="reportData.metrics || reportData.action_distribution"`）
- `loadReport` 默认加载 `full`，`setTimeout` 内同时调用 `initPieChart()` 和 `initBarChart()`
- `exportReport` 导出类型固定为 `full`
- 标题固定为"完整报告"，清理未使用的 `currentReportType`、`VideoPlay` 等变量

### 涉及文件
- `apps/ui_automation/reports.py`
- `apps/ui_automation/views.py`
- `frontend/src/views/ui-automation/ai/AIExecutionReport.vue`

---

## 2026-06-20：报告查看从抽屉弹窗改为新页面跳转

### 问题原因
- 执行报告列表页点击"查看报告"按钮后，通过 `el-drawer` 抽屉弹窗展示报告
- 抽屉弹窗空间受限，用户查看长报告时需要滚动，体验不佳
- 与其他模块（如 API 测试报告）的页面跳转模式不一致

### 修复方案
#### 1. 新建报告详情页面组件
- 复制 `AIExecutionReport.vue` 为 `AIExecutionReportPage.vue`
- 去掉 `el-drawer` 包裹，改为 `<div class="report-page">` 页面容器
- 添加页面头部：返回按钮 + 面包屑导航 + 页面标题
- 使用 `useRoute` 从路由参数 `id` 获取 `recordId`，`useRouter` 实现返回
- 去掉 `props`/`emit`/`visible`/`handleClose` 等抽屉相关逻辑
- `onMounted` 时自动调用 `loadReport('full')`
- 保留所有报告展示逻辑（摘要、详细步骤、性能分析）和导出功能

#### 2. 添加路由配置
- `router/index.js` 导入 `AIExecutionReportPage.vue`
- 在 `/ai-intelligent-mode` 下新增子路由 `execution-report/:id`，名称 `AIExecutionReport`

#### 3. 修改列表页交互
- `AIExecutionRecords.vue` 导入 `useRouter`
- `viewReport(row)` 改为 `router.push(`/ai-intelligent-mode/execution-report/${row.id}`)`
- `openReportFromDetail()` 同样改为路由跳转
- 保留原有抽屉组件引用，仅修改跳转逻辑，不影响其他功能

### 涉及文件
- `frontend/src/views/ui-automation/ai/AIExecutionReportPage.vue`

---

## 2026-06-21：测试套件执行只产生单条执行报告记录

### 问题原因
- 测试套件运行时，`run_suite` 方法在循环中每执行一个用例就创建一条 `AIExecutionRecord`
- 导致执行报告列表中一个套件会产生多条记录，且每条记录只包含单个用例的截图和步骤
- 用户预期是：套件执行只产生一条报告记录，里面汇总展示所有用例的步骤和截图

### 修复方案
#### 1. 模型扩展
- `AIExecutionRecord` 增加 `ai_test_suite` 外键字段（nullable），用于标识该记录是套件执行记录
- 使用字符串引用 `'AITestSuite'` 解决前向引用问题（`AITestSuite` 定义在 `AIExecutionRecord` 之后）

#### 2. 后端逻辑重构（`views.py` 的 `run_suite`）
- 在循环开始前创建**一条**套件级别的 `AIExecutionRecord`，`case_name` 设为套件名称，`ai_test_suite` 指向当前套件
- 循环中用 `all_steps`、`all_screenshots`、`all_planned_tasks`、`all_logs` 累积每个用例的数据
- 每个步骤增加 `case_name` 字段，便于前端按用例分组展示
- 所有用例执行完毕后，统一保存合并后的数据到这条记录
- 状态统计（passed_count / failed_count）仍然在循环中累加，最终更新到 `ai_test_suite`

#### 3. 序列化器调整
- `AIExecutionRecordSerializer` 添加 `ai_test_suite`、`ai_test_suite_id`、`ai_test_suite_name` 字段
- 使用 `SerializerMethodField` 返回简化版的套件信息（`id` 和 `name`），避免前向引用 `AITestSuiteSerializer`

#### 4. 前端列表页（`AIExecutionRecords.vue`）
- `case_name` 列增加套件标识：如果 `row.ai_test_suite` 存在，显示 `套件` 标签

#### 5. 前端报告页分组展示
- `AIExecutionReportPage.vue` 和 `AIExecutionReport.vue` 都增加 `groupedSteps` computed 属性
- 按 `case_name` 将步骤分组，每组前显示用例名称标签（`el-divider` + `el-tag`）
- 单用例执行时 `case_name` 为空，不会显示分组标题，保持原有展示效果

### 涉及文件
- `apps/ui_automation/models.py`
- `apps/ui_automation/views.py`
- `apps/ui_automation/serializers.py`
- `frontend/src/views/ui-automation/ai/AIExecutionRecords.vue`
- `frontend/src/views/ui-automation/ai/AIExecutionReportPage.vue`
- `frontend/src/views/ui-automation/ai/AIExecutionReport.vue`

---

## 2026-06-21：测试套件运行报告支持每步截图展示

### 问题原因
- 单用例执行完毕后，`views.py` 会提取 `result.screenshots_sequence` 并关联到 `steps_completed` 中的每个步骤，前端报告因此能展示每一步的截图
- AI 测试套件执行时，`run_suite` 虽然同样调用了 `browser_session.run_single_case()`，但返回结果后仅处理了 GIF 录制文件（`_process_gif_recording`），完全没有提取和保存 `screenshots_sequence` 与 `steps_completed`
- 导致套件中的每个用例执行记录都缺少步骤截图数据，前端报告只能回退到展示 GIF 动画，无法像单用例那样逐步骤查看截图

### 修复方案
#### 1. 清空截图列表避免累积
- `ai_base.py` 的 `run_single_case()` 开头添加 `self.screenshots_sequence = []`
- 原因：`PersistentBrowserSession` 在套件执行中被复用，`screenshots_sequence` 如果不清空，前一个用例的截图会累积到下一个用例，导致截图与步骤错位

#### 2. 套件执行后提取并保存截图和步骤
- `views.py` 的 `run_suite()` 中，每个用例执行完毕后（`execution_record.end_time` 赋值后），新增以下逻辑：
  - 从 `result` 中提取 `screenshots_sequence`
  - 从 `history` 中格式化 `steps`（复用 `extract_step_info`）
  - 按索引将截图路径关联到对应步骤的 `screenshot` 字段
  - 将 `steps_completed` 和 `screenshots_sequence` 写入 `execution_record`
  - 立即调用 `safe_save(execution_record)` 保存到数据库

#### 3. 保留 GIF 处理
- `_process_gif_recording` 仍放在后台线程执行，不影响现有 GIF 生成逻辑
- 前端报告优先使用 `step.screenshot` 展示截图，当截图不存在时仍可通过 `gif_path` 查看 GIF 回放

### 涉及文件
- `apps/ui_automation/ai_base.py`
- `apps/ui_automation/views.py`

---

## 2026-06-20：AI 报告页步骤截图预览串图

### 问题原因
- `AIExecutionReportPage.vue` 中每个步骤的 `el-image` 都绑定了同一个 `preview-src-list`（所有有截图的步骤的图片列表）
- Element Plus 的 `el-image` 在多个实例共享同一个 `preview-src-list` 时，预览组件的初始索引匹配会出错
- 导致点击步骤 9 的图片放大后，显示的却是步骤 1（或其他步骤）的图片

### 修复方案
- 将每个步骤 `el-image` 的 `preview-src-list` 改为只包含当前步骤的截图：`['/' + step.screenshot]`
- 每个步骤独立预览，互不影响，点击放大后只显示当前步骤对应的截图

### 涉及文件
- `frontend/src/views/ui-automation/ai/AIExecutionReportPage.vue`

---

## 2026-06-20：AI 用例执行状态判定错误（AI 返回失败但显示成功）

### 问题原因
- `views.py` 中 AI 用例执行完毕后，只要 `agent.run()` 没有抛异常、不是用户手动停止，就直接把 `execution_record.status` 写死为 `'passed'`
- 完全没有读取 AI 在 `done` action 中返回的 `success` 参数
- 当 AI 因为找不到目标内容而调用 `done(success=False)` 时，执行记录仍然显示成功，造成"任务明明失败但显示通过"的 Bug

### 修复方案
- 在 `ai_base.py` 的 `BaseBrowserAgent.run_task()` 返回结果前，从 `history` 中倒序查找最后一个 `done` action，解析其 `params.success` 参数
- 将解析出的 `success`（True/False/None）一并返回给调用方
- 在 `views.py` 的状态判定逻辑中，优先使用 AI 返回的 `success` 参数：
  - `success is False` → 状态设为 `'failed'`，日志提示 AI 判定任务失败
  - `success is True` 或未返回 → 保持原有逻辑，状态设为 `'passed'`

### 补充修复（步骤级别状态显示）
- 上述修复只解决了"整条执行记录"的状态判定问题，但报告详情页中每个步骤右侧的"已完成/失败"标签仍然显示不正确
- 原因是 `views.py` 的 `extract_step_info()` 在解析 browser-use 的 `AgentOutput` 时，只提取了 action 文本，没有记录 `done` action 的 `success` 参数
- `reports.py` 的 `_parse_detailed_steps()` 在 `step` 中没有 `status` 字段时，默认给所有步骤 `'completed'` 状态
- 修复：在 `extract_step_info()` 解析到 `done` action 时，根据 `params.success` 的值设置 `step_info['status']`：`False` 设为 `'failed'`，`True` 设为 `'completed'`

### 涉及文件
- `apps/ui_automation/ai_base.py`
- `apps/ui_automation/views.py`
- `apps/ui_automation/reports.py`

---

## 2026-06-20：单条 AI 用例执行完毕后浏览器进程未销毁

### 问题原因
- `BaseBrowserAgent.run_task()` 中创建 `Agent` 并执行 `agent.run()` 后，直接返回结果，没有任何浏览器关闭逻辑
- 单条用例跑完后，底层的 Chromium 浏览器进程仍然驻留在内存中，不会被自动回收
- 频繁执行单条用例时，系统中会累积大量僵尸浏览器进程，占用内存和系统资源

### 修复方案
- 在 `ai_base.py` 的 `BaseBrowserAgent.run_task()` 中，`agent.run()` 的 `try...except` 块追加 `finally`
- `finally` 中先尝试获取 `agent.browser_session`，如果存在则调用 `await browser_session.kill()` 强制关闭浏览器进程
- 再尝试关闭 `controller`（`await controller.close()`）
- 关闭逻辑外加 `try...except` 保护，避免清理异常影响主流程结果返回

### 涉及文件
- `apps/ui_automation/ai_base.py`

---

## 2026-06-20：压缩执行概览与任务统计区域占用空间

### 问题原因
- 执行概览的 4 个卡片 padding 过大，数值字体偏大，整体占用较多横向和纵向空间
- 任务统计区域的饼图和表格排列松散，表格行高和 padding 过大，存在大量空白
- 两个区域合计占用了首屏大部分空间，导致下方内容需要大量滚动才能查看

### 修复方案
#### 1. 执行概览卡片压缩
- `.overview-cards` gap 从 16px 改为 12px
- `.overview-card` padding 从 20px 改为 12px，圆角从 12px 改为 8px
- `.card-label` font-size 从 12px 改为 11px，margin-bottom 从 8px 改为 4px
- `.card-value` font-size 从 22px 改为 18px

#### 2. 任务统计区域压缩
- `.statistics-container` gap 从 30px 改为 20px，align-items 从 `center` 改为 `flex-start`（顶部对齐更紧凑）
- `.chart-wrapper` 从 `flex: 1; max-width: 300px` 改为 `flex: 0 0 180px; max-width: 180px`，饼图高度从 200px 改为 160px
- `.stats-table-content td` padding 从 `12px 20px` 改为 `6px 12px`，新增 `font-size: 13px`
- `.stat-value` font-size 从 16px 改为 14px

#### 3. 修复饼图图例与表格重叠
- 饼图 legend 从右侧垂直排列改为底部水平排列（`orient: 'horizontal'`, `bottom: 0`, `left: 'center'`）
- legend 图标缩小为 10x10，字体 11px，确保在 180px 容器内正常显示
- 饼图 center 从 `['35%', '50%']` 改为 `['50%', '42%']`，radius 从 `['40%', '70%']` 改为 `['45%', '65%']`
- 饼图 emphasis label 字体从 16px 改为 14px，避免高亮时文字溢出

### 涉及文件
- `frontend/src/views/ui-automation/ai/AIExecutionReportPage.vue`（新建）
- `frontend/src/views/ui-automation/ai/AIExecutionRecords.vue`
- `frontend/src/router/index.js`

---

## 2026-06-20：合并任务时间线与执行步骤详情

### 问题原因
- "任务执行时间线"和"执行步骤详情"两个区域展示的内容都是执行过程，存在重复感
- 时间线只展示任务描述和状态，步骤详情展示操作标签、元素、思考过程、截图等，两者分开显示导致页面冗长
- 用户需要在两个区域之间滚动查看，体验不佳

### 修复方案
#### 1. 移除独立的任务时间线区域
- 删除第一个 `card-container` 中的"任务执行时间线"区块（含 `el-timeline` 循环展示 `reportData.timeline`）

#### 2. 步骤详情改为时间线形式
- 将原来的 `div.steps-list` 卡片列表改为 `el-timeline` 时间线形式
- 每个 `el-timeline-item` 对应一个 `detailed_step`
- `timestamp` 显示步骤编号，`:type` 根据步骤状态（复用 `getStepStatusType`）
- 步骤卡片内保留操作标签、状态标签、元素、思考过程、截图等全部信息

#### 3. 添加任务规划概览条
- 在步骤时间线上方添加 `.task-plan-bar` 紧凑概览行
- 左侧显示"任务规划："标签，右侧用一行小标签展示所有 `timeline` 任务
- 每个任务标签带 tooltip（鼠标悬停显示任务描述），标签颜色根据任务状态
- 背景使用淡紫色渐变，圆角 8px，与概览卡片风格一致

#### 4. 样式调整
- 新增 `.task-plan-bar`、`.plan-label`、`.plan-tags`、`.plan-tag` 样式
- 删除不再使用的 `.timeline-task-content`、`.task-description`、`.steps-list`、`.step-number` 样式
- `.step-card` 移除左侧紫色边框（时间线节点已提供视觉引导），padding 从 16px 改为 14px
- `.step-header` 增加 `gap: 8px`，底部间距改为 8px
- `.step-element`、`.step-thinking` 字体改为 13px，行高 1.6，strong 颜色统一为 `#5a32a3`

#### 5. i18n 补充
- 在 `zh-cn/ui-automation.js` 和 `en/ui-automation.js` 中添加 `taskPlan: '任务规划' / 'Task Plan'`

### 涉及文件
- `frontend/src/views/ui-automation/ai/AIExecutionReportPage.vue`
- `frontend/src/locales/lang/zh-cn/ui-automation.js`
- `frontend/src/locales/lang/en/ui-automation.js`

---

## 2026-06-20：隐藏性能分析报告区域

### 问题原因
- 当前性能指标数据不够完善，展示价值有限
- 性能分析区域（性能指标、操作分布、性能瓶颈、优化建议）占用页面较大空间

### 修复方案
- 将整个性能分析报告 `card-container` 注释隐藏，保留代码便于后续恢复
- 隐藏内容包括：性能指标卡片、操作分布柱状图、性能瓶颈表格、优化建议列表

### 涉及文件
- `frontend/src/views/ui-automation/ai/AIExecutionReportPage.vue`

---

## 2026-06-20：隐藏执行概览与任务统计区域

### 问题原因
- 执行概览中的步骤数量、任务统计中的任务分布，与执行步骤详情区域的信息重复
- 执行概览的任务完成率、执行时长等摘要信息，在当前场景下用户更关注具体执行过程
- 任务统计的饼图和表格与步骤详情顶部的任务规划条内容一致

### 修复方案
- 将整个执行概览与任务统计的 `card-container` 注释隐藏，保留代码便于后续恢复
- 页面只保留：页面头部（返回+标题+导出）+ 执行步骤详情（含任务规划条+时间线步骤）+ 错误信息

### 涉及文件
- `frontend/src/views/ui-automation/ai/AIExecutionReportPage.vue`

---

## 2026-06-20：隐藏返回按钮并优化页面标题

### 问题原因
- 从列表页跳转进入报告详情页，浏览器自带返回功能，页面内返回按钮冗余
- 固定标题"AI测试执行报告"无法体现当前查看的是哪个用例的报告

### 修复方案
#### 1. 隐藏返回按钮
- 删除页面头部中的返回按钮（`<el-button type="primary" text @click="goBack">`）
- 保留 `goBack` 方法代码，便于后续恢复

#### 2. 隐藏导出报告按钮
- 删除页面头部右侧的导出报告按钮（`<el-button type="success" @click="exportReport">`）
- 保留 `exportReport` 方法代码，便于后续恢复
- 页面头部结构从左右分栏简化为单行标题

#### 3. 标题改为用例名称+报告
- 将固定标题 `{{ $t('uiAutomation.ai.executionReport.title') }}` 改为动态拼接
- 优先取 `reportData.execution_details.case_name`，其次 `reportData.case_name`，兜底显示原标题
- 后缀统一使用 `reportSuffix`（中文"报告"，英文" Report"）
- 在 `zh-cn/ui-automation.js` 和 `en/ui-automation.js` 中新增 `reportSuffix` 字段

#### 4. 任务规划条移至页面头部
- 将步骤详情区域中的任务规划概览条（`task-plan-bar`）移至页面头部标题下方
- 页面头部改为垂直布局（`flex-direction: column`），标题在上，任务规划条在下
- 任务规划条去掉背景色、边框、圆角，与头部卡片融为一体，更简洁
- Tooltip 方向从 `top` 改为 `bottom`，避免与上方内容冲突
- 步骤详情区域移除原任务规划条，避免重复

### 涉及文件
- `frontend/src/views/ui-automation/ai/AIExecutionReportPage.vue`
- `frontend/src/locales/lang/zh-cn/ui-automation.js`
- `frontend/src/locales/lang/en/ui-automation.js`

---

## 2026-06-20：报告详情页样式优化，统一平台设计风格

### 问题原因
- 新创建的 `AIExecutionReportPage.vue` 页面样式较为朴素，与平台其他页面风格不一致
- 页面头部、卡片容器、区块标题等缺少平台统一的紫色渐变主题风格
- 概览卡片和性能指标卡片样式简单，缺少视觉层次和交互反馈

### 修复方案
#### 1. 外层容器与页面头部
- 外层 `.report-page` 改为 `.page-container`：紫色渐变背景 `linear-gradient(135deg, #f5f3ff 0%, #ede9fe 100%)`，padding 24px，gap 20px
- `.page-header` 改为平台标准风格：白色到淡紫色渐变背景，圆角 12px，阴影 `0 4px 16px rgba(147, 112, 219, 0.1)`，边框
- `.page-title` 改为 24px 紫色渐变文字（`#7b42f6` 到 `#5a32a3`），font-weight 700
- 导出按钮从内容区移至页面头部右侧，与列表页操作栏布局一致
- 移除面包屑导航，保持页面头部简洁

#### 2. 内容区域卡片化
- 三个报告区域（摘要、详细步骤、性能分析）统一使用 `.card-container` 包裹
- `.card-container` 样式：白色背景，圆角 12px，阴影 `0 4px 16px rgba(147, 112, 219, 0.08)`，紫色边框
- 移除 `.report-top-bar` 顶部操作栏，合并到页面头部
- 步骤卡片从 `el-card` 改为 `div.step-card`，外层已有卡片容器包裹

#### 3. 区块标题与内部样式
- `.section-title` 改为平台标准：16px，font-weight 600，左边框 4px 紫色 `#7b42f6`，padding-left 10px，颜色 `#5a32a3`
- 概览卡片 `.overview-card`：改为淡紫色渐变背景，圆角 12px，添加 hover 上浮效果和阴影
- 性能指标卡片 `.metric-card`：同样改为淡紫色渐变背景，圆角 12px，添加 hover 效果
- 统计表格 `.stats-table-content`：行背景色调整为更柔和的色系（success `#f0fdf4`、info `#f8f7ff`、danger `#fef2f2`、warning `#fffbeb`）
- 步骤卡片 `.step-card`：添加白色背景、边框、圆角、hover 阴影和左边框加粗动画
- 所有标签颜色统一为平台紫色系（`#7b42f6`、`#5a32a3`、`#7b6ca0`）

#### 4. 代码清理
- 移除未使用的 `reportTypeDisplay` computed 属性
- 移除 `.report-container` 的自定义滚动条样式，保持简洁

### 涉及文件
- `frontend/src/views/ui-automation/ai/AIExecutionReportPage.vue`

---

## 2026-06-21：测试套件运行截图捕获失败

### 问题原因
- `ai_base.py` 的 `on_step_end` 回调中，截图代码尝试通过 `agent_instance.browser_session` 获取 `page` 对象
- `browser-use` 库的 `BrowserSession` 没有直接的 `page` 属性，也没有 `browser.page` 属性（`browser` 属性不存在或 `page` 为 None）
- 截图代码中 `page = None`，导致 `if page:` 条件不成立，截图逻辑被跳过
- 之前 `get_current_page()` 方法被调用时未加 `await`，返回的是协程对象而非 `Page`，导致 `'coroutine' object has no attribute 'screenshot'` 错误

### 修复方案
#### 1. 修正 page 获取方式
- 将 `browser_session.page` 和 `browser_session.browser.page` 的获取方式改为优先使用 `await browser_session.get_current_page()`
- `get_current_page()` 是 `browser-use` 提供的 async 方法，返回当前活跃的 `Page` 对象
- 保留原有备用方案作为兜底：
  1. `await browser_session.get_current_page()`（主方案）
  2. `browser_session.page`（兼容旧版本）
  3. `browser_session.browser.page` / `browser_session.browser.pages[0]`（兼容旧版本）

#### 2. 确保 async 调用正确
- `on_step_end` 本身是 async 函数，可以直接使用 `await`
- `_capture_and_save` 内部调用 `_page.screenshot()` 也需要 `await`（已在原有代码中正确处理）

### 涉及文件
- `apps/ui_automation/ai_base.py`

---

## 2026-06-21：测试套件运行后前端报告缺少用例分组信息

### 问题原因
- `views.py` 的 `run_suite` 中，每个步骤已正确添加了 `case_name` 字段
- 但 `reports.py` 的 `_parse_detailed_steps()` 在构建 `detailed_steps` 时，没有将 `case_name` 从 `steps_completed` 传递到返回结果中
- 前端 `groupedSteps` computed 属性依赖 `step.case_name` 进行分组，缺少该字段导致无法按用例分组展示

### 修复方案
- 在 `reports.py` 的 `_parse_detailed_steps()` 中，将 `step.get('case_name', '')` 添加到 `detailed_steps` 的每个步骤字典中
- 前端报告页现在可以正确按用例名称分组展示步骤和截图

### 涉及文件
- `apps/ui_automation/reports.py`
