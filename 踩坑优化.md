

## 2026-06-21 AI测试报告详情抽屉-执行日志行级颜色区分

### 问题
执行日志所有文字颜色一致，无法快速区分不同阶段（开始、步骤、执行、成功、错误等），阅读体验差。

### 修改方案
1. **渲染方式**：`<pre>` 纯文本 → `<div>` 逐行渲染，每行根据内容特征动态添加 CSS class
2. **新增 `formatLogLines`**：将日志按 `\n` 拆分为行数组
3. **新增 `getLogLineClass`**：通过正则匹配判断每行类型
4. **颜色体系**：
   - **紫色（#7b42f6）**：分隔线 `===== ... =====`、步骤标识 `[Step X]`
   - **蓝色（#1890ff）**：执行动作 `执行: 访问/点击/输入...`
   - **绿色（#52c41a）**：开始信息、完成/成功
   - **红色（#f5222d）**：错误/失败，带淡红背景高亮
   - **灰色（#8c8c8c）**：提示信息（任务分析、等待等）
   - **默认（#2c3e50）**：普通日志行
5. **交互细节**：每行 hover 时有淡紫背景反馈，提升可读性

### 涉及文件
- `frontend/src/views/ui-automation/ai/AIExecutionRecords.vue`

